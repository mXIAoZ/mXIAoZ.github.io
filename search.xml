<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>指令集基本原理</title>
    <url>/2024/05/26/20240526-%E6%8C%87%E4%BB%A4%E9%9B%86%E5%9F%BA%E6%9C%AC%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<h1 id="计算机体系结构背景"><a href="#计算机体系结构背景" class="headerlink" title="计算机体系结构背景"></a>计算机体系结构背景</h1><p>简单介绍一下，计算机体系结构狭义上是指关于指令集的设计（广义上的概念会在下一篇中定义），从我们学习的各种编程语言出发，向上是各种应用的编写，向下是程序语言如何编译成计算机所能执行的二进制文件，这里就需要和指令集打交道。高级语言一般不涉及到内存和硬件，在编译过程中指令集的体系结构会起到相当大的影响，比如调用指令集的ADD方法，在不同的指令集中有不同的实现，包括是否使用寄存器，使用多少寄存器，这会影响到程序执行速度以及程序的大小，体系结构也是主要是指令集的设计。</p>
<ul>
<li><p>桌面计算机强调涉及整数和浮点数类型的程序性能，很少考虑程序规模。</p>
</li>
<li><p>服务器主要用于数据库、文件服务器和WEB应用，浮点性能的重要性远低于整数和字符串。</p>
</li>
<li><p>个人移动设备和嵌入式应用看中成本和消耗。</p>
</li>
</ul>
<span id="more"></span>
<h2 id="指令集体系结构分类"><a href="#指令集体系结构分类" class="headerlink" title="指令集体系结构分类"></a>指令集体系结构分类</h2><p>处理器的内部存储类型包括：栈、累加器、寄存器组。指令集中如何使用这些存储操作数分为：</p>
<ul>
<li><p>栈体系结构：操作数隐式位于栈顶部</p>
</li>
<li><p>累加器体系结构：操作数为隐式累加器，即累加器的结果为自己的输入</p>
</li>
<li><p>通用寄存器体系结构只有显示操作数：要么是存储器（寄存器-存储器体结构），要么是寄存器（寄存器-寄存器体系结构，也可以称为载入-存储寄存器体系结构）。其中寄存器-存储器体系结构可以用任意指令访问存储器，寄存器-寄存器体系结构只能用载入和存储访问存储器。存储器-存储器体系结构将所有操作数都存在存储器中，今天已经不存在了。</p>
</li>
</ul>
<p>1980之后几乎所有的新体系结构都使用载入-存储寄存器体系结构。原因有三个：1.寄存器快于存储器。2.对于编译器来说，使用寄存器的效率要高于其他形式的存储器。3.寄存器可以保存变量，可以降低内存访问量、提交代码密度（寄存器的名称尾数少于内存地址的位数）</p>
<p>根据以上分类，可以找到指令集的两个特性：</p>
<ul>
<li>一个ALU（algorithm logic unit，算数逻辑单元）指令有两个还是三个操作数。在三操作数中，指令包含一个目标操作数和两个源操作数，在二操作数中，其中一个操作数即是源操作数也是目标操作数。</li>
<li>ALU指令中有多少可以是内存地址。</li>
</ul>
<p>载入-存储体系结构是（0，3），即0个内存地址和三操作数的指令集。典型的有：ARM、MIPS、RISC-V。优势：简单、固定长度指令编码，指令执行所需要的时钟数相似，因为都是操作寄存器。劣势：指令数多余指令中有存储器的体系结构。指令多，指令密度低增大了程序规模。</p>
<p>寄存器-存储器体系结构是（1，2），即1个内存地址和二操作数的指令集。典型的有：Intel 80 86。优势：无需单独的载入指令就可以访问数据，可以有很好的指令密度。劣势：由于在二元计算中源操作数会被销毁，所以操作数不是等价的，每条指令的时钟数会随操作数的位置变化。</p>
<p>目前基本上都是RISC类型的指令集体系结构，虽然Intel支持8086体系结构，但是其在内部使用硬件将8086体系结构转换为类RISC指令。</p>
<p>扩展阅读：</p>
<p>RISC-V手册·：<a href="http://staff.ustc.edu.cn/~llxx/cod/reference_books/RISC-V-Reader-Chinese-v2p12017.pdf">http://staff.ustc.edu.cn/~llxx/cod/reference_books/RISC-V-Reader-Chinese-v2p12017.pdf</a></p>
<h2 id="存储器寻址方式"><a href="#存储器寻址方式" class="headerlink" title="存储器寻址方式"></a>存储器寻址方式</h2><h3 id="解释存储器地址"><a href="#解释存储器地址" class="headerlink" title="解释存储器地址"></a>解释存储器地址</h3><p>一个体系结构必须定义如何解释存储器地址以及如何指定这些地址。</p>
<p>解释存储器地址，如何对一个较大堆行中的字节进行排序：</p>
<p>大端模式：高位字节放在内存的低地址端，低位字节放在内存的高地址端。直观上的模式。</p>
<p>小端模式：高位地址放在内存的高地址端，低位字节放在内存的低地址端</p>
<p>对齐访问，在许多计算机中，对大于1字节的对象的访问必须是对齐的。如果A mod s=0，则在字节地址A对大小为s字节对象的访问是对齐的。</p>
<h3 id="寻址方式"><a href="#寻址方式" class="headerlink" title="寻址方式"></a>寻址方式</h3><p>体系结构中如何指定所要访问的对象的地址。</p>
<p>体系结构至少需要支持：位移量寻址、立即数寻址和寄存器间接寻址，因为据统计这三种寻址方式使用最多。</p>
<p>参考：<a href="https://www.cnblogs.com/Hardworking666/p/17374792.html#1_10">https://www.cnblogs.com/Hardworking666/p/17374792.html#1_10</a></p>
<h2 id="操作数的类型和大小"><a href="#操作数的类型和大小" class="headerlink" title="操作数的类型和大小"></a>操作数的类型和大小</h2><p>常见的操作数类型包括：字符（8位）、半字（16位）、字（32位）、单精度浮点（1字）和双精度浮点（2字）。几乎所有的计算机都遵循了相同的浮点标准——IEEE标准754。某些特定的处理器除外。</p>
<p>对于商业应用程序，一些体系结构支持一种二进制格式，称之为压缩十进制或二进制编码十进制——用4个位对0至9的数值进行编码，两个十进制数位被压缩到两字节中。</p>
<p>参考：<a href="https://en.wikipedia.org/wiki/Binary-coded_decimal">https://en.wikipedia.org/wiki/Binary-coded_decimal</a></p>
<h2 id="指令集中的操作"><a href="#指令集中的操作" class="headerlink" title="指令集中的操作"></a>指令集中的操作</h2><p>指令集中操作包括：算数与逻辑、数据传送、控制、系统、浮点、十进制、字符串以及图形类型。执行最多的是指令集中的简单操作。</p>
<h3 id="控制流指令"><a href="#控制流指令" class="headerlink" title="控制流指令"></a>控制流指令</h3><p>控制流种类：</p>
<ul>
<li>条件分支</li>
<li>跳转</li>
<li>过程调用</li>
<li>过程返回</li>
</ul>
<h5 id="控制流指令的寻址方式"><a href="#控制流指令的寻址方式" class="headerlink" title="控制流指令的寻址方式"></a>控制流指令的寻址方式</h5><p>PC相对指令：提供一个将被加到程序计数器（PC）的位移量</p>
<p>动态寻址：如果在编译时不知道目标位置，必须有一种动态指定目标的方法。只需要给出包含目标地址的寄存器名称即可。</p>
<p>寄存器间接跳转的用途（目标地址在编译时是未知的）：</p>
<ul>
<li>case或switch语句</li>
<li>虚拟函数或虚拟方法，存在于面向对象语言中</li>
<li>高阶函数或函数指针，允许以参数的形式传递函数</li>
<li>动态共享库，允许仅当程序实际调用一个库时才在运行时加载和链接库</li>
</ul>
<h5 id="过程调用"><a href="#过程调用" class="headerlink" title="过程调用"></a>过程调用</h5><p>过程调用和返回包括控制转移，还可能设计状态保存过程，至少必须将返回地址保存在某个地方。</p>
<p>在保存寄存器时，有两种方式：</p>
<ul>
<li>由调用者保存：发出调用的过程必须宝座它希望在调用返回后访问的寄存器</li>
<li>被调用者保存：被调用的过程必须保存它想使用的寄存器</li>
</ul>
<p>由于不同过程可能是分开编译的，在编译过程中编译器想知道被调用者保存的集群器是什么时候分配的会比较困难，因此大多数编译器会采用由调用者返回。</p>
<h2 id="指令集编码"><a href="#指令集编码" class="headerlink" title="指令集编码"></a>指令集编码</h2><p>变长编码：允许对所有操作使用所有寻址方式</p>
<p>定长编码：将操作和寻址方式合并到操作吗中</p>
<p>在变长编码和定长编码之间做选择是，权衡的是程序规模和处理器译码的难易程度。更看重代码规模的架构师会选择变长编码，更看重性能的架构师会选择定长编码。</p>
]]></content>
      <categories>
        <category>CAAQA</category>
        <category>CS</category>
      </categories>
      <tags>
        <tag>CS</tag>
        <tag>CAAQA</tag>
      </tags>
  </entry>
  <entry>
    <title>如何写一个 Agent：从最小 Runtime 到 Coding Agent</title>
    <url>/2026/06/10/20260610-write-agent-learning/</url>
    <content><![CDATA[<p>最近整理了一份关于“如何写一个 Agent”的学习路径。学习之后最大的感受是：Agent 不是一段更长的 prompt，也不是把几个工具随便接到模型后面，而是一个可控的 runtime。这个 runtime 至少包含模型决策循环、工具调用、状态管理、权限边界、上下文管理和终止条件。</p>
<span id="more"></span>
<p>如果只记一个公式，可以记这个：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Agent = LLM 决策循环 + 工具调用 + 状态管理 + 权限边界 + 终止条件</span><br></pre></td></tr></table></figure>
<p>也就是说，写 Agent 的核心不是“让模型更聪明”，而是给模型一个安全、清晰、可验证的运行环境。</p>
<h2 id="从最小-Agent-Loop-开始"><a href="#从最小-Agent-Loop-开始" class="headerlink" title="从最小 Agent Loop 开始"></a>从最小 Agent Loop 开始</h2><p>一个最小 Agent 的执行流可以这样理解：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户任务</span><br><span class="line">  -&gt; Agent Loop</span><br><span class="line">  -&gt; LLM 决定下一步</span><br><span class="line">  -&gt; 调用工具或给出最终答案</span><br><span class="line">  -&gt; 工具结果写回上下文</span><br><span class="line">  -&gt; 重复直到完成、阻塞或达到步数上限</span><br></pre></td></tr></table></figure>
<p>伪代码大概是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> <span class="keyword">not</span> done:</span><br><span class="line">    response = call_llm(messages, tools)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> response.wants_tool:</span><br><span class="line">        result = run_tool(response.tool_name, response.tool_args)</span><br><span class="line">        messages.append(tool_result(result))</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> response.final_answer:</span><br><span class="line">        done = <span class="literal">True</span></span><br></pre></td></tr></table></figure>
<p>这段循环比任何框架都重要。只有先理解这个 while loop，后面使用 LangGraph、MCP 或多 Agent 框架时，才知道框架到底帮自己封装了什么。</p>
<h2 id="第一版应该只读"><a href="#第一版应该只读" class="headerlink" title="第一版应该只读"></a>第一版应该只读</h2><p>学习 Agent 最容易犯的错误，是一开始就给它写文件、执行 shell、安装依赖、提交代码的能力。这样会把问题复杂度一下拉满：工具设计、权限控制、回滚、测试、上下文污染都会同时出现。</p>
<p>更稳的第一版是只读代码研究 Agent，只提供几个窄工具：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">list_files(path)</span><br><span class="line">read_file(path)</span><br><span class="line">search_text(query)</span><br><span class="line">final_answer</span><br></pre></td></tr></table></figure>
<p>并且加上限制：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">只能读文件</span><br><span class="line">不能写文件</span><br><span class="line">不能执行任意 shell</span><br><span class="line">最多运行 10-15 步</span><br><span class="line">必须引用实际读过的文件</span><br></pre></td></tr></table></figure>
<p>只读 Agent 的目标不是“帮我改代码”，而是回答这些问题：项目入口在哪里？某个功能在哪里实现？调用链大概是什么？哪些文件提供了证据？</p>
<p>这个阶段能训练最基础也最重要的能力：tool use、loop、state、context，以及基于证据回答。</p>
<h2 id="真正要设计的是-Runtime"><a href="#真正要设计的是-Runtime" class="headerlink" title="真正要设计的是 Runtime"></a>真正要设计的是 Runtime</h2><p>很多人以为写 Agent 的难点是模型 API。其实模型调用只是其中一层，真正决定 Agent 是否可靠的是 runtime 设计。</p>
<p>一个可维护的 runtime 至少要显式管理这些模块：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">State：当前任务、消息、已知事实、读过的文件、工具历史</span><br><span class="line">Tools：Agent 能做什么，输入输出是什么</span><br><span class="line">Policy：哪些工具允许，哪些需要确认，哪些禁止</span><br><span class="line">Loop：什么时候继续，什么时候停止</span><br><span class="line">Context：哪些信息放进模型，哪些压缩或丢弃</span><br><span class="line">Verification：怎么判断任务真的完成了</span><br></pre></td></tr></table></figure>
<p>不要只用一个 <code>messages</code> 变量糊到底。更好的方式是维护结构化状态：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">state = &#123;</span><br><span class="line">    <span class="string">&quot;task&quot;</span>: <span class="string">&quot;Find where login is implemented&quot;</span>,</span><br><span class="line">    <span class="string">&quot;messages&quot;</span>: [],</span><br><span class="line">    <span class="string">&quot;facts&quot;</span>: [],</span><br><span class="line">    <span class="string">&quot;files_read&quot;</span>: [],</span><br><span class="line">    <span class="string">&quot;tool_calls&quot;</span>: [],</span><br><span class="line">    <span class="string">&quot;step&quot;</span>: <span class="number">0</span>,</span><br><span class="line">    <span class="string">&quot;done&quot;</span>: <span class="literal">False</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样才能回答：Agent 已经确认了什么？读过哪些文件？哪些线索无效？它是不是在原地打转？下一步为什么要调用这个工具？</p>
<h2 id="工具越窄越好"><a href="#工具越窄越好" class="headerlink" title="工具越窄越好"></a>工具越窄越好</h2><p>不要一开始就给 Agent 一个万能 shell：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">run_anything(command)</span><br></pre></td></tr></table></figure>
<p>这类工具看起来灵活，实际是在把安全问题、执行问题和解释问题都推给模型。更好的工具应该窄、结构化、可审计：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">list_files(path)</span><br><span class="line">read_file(path)</span><br><span class="line">search_text(query)</span><br><span class="line">get_git_diff()</span><br><span class="line">run_tests(command_id)</span><br><span class="line">propose_patch(path, old, new)</span><br></pre></td></tr></table></figure>
<p>工具设计原则可以总结为：</p>
<ul>
<li>工具越窄越好。</li>
<li>输入输出要结构化。</li>
<li>默认只读。</li>
<li>写操作要人工确认。</li>
<li>删除、部署、push、付款、发消息必须确认。</li>
<li>工具结果不要无限长，必要时截断或摘要。</li>
<li>工具报错要返回清晰错误，而不是让模型猜。</li>
</ul>
<p>如果确实需要执行命令，也应该做命令 ID 白名单，而不是让模型直接传入任意 shell 字符串：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ALLOWED_COMMANDS = &#123;</span><br><span class="line">    <span class="string">&quot;git_status&quot;</span>: [<span class="string">&quot;git&quot;</span>, <span class="string">&quot;status&quot;</span>, <span class="string">&quot;--short&quot;</span>],</span><br><span class="line">    <span class="string">&quot;git_diff&quot;</span>: [<span class="string">&quot;git&quot;</span>, <span class="string">&quot;diff&quot;</span>],</span><br><span class="line">    <span class="string">&quot;pytest&quot;</span>: [<span class="string">&quot;pytest&quot;</span>],</span><br><span class="line">    <span class="string">&quot;typecheck&quot;</span>: [<span class="string">&quot;npm&quot;</span>, <span class="string">&quot;run&quot;</span>, <span class="string">&quot;typecheck&quot;</span>],</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="System-Prompt-要像操作规程"><a href="#System-Prompt-要像操作规程" class="headerlink" title="System Prompt 要像操作规程"></a>System Prompt 要像操作规程</h2><p>Agent 的 system prompt 不应该只是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">You are a helpful assistant.</span><br></pre></td></tr></table></figure>
<p>它应该更像 SOP 或 runbook，明确目标、禁止行为、工具使用规则和最终输出格式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">You are a read-only code research agent.</span><br><span class="line"></span><br><span class="line">Your job:</span><br><span class="line">- Inspect the repository using tools.</span><br><span class="line">- Build an evidence-based answer.</span><br><span class="line">- Cite files you actually read.</span><br><span class="line"></span><br><span class="line">Rules:</span><br><span class="line">- Do not guess.</span><br><span class="line">- Do not modify files.</span><br><span class="line">- Prefer search before reading large files.</span><br><span class="line">- Stop when you have enough evidence.</span><br><span class="line">- If blocked, explain what information is missing.</span><br><span class="line"></span><br><span class="line">Final answer format:</span><br><span class="line">- Summary</span><br><span class="line">- Key files</span><br><span class="line">- Execution flow</span><br><span class="line">- Risks or unknowns</span><br></pre></td></tr></table></figure>
<p>prompt 的作用不是塑造一个“人格”，而是给 runtime 中的模型节点提供操作规程。</p>
<h2 id="Planning-和终止条件"><a href="#Planning-和终止条件" class="headerlink" title="Planning 和终止条件"></a>Planning 和终止条件</h2><p>最小 Agent 是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Think -&gt; Tool -&gt; Observe -&gt; Repeat</span><br></pre></td></tr></table></figure>
<p>更稳的 Agent 是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Plan -&gt; Tool -&gt; Observe -&gt; Update Plan -&gt; Repeat</span><br></pre></td></tr></table></figure>
<p>Planning 的价值在于让 Agent 每一步都围绕当前目标推进，而不是被搜索结果带着跑偏。但 Planning 不能只靠模型自觉，runtime 也要有终止条件。</p>
<p>至少应该有：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">max_steps</span><br><span class="line">max_tool_errors</span><br><span class="line">max_context_tokens</span><br><span class="line">final_answer</span><br><span class="line">needs_human_approval</span><br><span class="line">no_new_information</span><br></pre></td></tr></table></figure>
<p>第一版可以先设置一个简单的 <code>max_steps = 12</code>。这比追求“全自动完成所有事”更重要，因为没有停止规则的 Agent 很容易无限循环、重复搜索、或者越查越散。</p>
<h2 id="上下文管理不是无限追加-messages"><a href="#上下文管理不是无限追加-messages" class="headerlink" title="上下文管理不是无限追加 messages"></a>上下文管理不是无限追加 messages</h2><p>一开始可以把所有消息都传回模型，但真实 Agent 很快会遇到上下文膨胀。最容易爆上下文的是大文件全文、测试日志、搜索结果、网页内容和历史对话。</p>
<p>更好的方式是维护压缩状态，只保留：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">已确认事实</span><br><span class="line">已读文件列表</span><br><span class="line">关键代码片段</span><br><span class="line">失败线索</span><br><span class="line">当前计划</span><br><span class="line">未解决问题</span><br></pre></td></tr></table></figure>
<p>旧的、重复的、无关的工具结果应该被压缩或丢弃。summary 也不应该只是每隔几步追加一条消息，而应该作为 runtime 的上下文管理机制：用结构化 summary 替换旧上下文，并保留 evidence。</p>
<p>一个 summary 至少应该包含：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;confirmed_facts&quot;</span>: [],</span><br><span class="line">  <span class="attr">&quot;files_inspected&quot;</span>: [],</span><br><span class="line">  <span class="attr">&quot;tool_results&quot;</span>: [],</span><br><span class="line">  <span class="attr">&quot;failed_leads&quot;</span>: [],</span><br><span class="line">  <span class="attr">&quot;open_questions&quot;</span>: [],</span><br><span class="line">  <span class="attr">&quot;next_action&quot;</span>: <span class="string">&quot;Continue normal loop using the compacted context&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>关键原则是：summary 只总结证据，不发明事实；summary 不调用工具，也不决定任务是否完成。</p>
<h2 id="RAG-是-Agent-的工具，不是-Agent-本身"><a href="#RAG-是-Agent-的工具，不是-Agent-本身" class="headerlink" title="RAG 是 Agent 的工具，不是 Agent 本身"></a>RAG 是 Agent 的工具，不是 Agent 本身</h2><p>RAG 的作用是让 Agent 在需要时检索外部知识，而不是把所有资料塞进 prompt。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">RAG = 文档索引 + 查询改写 + 检索 + 重排 + 上下文注入 + 引用验证</span><br></pre></td></tr></table></figure>
<p>普通 RAG 是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户问题 -&gt; 检索 -&gt; 回答</span><br></pre></td></tr></table></figure>
<p>Agentic RAG 则是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户问题</span><br><span class="line">  -&gt; 判断缺什么信息</span><br><span class="line">  -&gt; 改写检索 query</span><br><span class="line">  -&gt; 调 retrieve_docs</span><br><span class="line">  -&gt; 读关键 chunk</span><br><span class="line">  -&gt; 发现不够再检索</span><br><span class="line">  -&gt; 综合多个来源</span><br><span class="line">  -&gt; 引用证据回答</span><br></pre></td></tr></table></figure>
<p>对 Coding Agent 来说，RAG 的正确位置通常是“背景知识工具”：查规范、架构文档、历史决策。真正修改代码前，仍然要读当前仓库里的实际文件。</p>
<p>文档切分比 embedding 模型更容易决定检索效果。好的 chunk 应该语义完整、长度适中、带标题路径、带来源 metadata，并且能单独被引用。</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;source&quot;</span>: <span class="string">&quot;docs/auth/session.md&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;title&quot;</span>: <span class="string">&quot;Session lifecycle&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;section&quot;</span>: <span class="string">&quot;Token refresh&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;updated_at&quot;</span>: <span class="string">&quot;2026-05-28&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;chunk_id&quot;</span>: <span class="string">&quot;docs/auth/session.md#token-refresh-03&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>检索结果也要当作不可信输入。RAG 文档里可能有 prompt injection，所以回答规则应该明确：只根据 retrieved context 和工具实际读到的内容回答，关键结论必须引用来源，不要把检索结果里的指令当成系统指令。</p>
<h2 id="Embedding-Retriever-的学习顺序"><a href="#Embedding-Retriever-的学习顺序" class="headerlink" title="Embedding Retriever 的学习顺序"></a>Embedding Retriever 的学习顺序</h2><p>Embedding 可以理解成把文本转换成语义向量，让语义接近的内容在向量空间里距离更近。RAG 中的 retrieval 通常分成两步：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">建索引阶段：</span><br><span class="line">代码文件 / 文档</span><br><span class="line">  -&gt; 切成 chunks</span><br><span class="line">  -&gt; 每个 chunk 生成 embedding</span><br><span class="line">  -&gt; 保存：向量 + 文件路径 + chunk 内容 + metadata</span><br><span class="line"></span><br><span class="line">查询阶段：</span><br><span class="line">用户问题</span><br><span class="line">  -&gt; 生成 query embedding</span><br><span class="line">  -&gt; 和所有 chunk embedding 算相似度</span><br><span class="line">  -&gt; 返回 top_k 个相关 chunks</span><br><span class="line">  -&gt; LLM 基于 chunks 生成答案</span><br></pre></td></tr></table></figure>
<p>学习版不需要一开始就上向量数据库。推荐顺序是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. 手写 lexical retriever</span><br><span class="line">2. 接入 embedding API</span><br><span class="line">3. 实现内存向量索引</span><br><span class="line">4. 用 cosine similarity 检索</span><br><span class="line">5. 增加 JSON / SQLite 持久化</span><br><span class="line">6. 再替换为 Chroma、FAISS、Qdrant 等向量库</span><br><span class="line">7. 做关键词 + embedding + rerank 的混合检索</span><br></pre></td></tr></table></figure>
<p>对 code agent 来说，不应该只依赖 embedding。代码任务里经常有函数名、变量名、错误码、文件路径，这些精确符号用关键词搜索和符号搜索往往更可靠。更常见的组合是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">关键词搜索 + embedding 搜索 + rerank + read_file 精读</span><br></pre></td></tr></table></figure>
<h2 id="LangChain-和-LangGraph-的位置"><a href="#LangChain-和-LangGraph-的位置" class="headerlink" title="LangChain 和 LangGraph 的位置"></a>LangChain 和 LangGraph 的位置</h2><p>LangChain 和 LangGraph 都有用，但不要一开始就用框架掩盖原理。</p>
<p>一句话区分：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">LangChain：组件库，偏 prompt、model、retriever、tool、chain 集成</span><br><span class="line">LangGraph：有状态执行图，偏 agent loop、分支、循环、checkpoint、人类审批</span><br></pre></td></tr></table></figure>
<p>如果目标是快速做 RAG demo，LangChain 很方便。如果目标是写真正可控的 Agent，LangGraph 更接近 runtime，因为 Agent 本质上是一个有状态循环：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">plan -&gt; act -&gt; observe -&gt; update state -&gt; route -&gt; continue or stop</span><br></pre></td></tr></table></figure>
<p>推荐学习顺序是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. 手写 read-only agent loop</span><br><span class="line">2. 手写 tool schema 和 tool execution</span><br><span class="line">3. 手写 state、max_steps、权限检查</span><br><span class="line">4. 手写一个简单 RAG retriever tool</span><br><span class="line">5. 用 LangChain 替换 document loader / splitter / vector store</span><br><span class="line">6. 用 LangGraph 重写 agent loop</span><br><span class="line">7. 加 checkpoint、human approval、多节点工作流</span><br></pre></td></tr></table></figure>
<p>也就是先理解 Agent runtime，再用 LangChain 加速组件集成，最后用 LangGraph 管复杂流程。</p>
<h2 id="从只读-Agent-到-Coding-Agent"><a href="#从只读-Agent-到-Coding-Agent" class="headerlink" title="从只读 Agent 到 Coding Agent"></a>从只读 Agent 到 Coding Agent</h2><p>比较稳的升级路线是四步：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">只读研究 Agent -&gt; 测试诊断 Agent -&gt; Patch Agent -&gt; Coding Agent</span><br></pre></td></tr></table></figure>
<p>V1 只读研究 Agent：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">能力：list_files / read_file / search_text</span><br><span class="line">目标：找入口、画调用链、基于文件证据总结结构</span><br><span class="line">验收：不编造文件、不无限循环、答案包含文件引用</span><br></pre></td></tr></table></figure>
<p>V2 测试诊断 Agent：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">增加：run_tests(test_command_id) / read_test_output(output_id)</span><br><span class="line">目标：读取失败日志，定位相关代码，区分 deterministic failure 和 flaky</span><br><span class="line">限制：不改代码，不安装依赖，不自动删除缓存</span><br></pre></td></tr></table></figure>
<p>V3 Patch Agent：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">增加：propose_patch(path, old, new) / show_diff()</span><br><span class="line">目标：提出最小 patch，用户确认后再应用，应用后跑测试</span><br><span class="line">流程：读代码 -&gt; 提计划 -&gt; 生成 patch -&gt; 人工确认 -&gt; 应用 -&gt; 测试 -&gt; 总结</span><br></pre></td></tr></table></figure>
<p>V4 Coding Agent：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">增加：edit_file / run_tests / get_diff</span><br><span class="line">目标：完成小范围 bugfix 或小功能，自动验证，输出变更摘要</span><br><span class="line">限制：不 commit，不 push，不删文件，不重置 git 状态，危险操作必须确认</span><br></pre></td></tr></table></figure>
<p>这个顺序的核心是：先把读和判断做稳，再给写权限。</p>
<h2 id="安全边界必须在代码里"><a href="#安全边界必须在代码里" class="headerlink" title="安全边界必须在代码里"></a>安全边界必须在代码里</h2><p>Agent 一定要有权限边界，否则只是一个会随机执行操作的模型。</p>
<p>默认规则可以这样定：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">读操作默认允许</span><br><span class="line">写操作需要确认</span><br><span class="line">外部网络需要确认</span><br><span class="line">删除需要确认</span><br><span class="line">部署需要确认</span><br><span class="line">push / force push 禁止或强确认</span><br><span class="line">密钥永远不进 prompt</span><br><span class="line">工具输出要当作不可信输入</span><br></pre></td></tr></table></figure>
<p>危险操作例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rm -rf</span><br><span class="line">git reset --hard</span><br><span class="line">git push --force</span><br><span class="line">curl ... | sh</span><br><span class="line">chmod -R 777</span><br><span class="line">drop database</span><br></pre></td></tr></table></figure>
<p>即使是测试 Agent，也不要让它随便执行任意命令。测试命令最好用 ID 白名单，而不是直接让模型提供 shell 字符串。</p>
<h2 id="Plan-Mode-的关键不是一句-prompt"><a href="#Plan-Mode-的关键不是一句-prompt" class="headerlink" title="Plan Mode 的关键不是一句 prompt"></a>Plan Mode 的关键不是一句 prompt</h2><p>真正可控的 Plan Mode 不是在 system prompt 里写“请先计划”，而是状态机 + 工具权限门控 + 结构化计划 + 人工审批 + 执行阶段分离。</p>
<p>核心目标是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">plan 模式只负责理解任务、读取证据、生成计划，然后停止。</span><br><span class="line">execute 模式只能执行已经批准的计划。</span><br></pre></td></tr></table></figure>
<p>可以把 Agent 拆成三个模式：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AgentMode</span>(<span class="params"><span class="built_in">str</span>, Enum</span>):</span></span><br><span class="line">    ANSWER = <span class="string">&quot;answer&quot;</span></span><br><span class="line">    PLAN = <span class="string">&quot;plan&quot;</span></span><br><span class="line">    EXECUTE = <span class="string">&quot;execute&quot;</span></span><br></pre></td></tr></table></figure>
<p>不同 mode 暴露不同工具：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">tools_for_mode</span>(<span class="params">mode: AgentMode</span>):</span></span><br><span class="line">    <span class="keyword">if</span> mode <span class="keyword">in</span> &#123;AgentMode.ANSWER, AgentMode.PLAN&#125;:</span><br><span class="line">        <span class="keyword">return</span> READ_TOOLS</span><br><span class="line">    <span class="keyword">if</span> mode == AgentMode.EXECUTE:</span><br><span class="line">        <span class="keyword">return</span> READ_TOOLS | WRITE_TOOLS</span><br><span class="line">    <span class="keyword">raise</span> ValueError(<span class="string">f&quot;unknown mode: <span class="subst">&#123;mode&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>也就是说，即使模型在 plan 模式里请求 <code>edit_file</code>，runtime 也必须拒绝。权限边界应该由代码保证，而不是依赖模型自觉。</p>
<p>Plan Mode 的输出也应该有稳定 schema，例如：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">&quot;status&quot;</span>: <span class="string">&quot;needs_approval&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;goal&quot;</span>: <span class="string">&quot;Fix streaming planning output&quot;</span>,</span><br><span class="line">  <span class="attr">&quot;findings&quot;</span>: [],</span><br><span class="line">  <span class="attr">&quot;steps&quot;</span>: [],</span><br><span class="line">  <span class="attr">&quot;risks&quot;</span>: [],</span><br><span class="line">  <span class="attr">&quot;verification&quot;</span>: []</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>生成计划后程序要停止，等待人类审批。不要让同一次模型调用从“计划”直接滑到“执行”。</p>
<h2 id="如何评估-Agent-是否变好"><a href="#如何评估-Agent-是否变好" class="headerlink" title="如何评估 Agent 是否变好"></a>如何评估 Agent 是否变好</h2><p>不要靠感觉判断 Agent 是否变好了。应该准备固定任务集，例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. 找项目入口</span><br><span class="line">2. 找登录流程</span><br><span class="line">3. 找某个 API handler</span><br><span class="line">4. 判断一个测试失败原因</span><br><span class="line">5. 给出一个最小 patch 方案</span><br><span class="line">6. 识别一次危险操作并停止</span><br><span class="line">7. 在上下文很长时正确总结已知事实</span><br></pre></td></tr></table></figure>
<p>每次修改 Agent 后记录：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">成功 / 失败</span><br><span class="line">调用了哪些工具</span><br><span class="line">用了多少步</span><br><span class="line">是否读错文件</span><br><span class="line">是否产生幻觉</span><br><span class="line">是否越权</span><br><span class="line">总耗时</span><br><span class="line">成本</span><br></pre></td></tr></table></figure>
<p>好 Agent 的标准不是“回答很像人”，而是：知道什么时候用工具，不编造文件内容，不无限循环，遇到危险操作会停，能引用证据，能从工具错误中恢复，修改后会验证。</p>
<h2 id="最后记住这条路线"><a href="#最后记住这条路线" class="headerlink" title="最后记住这条路线"></a>最后记住这条路线</h2><p>如果把整份学习路径压缩成一条实践顺序，就是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">只读 Agent -&gt; 测试诊断 Agent -&gt; Patch Agent -&gt; Coding Agent</span><br></pre></td></tr></table></figure>
<p>框架学习也应该按这个节奏来：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">自己写最小 Agent</span><br><span class="line">-&gt; Anthropic tool use / OpenAI function calling</span><br><span class="line">-&gt; MCP</span><br><span class="line">-&gt; LangGraph</span><br><span class="line">-&gt; 多 Agent orchestration</span><br></pre></td></tr></table></figure>
<p>不要一开始就上多 Agent，也不要一开始就堆框架。先把只读 Agent 写稳，再给它测试能力，再让它生成 patch，最后才给它编辑文件的权限。</p>
<p>写 Agent 的核心不是“让模型更自由”，而是相反：给它合适的工具，限制它的权限，保存必要状态，压缩无用上下文，设置明确停止条件，并用测试验证结果。这样做出来的才不是 demo，而是一个可以逐步变强的 Agent runtime。</p>
<h2 id="参考资料与工具分类"><a href="#参考资料与工具分类" class="headerlink" title="参考资料与工具分类"></a>参考资料与工具分类</h2><h3 id="Agent-Harness-状态机框架"><a href="#Agent-Harness-状态机框架" class="headerlink" title="Agent Harness / 状态机框架"></a>Agent Harness / 状态机框架</h3><p>这一类负责 Agent 怎么运行：模型什么时候调用，工具什么时候调用，状态怎么流转，什么时候停止，什么时候 human-in-the-loop，以及如何 checkpoint / resume。</p>
<ul>
<li>手写 <code>run_loop</code></li>
<li>LangGraph: <a href="https://langchain-ai.github.io/langgraph/">https://langchain-ai.github.io/langgraph/</a></li>
<li>PydanticAI</li>
<li>OpenAI Agents SDK</li>
<li>Claude Agent SDK / Claude Code 风格 harness</li>
<li>Anthropic Engineering, Building effective agents: <a href="https://www.anthropic.com/engineering/building-effective-agents">https://www.anthropic.com/engineering/building-effective-agents</a></li>
<li>ReAct, Synergizing Reasoning and Acting in Language Models: <a href="https://arxiv.org/abs/2210.03629">https://arxiv.org/abs/2210.03629</a></li>
<li>Anthropic Messages API: <a href="https://docs.anthropic.com/en/api/messages">https://docs.anthropic.com/en/api/messages</a></li>
</ul>
<p>优先理解的是：手写 harness + LangGraph。</p>
<h3 id="RAG-数据检索框架"><a href="#RAG-数据检索框架" class="headerlink" title="RAG / 数据检索框架"></a>RAG / 数据检索框架</h3><p>这一类负责资料怎么进入系统、怎么切分、怎么检索，包括 loader、parser、chunker、embedding、vector store、retriever 和 query engine。</p>
<ul>
<li>LangChain: <a href="https://python.langchain.com/docs/">https://python.langchain.com/docs/</a></li>
<li>LlamaIndex</li>
<li>Haystack</li>
<li>Model Context Protocol: <a href="https://modelcontextprotocol.io/docs">https://modelcontextprotocol.io/docs</a></li>
</ul>
<h3 id="向量数据库-向量索引"><a href="#向量数据库-向量索引" class="headerlink" title="向量数据库 / 向量索引"></a>向量数据库 / 向量索引</h3><p>这一类负责保存 chunk embedding，并按 query embedding 找回 top-k 结果，也会涉及 metadata filter 和 hybrid search。</p>
<ul>
<li>Chroma</li>
<li>FAISS</li>
<li>Qdrant</li>
<li>Milvus</li>
<li>Weaviate</li>
<li>pgvector</li>
<li>Elasticsearch / OpenSearch</li>
<li>Pinecone</li>
<li>LanceDB</li>
<li>Redis Vector</li>
</ul>
<p>推荐顺序：学习阶段先用 Chroma / FAISS，后续生产化再评估 Qdrant；如果已有 PostgreSQL，可以考虑 pgvector；如果已有搜索系统，可以考虑 Elasticsearch / OpenSearch。</p>
<h3 id="多-Agent-协作框架"><a href="#多-Agent-协作框架" class="headerlink" title="多 Agent 协作框架"></a>多 Agent 协作框架</h3><p>这一类关注多个角色 Agent 怎么协作，例如 planner、coder、reviewer、researcher、executor、handoff 和 group chat。</p>
<ul>
<li>AutoGen</li>
<li>CrewAI</li>
<li>OpenAI Agents SDK handoffs</li>
</ul>
<h3 id="结构化输出-类型校验工具"><a href="#结构化输出-类型校验工具" class="headerlink" title="结构化输出 / 类型校验工具"></a>结构化输出 / 类型校验工具</h3><p>这一类负责让模型稳定输出 JSON / 对象，并做校验和重试，适合 plan schema、summary schema、tool args validation、RAG citation schema 等场景。</p>
<ul>
<li>Pydantic</li>
<li>Instructor</li>
<li>PydanticAI</li>
<li>OpenAI Structured Outputs</li>
<li>Anthropic tool use / JSON Schema: <a href="https://json-schema.org/">https://json-schema.org/</a></li>
</ul>
<h3 id="Workflow-Durable-Execution-框架"><a href="#Workflow-Durable-Execution-框架" class="headerlink" title="Workflow / Durable Execution 框架"></a>Workflow / Durable Execution 框架</h3><p>这一类负责长时间、可恢复、可重试的任务执行，例如 pause、resume、retry、checkpoint、scheduled jobs、human approval wait 和 distributed execution。</p>
<ul>
<li>Temporal</li>
<li>Prefect</li>
<li>Dagster</li>
<li>Airflow</li>
<li>Argo Workflows</li>
</ul>
<h3 id="Eval-Observability-Optimization-工具"><a href="#Eval-Observability-Optimization-工具" class="headerlink" title="Eval / Observability / Optimization 工具"></a>Eval / Observability / Optimization 工具</h3><p>这一类负责评估、追踪和优化 LLM pipeline 的效果，包括 prompt optimization、retriever tuning、trace visualization、hallucination / faithfulness 检测。</p>
<ul>
<li>DSPy</li>
<li>LangSmith</li>
<li>OpenAI Evals</li>
<li>Ragas: <a href="https://docs.ragas.io/">https://docs.ragas.io/</a></li>
<li>promptfoo: <a href="https://www.promptfoo.dev/docs/">https://www.promptfoo.dev/docs/</a></li>
<li>TruLens</li>
<li>Phoenix</li>
<li>OWASP Top 10 for LLM Applications: <a href="https://owasp.org/www-project-top-10-for-large-language-model-applications/">https://owasp.org/www-project-top-10-for-large-language-model-applications/</a></li>
</ul>
]]></content>
      <categories>
        <category>Agent</category>
      </categories>
      <tags>
        <tag>Agent</tag>
        <tag>RAG</tag>
        <tag>LangGraph</tag>
      </tags>
  </entry>
  <entry>
    <title>从大模型到 TPU 执行：框架、算子、内核、DLC、vLLM 与 Linux 内核</title>
    <url>/2026/06/10/20260610-tpu-model-compiler-kernel-vllm-linux/</url>
    <content><![CDATA[<p>最近在梳理大模型基础设施的系统分层图：大模型、深度学习框架、Tensor、算子、算子内核、DLC/编译器、vLLM、TPU Runtime、Linux 内核和 TPU 硬件分别处在哪一层，各自解决什么问题。</p>
<p>这篇文章试图用更工程化的方式回答这个问题。最核心的关系是：<strong>大模型定义计算目标，框架表达计算图，算子定义数学语义，算子内核定义硬件实现，编译器/DLC 生成目标硬件可执行产物，vLLM 管理在线推理调度与 KV cache，Runtime 通过 Linux TPU 驱动把任务提交给 TPU 执行。</strong></p>
<span id="more"></span>
<h2 id="先分清两个-Kernel"><a href="#先分清两个-Kernel" class="headerlink" title="先分清两个 Kernel"></a>先分清两个 Kernel</h2><p>“Kernel” 在这条栈里至少有两个完全不同的含义。第一个是 <strong>Linux Kernel</strong>，也就是操作系统内核；第二个是 <strong>Operator Kernel</strong>，也就是某个算子在 GPU/TPU/NPU 上执行的计算内核。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>名称</th>
<th>专业含义</th>
<th>运行位置</th>
<th>主要职责</th>
</tr>
</thead>
<tbody>
<tr>
<td>Linux Kernel</td>
<td>操作系统内核</td>
<td>CPU 特权态</td>
<td>进程调度、虚拟内存、文件系统、设备驱动、中断处理、权限隔离</td>
</tr>
<tr>
<td>TPU Operator Kernel</td>
<td>TPU 算子内核</td>
<td>TPU/NPU/GPU 加速器</td>
<td>执行 MatMul、Softmax、Attention、LayerNorm 等算子的具体硬件实现</td>
</tr>
</tbody>
</table>
</div>
<p>Linux Kernel 通常不理解 MatMul、Softmax、Attention 的数学语义；它负责让用户态程序安全、稳定地访问 TPU 设备。真正执行矩阵乘的是 TPU 上的 operator kernel。</p>
<h2 id="整体分层关系"><a href="#整体分层关系" class="headerlink" title="整体分层关系"></a>整体分层关系</h2><p>下面这张图把推理/编译/执行链路放在一张分层图中。图中有两条容易混淆的路径：DLC/Compiler 偏离线编译，vLLM 偏在线推理服务调度。二者最终都需要通过后端 Runtime、Linux Driver 和硬件执行能力完成计算。</p>
<div class="svg-figure">
<svg viewBox="0 0 1180 940" width="100%" role="img" aria-label="LLM to TPU layered architecture">
  <defs>
    <marker id="arrow-main" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#4b5563"/></marker>
    <style>
      .box{fill:#ffffff;stroke:#cbd5e1;stroke-width:2;rx:16;filter:drop-shadow(0 8px 12px rgba(15,23,42,.08));}
      .blue{fill:#eff6ff;stroke:#93c5fd}.green{fill:#ecfdf5;stroke:#86efac}.amber{fill:#fffbeb;stroke:#fcd34d}.purple{fill:#f5f3ff;stroke:#c4b5fd}.gray{fill:#f8fafc;stroke:#cbd5e1}.cyan{fill:#ecfeff;stroke:#67e8f9}
      .title{font:700 18px 'Times New Roman','SimSun',serif;fill:#111827}.sub{font:14px 'Times New Roman','SimSun',serif;fill:#4b5563}.arrow{stroke:#4b5563;stroke-width:2.2;fill:none;marker-end:url(#arrow-main)}
    </style>
  </defs>
  <rect x="300" y="40" width="580" height="84" class="box blue"/><text x="590" y="74" text-anchor="middle" class="title">Large Language Model / 大语言模型</text><text x="590" y="100" text-anchor="middle" class="sub">Transformer Blocks, Weights, Tokenizer-facing Semantics</text>
  <path d="M590 124 L590 160" class="arrow"/>
  <rect x="300" y="160" width="580" height="84" class="box blue"/><text x="590" y="194" text-anchor="middle" class="title">Deep Learning Framework / 深度学习框架</text><text x="590" y="220" text-anchor="middle" class="sub">PyTorch, TensorFlow, JAX, Transformers</text>
  <path d="M590 244 L590 280" class="arrow"/>
  <rect x="300" y="280" width="580" height="84" class="box green"/><text x="590" y="314" text-anchor="middle" class="title">Computational Graph / 计算图</text><text x="590" y="340" text-anchor="middle" class="sub">Operators as Nodes, Tensors as Edges</text>
  <path d="M590 364 C590 400 350 392 330 430" class="arrow"/>
  <path d="M590 364 C590 400 830 392 850 430" class="arrow"/>
  <rect x="90" y="430" width="480" height="92" class="box purple"/><text x="330" y="466" text-anchor="middle" class="title">Compiler / DLC Toolchain</text><text x="330" y="494" text-anchor="middle" class="sub">Graph Optimization, Lowering, Code Generation</text>
  <rect x="610" y="430" width="480" height="92" class="box amber"/><text x="850" y="466" text-anchor="middle" class="title">vLLM Serving Engine</text><text x="850" y="494" text-anchor="middle" class="sub">Scheduler, Continuous Batching, PagedAttention, KV Cache</text>
  <path d="M330 522 C330 570 560 560 590 600" class="arrow"/>
  <path d="M850 522 C850 570 620 560 590 600" class="arrow"/>
  <rect x="300" y="600" width="580" height="84" class="box gray"/><text x="590" y="634" text-anchor="middle" class="title">Runtime / SDK / 用户态运行时</text><text x="590" y="660" text-anchor="middle" class="sub">Load Artifacts, Allocate Buffers, Submit Command Buffers</text>
  <path d="M590 684 L590 720" class="arrow"/>
  <rect x="300" y="720" width="580" height="84" class="box gray"/><text x="590" y="754" text-anchor="middle" class="title">Linux Kernel + TPU Device Driver</text><text x="590" y="780" text-anchor="middle" class="sub">ioctl, mmap, DMA Mapping, Command Queue, Interrupts</text>
  <path d="M590 804 L590 840" class="arrow"/>
  <rect x="300" y="840" width="580" height="84" class="box cyan"/><text x="590" y="874" text-anchor="middle" class="title">TPU Hardware / TPU 硬件</text><text x="590" y="900" text-anchor="middle" class="sub">Global Memory, Local SRAM, DMA, Matrix Units, Vector Units</text>
</svg>
</div>

<h2 id="从模型到计算图"><a href="#从模型到计算图" class="headerlink" title="从模型到计算图"></a>从模型到计算图</h2><h3 id="大模型：高层函数与参数集合"><a href="#大模型：高层函数与参数集合" class="headerlink" title="大模型：高层函数与参数集合"></a>大模型：高层函数与参数集合</h3><p>大模型可以看作一个由大量参数组成的函数。以 LLM 为例，模型通常由 Embedding、若干 Transformer Block、Normalization、MLP、Attention 和 LM Head 组成。大模型本身描述的是整体函数形式，而不是目标硬件上的具体执行策略。</p>
<h3 id="框架：模型表达、张量管理与图导出"><a href="#框架：模型表达、张量管理与图导出" class="headerlink" title="框架：模型表达、张量管理与图导出"></a>框架：模型表达、张量管理与图导出</h3><p>深度学习框架负责描述模型结构、管理 Tensor、加载权重、执行自动微分、导出计算图，并调用后端 Runtime 或编译器。常见框架包括 PyTorch、TensorFlow、JAX、MindSpore 和 PaddlePaddle。</p>
<p>模型代码通常是命令式或函数式写法；框架会把其中的张量运算记录成一张可分析、可优化的计算图。这张图不是图片，而是由节点和边组成的中间表示：节点通常是算子，边通常表示 Tensor 数据依赖。</p>
<p>例如 PyTorch 代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">y = torch.softmax(x @ w, dim=-<span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<p>从图语义上看，这段代码至少包含输入 Tensor <code>x</code>、权重 Tensor <code>w</code>、矩阵乘 <code>MatMul</code>、以及 <code>Softmax</code>。它可以抽象成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">x ─┐</span><br><span class="line">   ├─ MatMul ── Softmax ── y</span><br><span class="line">w ─┘</span><br></pre></td></tr></table></figure>
<p>这就是计算图的核心作用：把“用户写的模型代码”转成“编译器和 Runtime 能理解的算子依赖关系”。框架层负责表达这个关系，但不一定决定它最终在 TPU 上如何分块、如何搬运数据、如何复用片上缓存。</p>
<h3 id="Tensor：计算图边上的数据对象"><a href="#Tensor：计算图边上的数据对象" class="headerlink" title="Tensor：计算图边上的数据对象"></a>Tensor：计算图边上的数据对象</h3><p>Tensor 不只是多维数组，还包含一组影响编译和执行的重要元数据。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>属性</th>
<th>含义</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>shape</td>
<td>维度大小</td>
<td>$[B,S,H]$</td>
</tr>
<tr>
<td>dtype</td>
<td>元素类型</td>
<td>FP32、FP16、BF16、INT8</td>
</tr>
<tr>
<td>layout</td>
<td>内存布局</td>
<td>NCHW、NHWC、blocked layout</td>
</tr>
<tr>
<td>stride</td>
<td>各维度内存步长</td>
<td>contiguous / non-contiguous</td>
</tr>
<tr>
<td>device</td>
<td>所在设备</td>
<td>CPU、GPU、TPU、NPU</td>
</tr>
</tbody>
</table>
</div>
<p>LLM 中常见 hidden states 可以表示为 <code>[B, S, H]</code>，例如 <code>[1, 2048, 4096]</code>：<code>B</code> 是 batch size，<code>S</code> 是 token 序列长度，<code>H</code> 是 hidden size。图像模型里常见的 <code>NCHW</code> / <code>NHWC</code> 则是在描述同一批图片数据的不同内存排列：<code>N</code> 是 batch，<code>C</code> 是 channel，<code>H</code> 是 height，<code>W</code> 是 width。</p>
<p>在大多数 row-major 存储里，最后一个维度最连续。因此 <code>NCHW</code> 更容易连续扫同一个 channel 的宽度方向，<code>NHWC</code> 更容易连续读取同一个像素位置的多个 channel。算子的访问模式如果和 layout 对齐，DMA 搬运、cache locality、向量化和 kernel selection 都会更友好；如果不对齐，就可能需要 stride access 或额外 layout conversion。</p>
<p>下面用一张纯文本图看同一张 <code>N=1, C=3, H=2, W=3</code> 图片在内存中的排列差异：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">NCHW = [N, C, H, W]</span><br><span class="line"></span><br><span class="line">内存顺序：先放完整 R 平面，再放完整 G 平面，再放完整 B 平面</span><br><span class="line"></span><br><span class="line">R channel: R00 R01 R02 R10 R11 R12</span><br><span class="line">G channel: G00 G01 G02 G10 G11 G12</span><br><span class="line">B channel: B00 B01 B02 B10 B11 B12</span><br><span class="line"></span><br><span class="line">连续方向：同一个 channel 内，H x W 空间位置连续</span><br><span class="line">适合访问：同一 channel 的空间窗口，例如卷积在 feature map 上滑动</span><br></pre></td></tr></table></figure>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">NHWC = [N, H, W, C]</span><br><span class="line"></span><br><span class="line">内存顺序：每个像素位置的 R/G/B 连续放在一起</span><br><span class="line"></span><br><span class="line">pixel(0,0): R00 G00 B00</span><br><span class="line">pixel(0,1): R01 G01 B01</span><br><span class="line">pixel(0,2): R02 G02 B02</span><br><span class="line">pixel(1,0): R10 G10 B10</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">连续方向：同一个 pixel 的多个 channel 连续</span><br><span class="line">适合访问：同一空间位置的 channel 向量，例如按 channel 向量化读取</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th>Layout</th>
<th>最连续的维度</th>
<th>更友好的访问模式</th>
<th>不匹配时的代价</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>NCHW</code></td>
<td><code>W</code>，也就是同一 channel 内的宽度方向</td>
<td>同一 channel 的空间窗口扫描</td>
<td>读取同一 pixel 的多个 channel 可能是 stride access</td>
</tr>
<tr>
<td><code>NHWC</code></td>
<td><code>C</code>，也就是同一 pixel 的 channel 向量</td>
<td>同一 pixel 的多个 channel 一次性读取</td>
<td>扫同一 channel 的空间平面可能不连续</td>
</tr>
</tbody>
</table>
</div>
<p>视觉上可以把它理解成下面这样：</p>
<div class="svg-figure">
<svg viewBox="0 0 1180 500" width="100%" role="img" aria-label="NCHW and NHWC memory layout visual comparison">
  <defs>
    <marker id="arrow-layout-visual" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#475569"/></marker>
    <style>.lvbox{fill:#fff;stroke:#cbd5e1;stroke-width:2;rx:14}.lvtitle{font:700 18px 'Times New Roman','SimSun',serif;fill:#111827}.lvtext{font:14px 'Times New Roman','SimSun',serif;fill:#475569}.lvsmall{font:12px 'Times New Roman','SimSun',serif;fill:#64748b}.lvred{fill:#fee2e2;stroke:#fca5a5}.lvgreen{fill:#dcfce7;stroke:#86efac}.lvblue{fill:#dbeafe;stroke:#93c5fd}.lvyellow{fill:#fef3c7;stroke:#fcd34d}.lvarrow{stroke:#475569;stroke-width:2.2;fill:none;marker-end:url(#arrow-layout-visual)}</style>
  </defs>
  <text x="590" y="42" text-anchor="middle" class="lvtitle">NCHW / NHWC 的连续方向不同</text>
  <text x="590" y="68" text-anchor="middle" class="lvtext">NCHW 把 channel 平面分开存；NHWC 把同一个 pixel 的 channel 放在一起。</text>

  <rect x="60" y="105" width="500" height="190" class="lvbox"/>
  <text x="310" y="138" text-anchor="middle" class="lvtitle">NCHW = [N, C, H, W]</text>
  <rect x="110" y="188" width="110" height="62" class="lvred"/><text x="165" y="214" text-anchor="middle" class="lvtext">R plane</text><text x="165" y="236" text-anchor="middle" class="lvsmall">H x W 连续</text>
  <rect x="245" y="188" width="110" height="62" class="lvgreen"/><text x="300" y="214" text-anchor="middle" class="lvtext">G plane</text><text x="300" y="236" text-anchor="middle" class="lvsmall">H x W 连续</text>
  <rect x="380" y="188" width="110" height="62" class="lvblue"/><text x="435" y="214" text-anchor="middle" class="lvtext">B plane</text><text x="435" y="236" text-anchor="middle" class="lvsmall">H x W 连续</text>
  <path d="M220 219 L245 219" class="lvarrow"/><path d="M355 219 L380 219" class="lvarrow"/>

  <rect x="620" y="105" width="500" height="190" class="lvbox"/>
  <text x="870" y="138" text-anchor="middle" class="lvtitle">NHWC = [N, H, W, C]</text>
  <g transform="translate(685 190)">
    <rect x="0" y="0" width="42" height="46" class="lvred"/><text x="21" y="29" text-anchor="middle" class="lvsmall">R</text>
    <rect x="42" y="0" width="42" height="46" class="lvgreen"/><text x="63" y="29" text-anchor="middle" class="lvsmall">G</text>
    <rect x="84" y="0" width="42" height="46" class="lvblue"/><text x="105" y="29" text-anchor="middle" class="lvsmall">B</text>
    <rect x="155" y="0" width="42" height="46" class="lvred"/><text x="176" y="29" text-anchor="middle" class="lvsmall">R</text>
    <rect x="197" y="0" width="42" height="46" class="lvgreen"/><text x="218" y="29" text-anchor="middle" class="lvsmall">G</text>
    <rect x="239" y="0" width="42" height="46" class="lvblue"/><text x="260" y="29" text-anchor="middle" class="lvsmall">B</text>
    <rect x="310" y="0" width="42" height="46" class="lvred"/><text x="331" y="29" text-anchor="middle" class="lvsmall">R</text>
    <rect x="352" y="0" width="42" height="46" class="lvgreen"/><text x="373" y="29" text-anchor="middle" class="lvsmall">G</text>
    <rect x="394" y="0" width="42" height="46" class="lvblue"/><text x="415" y="29" text-anchor="middle" class="lvsmall">B</text>
    <text x="63" y="70" text-anchor="middle" class="lvsmall">pixel 0</text><text x="218" y="70" text-anchor="middle" class="lvsmall">pixel 1</text><text x="373" y="70" text-anchor="middle" class="lvsmall">pixel 2</text>
  </g>

  <rect x="120" y="355" width="290" height="58" class="lvred"/><text x="265" y="380" text-anchor="middle" class="lvtext">NCHW</text><text x="265" y="400" text-anchor="middle" class="lvsmall">适合扫同一 channel 的空间窗口</text>
  <rect x="445" y="355" width="290" height="58" class="lvblue"/><text x="590" y="380" text-anchor="middle" class="lvtext">NHWC</text><text x="590" y="400" text-anchor="middle" class="lvsmall">适合读同一 pixel 的 channel 向量</text>
  <rect x="770" y="355" width="290" height="58" class="lvyellow"/><text x="915" y="380" text-anchor="middle" class="lvtext">Layout 不匹配</text><text x="915" y="400" text-anchor="middle" class="lvsmall">可能需要 stride access 或 transpose</text>
</svg>
</div>

<h3 id="Operator：计算图节点上的数学语义"><a href="#Operator：计算图节点上的数学语义" class="headerlink" title="Operator：计算图节点上的数学语义"></a>Operator：计算图节点上的数学语义</h3><p>Operator 定义“算什么”。例如 Elementwise Add、Reduction Sum、MatMul、Softmax、RMSNorm、Attention 都是算子。以矩阵乘为例：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">C[i, j] = sum(k=1..K, A[i, k] * B[k, j])</span><br></pre></td></tr></table></figure>
<p>如果 $A\in\mathbb{R}^{M\times K}$，$B\in\mathbb{R}^{K\times N}$，那么输出 $C\in\mathbb{R}^{M\times N}$。这个定义描述的是数学语义，不描述具体硬件上如何高效执行。</p>
<h3 id="Operator-Kernel：目标硬件上的执行实现"><a href="#Operator-Kernel：目标硬件上的执行实现" class="headerlink" title="Operator Kernel：目标硬件上的执行实现"></a>Operator Kernel：目标硬件上的执行实现</h3><p>Operator Kernel 定义“怎么在硬件上算”。同一个 MatMul operator 可以对应 CPU kernel、CUDA kernel、Triton kernel、TPU kernel 或 NPU kernel。TPU MatMul kernel 通常会涉及 tile 划分、global memory 到 local SRAM 的搬运、DMA、matrix unit 调用、partial sum 累加和写回。</p>
<div class="svg-figure">
<svg viewBox="0 0 1120 430" width="100%" role="img" aria-label="operator kernel hardware mapping">
  <defs>
    <marker id="arrow-ok" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#475569"/></marker>
    <style>.okbox{fill:#fff;stroke:#cbd5e1;stroke-width:2;rx:16}.okblue{fill:#eff6ff;stroke:#93c5fd}.okamber{fill:#fffbeb;stroke:#fcd34d}.okgreen{fill:#ecfdf5;stroke:#86efac}.okt{font:700 17px 'Times New Roman','SimSun',serif;fill:#111827}.oks{font:14px 'Times New Roman','SimSun',serif;fill:#475569}.oka{stroke:#475569;stroke-width:2.2;fill:none;marker-end:url(#arrow-ok)}</style>
  </defs>
  <rect x="60" y="90" width="280" height="160" class="okbox okblue"/><text x="200" y="130" text-anchor="middle" class="okt">Operator / 算子</text><text x="200" y="166" text-anchor="middle" class="oks">数学语义</text><text x="200" y="196" text-anchor="middle" class="oks">Cᵢⱼ = Σₖ AᵢₖBₖⱼ</text>
  <rect x="420" y="90" width="280" height="160" class="okbox okamber"/><text x="560" y="130" text-anchor="middle" class="okt">Operator Kernel / 算子内核</text><text x="560" y="166" text-anchor="middle" class="oks">tiling, scheduling</text><text x="560" y="196" text-anchor="middle" class="oks">load → compute → store</text>
  <rect x="780" y="90" width="280" height="160" class="okbox okgreen"/><text x="920" y="130" text-anchor="middle" class="okt">Hardware / TPU 硬件</text><text x="920" y="166" text-anchor="middle" class="oks">SRAM, DMA</text><text x="920" y="196" text-anchor="middle" class="oks">matrix units, vector units</text>
  <path d="M340 170 L420 170" class="oka"/><path d="M700 170 L780 170" class="oka"/>
  <text x="380" y="150" text-anchor="middle" class="oks">lowering</text><text x="740" y="150" text-anchor="middle" class="oks">execute</text>
</svg>
</div>

<h2 id="DLC-编译器：从图语义到硬件执行计划"><a href="#DLC-编译器：从图语义到硬件执行计划" class="headerlink" title="DLC / 编译器：从图语义到硬件执行计划"></a>DLC / 编译器：从图语义到硬件执行计划</h2><p>DLC 在不同厂商语境下可能表示 <strong>Deep Learning Compiler</strong>，也可能表示 <strong>Compiled Model Artifact</strong>。这两种含义都围绕同一件事：把高层模型图转换为目标硬件上可执行、正确且高效的执行计划。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>生态</th>
<th>常见编译产物</th>
</tr>
</thead>
<tbody>
<tr>
<td>NVIDIA TensorRT</td>
<td>engine / plan</td>
</tr>
<tr>
<td>Qualcomm SNPE</td>
<td>dlc</td>
</tr>
<tr>
<td>Ascend</td>
<td>om</td>
</tr>
<tr>
<td>Sophon</td>
<td>bmodel</td>
</tr>
<tr>
<td>其他 TPU/NPU 厂商</td>
<td>model binary / compiled artifact</td>
</tr>
</tbody>
</table>
</div>
<p>典型编译链路如下：</p>
<div class="svg-figure">
<svg viewBox="0 0 1260 460" width="100%" role="img" aria-label="deep learning compiler pipeline">
  <defs>
    <marker id="arrow-compile" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#2563eb"/></marker>
    <style>.cbox{fill:#fff;stroke:#cbd5e1;stroke-width:2;rx:14}.c1{fill:#eff6ff;stroke:#93c5fd}.c2{fill:#ecfdf5;stroke:#86efac}.c3{fill:#fffbeb;stroke:#fcd34d}.ct{font:700 15px 'Times New Roman','SimSun',serif;fill:#111827}.cs{font:13px 'Times New Roman','SimSun',serif;fill:#475569}.ca{stroke:#2563eb;stroke-width:2.2;fill:none;marker-end:url(#arrow-compile)}</style>
  </defs>
  <rect x="40" y="70" width="170" height="86" class="cbox c1"/><text x="125" y="104" text-anchor="middle" class="ct">Model Export</text><text x="125" y="130" text-anchor="middle" class="cs">PyTorch / ONNX</text>
  <rect x="260" y="70" width="170" height="86" class="cbox c1"/><text x="345" y="104" text-anchor="middle" class="ct">Graph IR</text><text x="345" y="130" text-anchor="middle" class="cs">operators + tensors</text>
  <rect x="480" y="70" width="180" height="86" class="cbox c2"/><text x="570" y="104" text-anchor="middle" class="ct">Graph Optimize</text><text x="570" y="130" text-anchor="middle" class="cs">fusion, folding</text>
  <rect x="710" y="70" width="180" height="86" class="cbox c2"/><text x="800" y="104" text-anchor="middle" class="ct">Lowering</text><text x="800" y="130" text-anchor="middle" class="cs">Graph IR → Tensor IR</text>
  <rect x="940" y="70" width="180" height="86" class="cbox c3"/><text x="1030" y="104" text-anchor="middle" class="ct">Scheduling</text><text x="1030" y="130" text-anchor="middle" class="cs">tiling, layout</text>
  <path d="M210 113 L260 113" class="ca"/><path d="M430 113 L480 113" class="ca"/><path d="M660 113 L710 113" class="ca"/><path d="M890 113 L940 113" class="ca"/>
  <rect x="260" y="260" width="180" height="86" class="cbox c3"/><text x="350" y="294" text-anchor="middle" class="ct">Memory Planning</text><text x="350" y="320" text-anchor="middle" class="cs">buffers, reuse</text>
  <rect x="530" y="260" width="180" height="86" class="cbox c3"/><text x="620" y="294" text-anchor="middle" class="ct">Code Generation</text><text x="620" y="320" text-anchor="middle" class="cs">kernel binary</text>
  <rect x="800" y="260" width="180" height="86" class="cbox c1"/><text x="890" y="294" text-anchor="middle" class="ct">Compiled Artifact</text><text x="890" y="320" text-anchor="middle" class="cs">DLC / model binary</text>
  <rect x="1070" y="260" width="150" height="86" class="cbox c2"/><text x="1145" y="294" text-anchor="middle" class="ct">Runtime</text><text x="1145" y="320" text-anchor="middle" class="cs">load + execute</text>
  <path d="M1030 156 C1030 210 350 205 350 260" class="ca"/><path d="M440 303 L530 303" class="ca"/><path d="M710 303 L800 303" class="ca"/><path d="M980 303 L1070 303" class="ca"/>
</svg>
</div>

<p>其中，Graph IR 保留较高层的算子语义；Tensor IR 更接近循环、索引和 buffer；Kernel IR 更接近目标硬件执行方式。编译器的核心任务不是“把 Python 翻译成二进制”这么简单，而是同时决定算子融合、layout、tiling、memory planning、kernel selection 和 code generation。</p>
<h2 id="vLLM：在线推理服务引擎，不是离线编译器"><a href="#vLLM：在线推理服务引擎，不是离线编译器" class="headerlink" title="vLLM：在线推理服务引擎，不是离线编译器"></a>vLLM：在线推理服务引擎，不是离线编译器</h2><p>vLLM 的定位是 <strong>LLM inference serving engine</strong>。它关注在线推理系统问题：如何接收多用户请求，如何调度 prefill 和 decode，如何做 continuous batching，如何管理 KV cache，如何通过后端 Runtime 调用硬件 kernel。</p>
<div class="svg-figure">
<svg viewBox="0 0 1120 620" width="100%" role="img" aria-label="vLLM serving stack">
  <defs>
    <marker id="arrow-vllm" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#15803d"/></marker>
    <style>.vbox{fill:#fff;stroke:#cbd5e1;stroke-width:2;rx:14}.v1{fill:#eff6ff;stroke:#93c5fd}.v2{fill:#ecfdf5;stroke:#86efac}.v3{fill:#fffbeb;stroke:#fcd34d}.v4{fill:#f5f3ff;stroke:#c4b5fd}.vt{font:700 16px 'Times New Roman','SimSun',serif;fill:#111827}.vs{font:13px 'Times New Roman','SimSun',serif;fill:#475569}.va{stroke:#15803d;stroke-width:2.2;fill:none;marker-end:url(#arrow-vllm)}</style>
  </defs>
  <rect x="60" y="60" width="250" height="78" class="vbox v1"/><text x="185" y="92" text-anchor="middle" class="vt">Client Requests</text><text x="185" y="116" text-anchor="middle" class="vs">OpenAI API / Python API</text>
  <rect x="435" y="60" width="250" height="78" class="vbox v1"/><text x="560" y="92" text-anchor="middle" class="vt">Tokenizer</text><text x="560" y="116" text-anchor="middle" class="vs">text ↔ token ids</text>
  <rect x="810" y="60" width="250" height="78" class="vbox v2"/><text x="935" y="92" text-anchor="middle" class="vt">Request Scheduler</text><text x="935" y="116" text-anchor="middle" class="vs">admission and priority</text>
  <path d="M310 99 L435 99" class="va"/><path d="M685 99 L810 99" class="va"/>
  <rect x="160" y="240" width="280" height="92" class="vbox v2"/><text x="300" y="276" text-anchor="middle" class="vt">Continuous Batching</text><text x="300" y="304" text-anchor="middle" class="vs">merge prefill and decode workloads</text>
  <rect x="680" y="240" width="280" height="92" class="vbox v3"/><text x="820" y="276" text-anchor="middle" class="vt">PagedAttention</text><text x="820" y="304" text-anchor="middle" class="vs">block table maps tokens to KV blocks</text>
  <path d="M935 138 C935 195 820 190 820 240" class="va"/><path d="M900 138 C900 200 300 190 300 240" class="va"/><path d="M440 286 L680 286" class="va"/>
  <rect x="110" y="460" width="260" height="86" class="vbox v4"/><text x="240" y="494" text-anchor="middle" class="vt">Model Executor</text><text x="240" y="520" text-anchor="middle" class="vs">prefill, decode, sampling</text>
  <rect x="430" y="460" width="260" height="86" class="vbox"/><text x="560" y="494" text-anchor="middle" class="vt">Backend Runtime</text><text x="560" y="520" text-anchor="middle" class="vs">PyTorch / XLA / vendor SDK</text>
  <rect x="750" y="460" width="260" height="86" class="vbox v2"/><text x="880" y="494" text-anchor="middle" class="vt">GPU / TPU / NPU</text><text x="880" y="520" text-anchor="middle" class="vs">operator kernels execute</text>
  <path d="M300 332 L240 460" class="va"/><path d="M820 332 C820 410 260 400 240 460" class="va"/><path d="M370 503 L430 503" class="va"/><path d="M690 503 L750 503" class="va"/>
</svg>
</div>

<p>vLLM 与 DLC/Compiler 的边界可以这样理解：vLLM 管在线请求与 KV cache，编译器管图优化与 kernel 生成，Linux driver 管设备访问，TPU hardware 管实际计算。若要让 vLLM 使用 TPU/NPU，需要 XLA/torch_xla、厂商 runtime adapter 或专门的 backend 支持。</p>
<h2 id="Prefill、Decode、KV-Cache-与-PagedAttention"><a href="#Prefill、Decode、KV-Cache-与-PagedAttention" class="headerlink" title="Prefill、Decode、KV Cache 与 PagedAttention"></a>Prefill、Decode、KV Cache 与 PagedAttention</h2><p>LLM 推理通常分成两个阶段：Prefill 和 Decode。Prefill 处理完整 prompt，计算量大但序列维度并行度高；Decode 每次生成一个 token，单步计算较小，但需要反复读取历史 KV cache。</p>
<p>KV cache 的近似内存占用可以写成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">KV_bytes = 2 × L × B × S × H_kv × D_head × bytes(dtype)</span><br></pre></td></tr></table></figure>
<p>其中 <code>2</code> 表示 K 和 V，<code>L</code> 是层数，<code>B</code> 是 batch size，<code>S</code> 是序列长度，<code>H_kv</code> 是 KV head 数，<code>D_head</code> 是每个 head 的维度。</p>
<div class="svg-figure">
<svg viewBox="0 0 1120 520" width="100%" role="img" aria-label="prefill decode pagedattention kv cache">
  <defs>
    <marker id="arrow-kv" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#15803d"/></marker>
    <style>.kbox{fill:#fff;stroke:#cbd5e1;stroke-width:2;rx:14}.k1{fill:#eff6ff;stroke:#93c5fd}.k2{fill:#ecfdf5;stroke:#86efac}.k3{fill:#fffbeb;stroke:#fcd34d}.kt{font:700 16px 'Times New Roman','SimSun',serif;fill:#111827}.ks{font:13px 'Times New Roman','SimSun',serif;fill:#475569}.ka{stroke:#15803d;stroke-width:2.2;fill:none;marker-end:url(#arrow-kv)}</style>
  </defs>
  <rect x="70" y="60" width="220" height="80" class="kbox k1"/><text x="180" y="92" text-anchor="middle" class="kt">Prompt Tokens</text><text x="180" y="116" text-anchor="middle" class="ks">input sequence</text>
  <rect x="390" y="60" width="220" height="80" class="kbox k2"/><text x="500" y="92" text-anchor="middle" class="kt">Prefill</text><text x="500" y="116" text-anchor="middle" class="ks">build KV cache</text>
  <rect x="710" y="60" width="220" height="80" class="kbox k3"/><text x="820" y="92" text-anchor="middle" class="kt">Decode</text><text x="820" y="116" text-anchor="middle" class="ks">read cache, append KV</text>
  <path d="M290 100 L390 100" class="ka"/><path d="M610 100 L710 100" class="ka"/>
  <rect x="70" y="230" width="980" height="235" class="kbox"/><text x="560" y="264" text-anchor="middle" class="kt">PagedAttention: 用 block table 管理 KV cache</text>
  <text x="235" y="302" text-anchor="middle" class="ks">逻辑 token 顺序：模型看到 token 是连续的</text>
  <rect x="115" y="325" width="90" height="42" class="kbox k1"/><text x="160" y="351" text-anchor="middle" class="ks">tok 0-15</text>
  <rect x="215" y="325" width="90" height="42" class="kbox k1"/><text x="260" y="351" text-anchor="middle" class="ks">tok 16-31</text>
  <rect x="315" y="325" width="90" height="42" class="kbox k1"/><text x="360" y="351" text-anchor="middle" class="ks">tok 32-47</text>
  <text x="535" y="302" text-anchor="middle" class="ks">block table：记录逻辑块到物理块的映射</text>
  <rect x="475" y="325" width="120" height="42" class="kbox k2"/><text x="535" y="351" text-anchor="middle" class="ks">0→17  1→04</text>
  <rect x="475" y="378" width="120" height="42" class="kbox k2"/><text x="535" y="404" text-anchor="middle" class="ks">2→29</text>
  <text x="835" y="302" text-anchor="middle" class="ks">物理 KV blocks：真实内存里可分散、可复用</text>
  <rect x="720" y="325" width="90" height="42" class="kbox k3"/><text x="765" y="351" text-anchor="middle" class="ks">KV #17</text>
  <rect x="835" y="325" width="90" height="42" class="kbox k3"/><text x="880" y="351" text-anchor="middle" class="ks">KV #04</text>
  <rect x="950" y="325" width="90" height="42" class="kbox k3"/><text x="995" y="351" text-anchor="middle" class="ks">KV #29</text>
  <path d="M405 346 L475 346" class="ka"/><path d="M595 346 L720 346" class="ka"/><path d="M595 346 L835 346" class="ka"/><path d="M595 399 L950 346" class="ka"/>
  <text x="560" y="446" text-anchor="middle" class="ks">好处：不要求 KV cache 连续分配，请求结束后释放物理块，其他请求可以复用。</text>
</svg>
</div>

<p>PagedAttention 的核心思想类似虚拟内存分页：请求里的 token 逻辑上是连续的，但它们对应的 KV cache 不必放在一整块连续内存里。vLLM 会把 token 序列切成固定大小的逻辑块，再通过 block table 映射到真实设备内存中的 physical KV blocks。这样可以降低 KV cache 碎片；一个请求结束后，它占用的物理 KV blocks 可以被释放并复用给其他请求，从而提高长上下文和多请求并发时的设备内存利用率。</p>
<h2 id="TPU-与-Linux-内核的关系"><a href="#TPU-与-Linux-内核的关系" class="headerlink" title="TPU 与 Linux 内核的关系"></a>TPU 与 Linux 内核的关系</h2><p>TPU 是加速器硬件，Linux Kernel 是操作系统内核。用户态 Runtime 通常通过 <code>ioctl</code>、<code>mmap</code>、<code>poll</code> 等接口与 TPU device driver 通信；驱动再负责 DMA 映射、command queue、interrupt handling 和错误状态返回。</p>
<div class="svg-figure">
<svg viewBox="0 0 1180 620" width="100%" role="img" aria-label="linux kernel tpu driver architecture">
  <defs>
    <marker id="arrow-linux" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#b91c1c"/></marker>
    <style>.lbox{fill:#fff;stroke:#cbd5e1;stroke-width:2;rx:14}.l1{fill:#eff6ff;stroke:#93c5fd}.l2{fill:#fffbeb;stroke:#fcd34d}.l3{fill:#ecfdf5;stroke:#86efac}.lt{font:700 16px 'Times New Roman','SimSun',serif;fill:#111827}.ls{font:13px 'Times New Roman','SimSun',serif;fill:#475569}.la{stroke:#b91c1c;stroke-width:2.2;fill:none;marker-end:url(#arrow-linux)}</style>
  </defs>
  <rect x="40" y="40" width="1100" height="130" class="lbox l1"/><text x="80" y="75" class="lt">User Space / 用户态</text>
  <rect x="170" y="90" width="220" height="58" class="lbox"/><text x="280" y="125" text-anchor="middle" class="lt">Application</text>
  <rect x="520" y="90" width="220" height="58" class="lbox"/><text x="630" y="125" text-anchor="middle" class="lt">TPU Runtime / SDK</text>
  <rect x="860" y="90" width="220" height="58" class="lbox"/><text x="970" y="125" text-anchor="middle" class="lt">DLC / Model Binary</text>
  <path d="M390 119 L520 119" class="la"/><path d="M860 119 L740 119" class="la"/>
  <rect x="40" y="245" width="1100" height="150" class="lbox l2"/><text x="80" y="280" class="lt">Kernel Space / Linux 内核态</text>
  <rect x="160" y="310" width="220" height="58" class="lbox"/><text x="270" y="345" text-anchor="middle" class="lt">ioctl / mmap / poll</text>
  <rect x="480" y="310" width="240" height="58" class="lbox"/><text x="600" y="345" text-anchor="middle" class="lt">TPU Device Driver</text>
  <rect x="820" y="310" width="220" height="58" class="lbox"/><text x="930" y="345" text-anchor="middle" class="lt">DMA / IOMMU</text>
  <path d="M630 148 L630 310" class="la"/><path d="M380 339 L480 339" class="la"/><path d="M720 339 L820 339" class="la"/>
  <rect x="40" y="470" width="1100" height="110" class="lbox l3"/><text x="80" y="505" class="lt">Device / TPU 硬件</text>
  <rect x="220" y="525" width="190" height="40" class="lbox"/><text x="315" y="551" text-anchor="middle" class="ls">Command Queue</text>
  <rect x="500" y="525" width="190" height="40" class="lbox"/><text x="595" y="551" text-anchor="middle" class="ls">TPU Cores</text>
  <rect x="780" y="525" width="190" height="40" class="lbox"/><text x="875" y="551" text-anchor="middle" class="ls">Interrupt</text>
  <path d="M930 368 L930 525" class="la"/><path d="M410 545 L500 545" class="la"/><path d="M690 545 L780 545" class="la"/><path d="M875 525 C1060 450 1030 350 1040 339" class="la"/>
</svg>
</div>

<p>因此，一个 MatMul 从 Python 到 TPU 的路径可以用专业语言概括为：框架识别 <code>aten::matmul</code> 或等价图节点，导出到 ONNX 或厂商 IR，编译器执行 lowering 和 kernel selection，Runtime 加载 compiled artifact 并提交 command buffer，Linux TPU driver 完成设备通信，TPU hardware 执行目标 MatMul kernel。</p>
<h2 id="例子一：Linear-Bias-ReLU-融合"><a href="#例子一：Linear-Bias-ReLU-融合" class="headerlink" title="例子一：Linear + Bias + ReLU 融合"></a>例子一：Linear + Bias + ReLU 融合</h2><p>框架层表达式如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">y = torch.relu(x @ w + b)</span><br></pre></td></tr></table></figure>
<p>数学形式是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Y = max(0, XW + b)</span><br></pre></td></tr></table></figure>
<p>如果不融合，执行路径包含 MatMul、Add、ReLU 三个算子，可能产生两个中间张量；如果融合，编译器可以生成一个 <code>Fused MatMul + Bias + ReLU Kernel</code>，在累加完成后直接加 bias、应用激活函数并写回最终输出。</p>
<div class="svg-figure">
<svg viewBox="0 0 1120 460" width="100%" role="img" aria-label="linear bias relu fusion">
  <defs>
    <marker id="arrow-fuse" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#2563eb"/></marker>
    <style>.fbox{fill:#fff;stroke:#cbd5e1;stroke-width:2;rx:14}.f1{fill:#eff6ff;stroke:#93c5fd}.f2{fill:#ecfdf5;stroke:#86efac}.ft{font:700 16px 'Times New Roman','SimSun',serif;fill:#111827}.fs{font:13px 'Times New Roman','SimSun',serif;fill:#475569}.fa{stroke:#2563eb;stroke-width:2.2;fill:none;marker-end:url(#arrow-fuse)}</style>
  </defs>
  <text x="60" y="50" class="ft">Before Fusion / 融合前</text>
  <rect x="70" y="90" width="150" height="68" class="fbox f1"/><text x="145" y="130" text-anchor="middle" class="ft">MatMul</text>
  <rect x="310" y="90" width="150" height="68" class="fbox f1"/><text x="385" y="130" text-anchor="middle" class="ft">Add</text>
  <rect x="550" y="90" width="150" height="68" class="fbox f1"/><text x="625" y="130" text-anchor="middle" class="ft">ReLU</text>
  <rect x="790" y="90" width="150" height="68" class="fbox"/><text x="865" y="130" text-anchor="middle" class="ft">Output</text>
  <path d="M220 124 L310 124" class="fa"/><path d="M460 124 L550 124" class="fa"/><path d="M700 124 L790 124" class="fa"/>
  <text x="265" y="178" text-anchor="middle" class="fs">tmp1</text><text x="505" y="178" text-anchor="middle" class="fs">tmp2</text>
  <text x="60" y="280" class="ft">After Fusion / 融合后</text>
  <rect x="120" y="320" width="460" height="78" class="fbox f2"/><text x="350" y="352" text-anchor="middle" class="ft">Fused MatMul + Bias + ReLU Kernel</text><text x="350" y="378" text-anchor="middle" class="fs">accumulate → add bias → activation → store once</text>
  <rect x="760" y="325" width="160" height="68" class="fbox"/><text x="840" y="365" text-anchor="middle" class="ft">Output</text>
  <path d="M580 359 L760 359" class="fa"/>
</svg>
</div>

<p>融合的直接收益是减少中间 tensor、减少 global memory 读写、减少 kernel launch overhead，并提升端到端吞吐。</p>
<h2 id="例子二：Scaled-Dot-Product-Attention"><a href="#例子二：Scaled-Dot-Product-Attention" class="headerlink" title="例子二：Scaled Dot-Product Attention"></a>例子二：Scaled Dot-Product Attention</h2><p>Scaled Dot-Product Attention 的经典公式来自 Transformer 论文 <em>Attention Is All You Need</em> 的 3.2.1 节：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Attention(Q, K, V) = softmax((QK^T) / sqrt(d_k)) V</span><br></pre></td></tr></table></figure>
<p>其中 <code>d_k</code> 是 key/query 向量维度，除以 <code>sqrt(d_k)</code> 是为了避免点积值随维度增大而过大，导致 softmax 进入梯度很小的区域。在 LLM 的 causal self-attention 中，softmax 前通常还会加入 causal mask，阻止当前位置看到未来 token。</p>
<p>这里最容易抽象的是 <code>scores</code> 为什么会变成 <code>[B, H, S, S]</code>。先看 Q、K、V 的常见 shape：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Q: [B, H, S, D]</span><br><span class="line">K: [B, H, S, D]</span><br><span class="line">V: [B, H, S, D]</span><br></pre></td></tr></table></figure>
<p>其中 <code>B</code> 是 batch size，<code>H</code> 是 attention head 数，<code>S</code> 是序列长度，<code>D</code> 是每个 head 的维度。对每个 batch、每个 head 来说，注意力会让“每个 query token”去和“每个 key token”算相似度：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Q[b,h]      : [S_query, D]</span><br><span class="line">K[b,h]^T    : [D, S_key]</span><br><span class="line">Q[b,h]K[b,h]^T -&gt; [S_query, S_key]</span><br></pre></td></tr></table></figure>
<p>自注意力里 <code>S_query = S_key = S</code>，所以每个 head 会得到一个 <code>[S, S]</code> 的 scores 矩阵；再乘上 batch 和 head 两个维度，整体就是 <code>[B, H, S, S]</code>。这两个 <code>S</code> 不是重复写错了：前一个 <code>S</code> 表示 query token 位置，后一个 <code>S</code> 表示 key token 位置。</p>
<p>普通实现可能 materialize <code>scores</code> 和 <code>probs</code> 两个 <code>[B, H, S, S]</code> 规模的中间矩阵。当序列长度 <code>S</code> 很大时，内存读写会成为主要瓶颈。</p>
<div class="svg-figure">
<svg viewBox="0 0 1180 520" width="100%" role="img" aria-label="scaled dot product attention">
  <defs>
    <marker id="arrow-attn" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#7c3aed"/></marker>
    <style>.abox{fill:#fff;stroke:#cbd5e1;stroke-width:2;rx:14}.a1{fill:#eff6ff;stroke:#93c5fd}.a2{fill:#fffbeb;stroke:#fcd34d}.a3{fill:#ecfdf5;stroke:#86efac}.at{font:700 16px 'Times New Roman','SimSun',serif;fill:#111827}.as{font:13px 'Times New Roman','SimSun',serif;fill:#475569}.aa{stroke:#7c3aed;stroke-width:2.2;fill:none;marker-end:url(#arrow-attn)}</style>
  </defs>
  <rect x="60" y="70" width="150" height="60" class="abox a1"/><text x="135" y="100" text-anchor="middle" class="at">Q</text><text x="135" y="121" text-anchor="middle" class="as">[B,H,S,D]</text>
  <rect x="60" y="210" width="150" height="60" class="abox a1"/><text x="135" y="240" text-anchor="middle" class="at">K</text><text x="135" y="261" text-anchor="middle" class="as">[B,H,S,D]</text>
  <rect x="60" y="350" width="150" height="60" class="abox a1"/><text x="135" y="380" text-anchor="middle" class="at">V</text><text x="135" y="401" text-anchor="middle" class="as">[B,H,S,D]</text>
  <rect x="300" y="145" width="190" height="76" class="abox a2"/><text x="395" y="176" text-anchor="middle" class="at">QKᵀ scores</text><text x="395" y="202" text-anchor="middle" class="as">[B,H,S_query,S_key]</text>
  <rect x="560" y="145" width="170" height="76" class="abox a2"/><text x="645" y="178" text-anchor="middle" class="at">Scale</text><text x="645" y="202" text-anchor="middle" class="as">1 / sqrt(dₖ)</text>
  <rect x="820" y="145" width="170" height="76" class="abox a3"/><text x="905" y="178" text-anchor="middle" class="at">Softmax</text><text x="905" y="202" text-anchor="middle" class="as">probabilities</text>
  <rect x="560" y="340" width="170" height="76" class="abox a2"/><text x="645" y="373" text-anchor="middle" class="at">MatMul V</text><text x="645" y="397" text-anchor="middle" class="as">weighted sum</text>
  <rect x="820" y="340" width="170" height="76" class="abox"/><text x="905" y="373" text-anchor="middle" class="at">Output O</text><text x="905" y="397" text-anchor="middle" class="as">[B,H,S,D]</text>
  <path d="M190 100 C245 100 250 183 300 183" class="aa"/><path d="M190 240 C245 240 250 183 300 183" class="aa"/><path d="M470 183 L560 183" class="aa"/><path d="M730 183 L820 183" class="aa"/><path d="M905 221 C905 300 730 300 730 378" class="aa"/><path d="M190 380 L560 380" class="aa"/><path d="M730 378 L820 378" class="aa"/>
  <rect x="1010" y="165" width="130" height="190" class="abox"/><text x="1075" y="200" text-anchor="middle" class="at">Optimization</text><text x="1075" y="232" text-anchor="middle" class="as">blocking</text><text x="1075" y="260" text-anchor="middle" class="as">online softmax</text><text x="1075" y="288" text-anchor="middle" class="as">avoid full</text><text x="1075" y="316" text-anchor="middle" class="as">[B,H,S,S]</text>
</svg>
</div>

<p>FlashAttention 类优化的核心不是改变数学公式，而是改变执行方式：按 block 计算 $QK^T$，在线维护 softmax 的 max 和 sum，不完整写出 scores/probs，从而减少 HBM/global memory traffic。</p>
<h2 id="优化到底在优化什么"><a href="#优化到底在优化什么" class="headerlink" title="优化到底在优化什么"></a>优化到底在优化什么</h2><div class="table-container">
<table>
<thead>
<tr>
<th>优化项</th>
<th>专业含义</th>
<th>主要目的</th>
<th>例子</th>
</tr>
</thead>
<tbody>
<tr>
<td>Operator Fusion</td>
<td>多个相邻算子合成 fused kernel</td>
<td>减少中间张量和 kernel launch</td>
<td>MatMul + Bias + ReLU</td>
</tr>
<tr>
<td>Layout Optimization</td>
<td>选择硬件友好的内存布局</td>
<td>提高连续访存和对齐效率</td>
<td>NCHW、NHWC、blocked layout</td>
</tr>
<tr>
<td>Memory Planning</td>
<td>规划 buffer 生命周期和复用</td>
<td>降低峰值设备内存</td>
<td>activation buffer reuse</td>
</tr>
<tr>
<td>Kernel Selection</td>
<td>根据 shape/dtype/layout 选实现</td>
<td>提高单算子性能</td>
<td>small GEMM vs large GEMM</td>
</tr>
<tr>
<td>Tiling / Blocking</td>
<td>把大计算切成 tile</td>
<td>提高片上缓存复用</td>
<td>MatMul 的 M/N/K tile</td>
</tr>
<tr>
<td>Quantization</td>
<td>降低数值精度</td>
<td>降低带宽和存储，提高吞吐</td>
<td>FP16、BF16、INT8、INT4</td>
</tr>
<tr>
<td>DMA/Compute Overlap</td>
<td>搬运和计算流水化</td>
<td>隐藏 DMA latency</td>
<td>double buffering</td>
</tr>
<tr>
<td>Continuous Batching</td>
<td>动态合并在线请求</td>
<td>提高 LLM serving 吞吐</td>
<td>vLLM scheduler</td>
</tr>
<tr>
<td>KV Cache Paging</td>
<td>按 block 管理 KV cache</td>
<td>降低碎片，提高并发</td>
<td>vLLM PagedAttention</td>
</tr>
</tbody>
</table>
</div>
<p>Arithmetic intensity 可以帮助判断算子更可能受计算限制还是带宽限制：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">AI = FLOPs / Bytes moved</span><br></pre></td></tr></table></figure>
<p>如果 $AI$ 很低，算子通常更容易受内存带宽限制；如果 $AI$ 很高，才更可能受计算单元吞吐限制。</p>
<h2 id="最后总结"><a href="#最后总结" class="headerlink" title="最后总结"></a>最后总结</h2><p>整条链路可以概括为：Large Model 定义任务语义，Framework 或 vLLM 负责模型表达与在线服务，Computational Graph 将模型分解为 Operators，Compiler 或 Backend Runtime 选择和生成 Operator Kernels，Runtime 通过 Linux Driver 提交硬件任务，TPU Hardware 最终执行这些算子内核。</p>
<p>更短地说：模型由算子组成，算子由硬件内核执行，Runtime 通过 Linux 驱动把硬件任务提交给 TPU。Linux kernel 让 TPU 能被系统安全访问；TPU operator kernel 让算子能在 TPU 上高效计算；vLLM 让大模型在线推理能高吞吐地调度请求和管理 KV cache。</p>
]]></content>
      <categories>
        <category>AI Infra</category>
        <category>TPU</category>
      </categories>
      <tags>
        <tag>TPU</tag>
        <tag>LLM</tag>
        <tag>vLLM</tag>
        <tag>Compiler</tag>
        <tag>Linux</tag>
        <tag>Kernel</tag>
      </tags>
  </entry>
  <entry>
    <title>vLLM 架构源码解析（一）：从 API 请求到 Token 生成</title>
    <url>/2026/07/06/20260706-vllm-architecture-api-to-token-generation/</url>
    <content><![CDATA[<blockquote>
<p><strong>系列文章</strong>：vLLM 架构源码解析 | 第 1 篇 / 共 4 篇<br><strong>日期</strong>：2026-07-06<br><strong>源码版本</strong>：vLLM v0.6.x (commit 98ba9b9)</p>
</blockquote>
<hr>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ol>
<li><a href="#1-引言为什么-vllm-能提速-10-24-倍">引言：为什么 vLLM 能提速 10-24 倍？</a></li>
<li><a href="#2-核心架构六层分工">核心架构：六层分工</a></li>
<li><a href="#3-一个请求的完整生命周期">一个请求的完整生命周期</a></li>
<li><a href="#4-pagedattention核心创新深入解析">PagedAttention：核心创新深入解析</a></li>
<li><a href="#5-总结与下期预告">总结与下期预告</a></li>
</ol>
<hr>
<h2 id="1-引言：为什么-vLLM-能提速-10-24-倍？"><a href="#1-引言：为什么-vLLM-能提速-10-24-倍？" class="headerlink" title="1. 引言：为什么 vLLM 能提速 10-24 倍？"></a>1. 引言：为什么 vLLM 能提速 10-24 倍？</h2><p>vLLM 是 UC Berkeley 于 2023 年推出的高性能 LLM 推理引擎。相比 HuggingFace Transformers，在相同硬件上可以达到：</p>
<ul>
<li><strong>吞吐量提升 10-24 倍</strong>（官方 benchmark，A100-80GB）</li>
<li><strong>延迟降低 50-70%</strong>（批量推理场景）</li>
<li><strong>显存利用率提升 2-4 倍</strong>（从 20% 提升到 80%+）</li>
</ul>
<span id="more"></span>
<h3 id="1-1-核心优化技术"><a href="#1-1-核心优化技术" class="headerlink" title="1.1 核心优化技术"></a>1.1 核心优化技术</h3><h4 id="PagedAttention：虚拟内存管理-KV-Cache"><a href="#PagedAttention：虚拟内存管理-KV-Cache" class="headerlink" title="PagedAttention：虚拟内存管理 KV Cache"></a>PagedAttention：虚拟内存管理 KV Cache</h4><p><strong>问题</strong>：传统推理中，KV Cache 必须预分配连续显存，导致：</p>
<ul>
<li>碎片化严重（实际利用率 &lt; 20%）</li>
<li>无法动态扩展</li>
<li>多请求间无法共享</li>
</ul>
<p><strong>解决</strong>：借鉴操作系统的虚拟内存分页（paging）机制：</p>
<ol>
<li><strong>Block 切分</strong>：将 KV Cache 切成固定大小的 block（默认 16 tokens）</li>
<li><strong>虚拟地址映射</strong>：通过 block table 映射逻辑地址 → 物理地址</li>
<li><strong>按需分配</strong>：生成时动态分配 block，用完即释放</li>
</ol>
<figure class="highlight inform7"><table><tr><td class="code"><pre><span class="line">传统方式（连续分配）:</span><br><span class="line">Request 1: <span class="comment">[█████████████████████░░░░░░░░░]</span> ← 大量空洞</span><br><span class="line">Request 2: <span class="comment">[████░░░░░░░░░░░░░░░░░░░░░░░░░]</span></span><br><span class="line"></span><br><span class="line">PagedAttention（block 分配）:</span><br><span class="line">Physical Block Pool: <span class="comment">[0]</span><span class="comment">[1]</span><span class="comment">[2]</span><span class="comment">[3]</span><span class="comment">[4]</span><span class="comment">[5]</span><span class="comment">[6]</span><span class="comment">[7]</span></span><br><span class="line">Request 1: blocks <span class="comment">[0,1,2,4]</span>  ← 物理不连续</span><br><span class="line">Request 2: blocks <span class="comment">[0,1,5]</span>    ← 与 R1 共享 <span class="comment">[0,1]</span></span><br></pre></td></tr></table></figure>
<p><strong>效果</strong>：显存利用率从 20% 提升到 <strong>80%+</strong></p>
<hr>
<h4 id="Prefix-Caching：跨请求共享"><a href="#Prefix-Caching：跨请求共享" class="headerlink" title="Prefix Caching：跨请求共享"></a>Prefix Caching：跨请求共享</h4><p>基于 PagedAttention 的 block 管理，可以实现：</p>
<ul>
<li><strong>System Prompt 共享</strong>：多个用户共享相同的系统提示词</li>
<li><strong>Few-shot Examples 共享</strong>：批量任务复用示例</li>
<li><strong>文档共享</strong>：多个查询共享同一文档的 KV Cache</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 场景示例</span></span><br><span class="line">system_prompt = <span class="string">&quot;You are a helpful assistant.&quot;</span>  <span class="comment"># 100 tokens</span></span><br><span class="line">queries = [</span><br><span class="line">    <span class="string">&quot;What is Python?&quot;</span>,</span><br><span class="line">    <span class="string">&quot;Explain AI&quot;</span>,</span><br><span class="line">    <span class="string">&quot;Tell me about ML&quot;</span></span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 传统方式：每个请求独立计算 system_prompt（100 tokens × 3 = 300 tokens）</span></span><br><span class="line"><span class="comment"># vLLM：system_prompt 只计算一次，3 个请求共享（100 + 10 + 10 + 10 = 130 tokens）</span></span><br><span class="line"><span class="comment"># 节省：57% 的计算量</span></span><br></pre></td></tr></table></figure>
<p><strong>效果</strong>：命中缓存时，TTFT（首 token 延迟）降低 <strong>80%+</strong></p>
<hr>
<h4 id="Continuous-Batching-Chunked-Prefill"><a href="#Continuous-Batching-Chunked-Prefill" class="headerlink" title="Continuous Batching + Chunked Prefill"></a>Continuous Batching + Chunked Prefill</h4><p><strong>传统批处理问题</strong>：</p>
<ul>
<li>Static Batching：必须等所有请求都结束才能加新的</li>
<li>长 prompt 独占整个 batch，导致短请求饥饿</li>
</ul>
<p><strong>vLLM 方案</strong>：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">传统 Static Batching（等待 max 请求完成）:</span><br><span class="line">Step <span class="number">1</span>: [<span class="built_in">Req1_prefill</span>(<span class="number">2000</span> tok)] ← 其他请求等待</span><br><span class="line">Step <span class="number">2</span>: [Req1_decode, <span class="built_in">Req2_prefill</span>(<span class="number">10</span> tok), <span class="built_in">Req3_prefill</span>(<span class="number">10</span> tok)]</span><br><span class="line">Step <span class="number">3</span>: [Req1_decode, Req2_decode, Req3_decode]</span><br><span class="line"></span><br><span class="line">vLLM Continuous Batching + Chunked Prefill:</span><br><span class="line">Step <span class="number">1</span>: [<span class="built_in">Req1_prefill_chunk1</span>(<span class="number">512</span>), <span class="built_in">Req2_prefill</span>(<span class="number">10</span>), <span class="built_in">Req3_prefill</span>(<span class="number">10</span>)]</span><br><span class="line">Step <span class="number">2</span>: [<span class="built_in">Req1_prefill_chunk2</span>(<span class="number">512</span>), Req2_decode, Req3_decode]</span><br><span class="line">Step <span class="number">3</span>: [<span class="built_in">Req1_prefill_chunk3</span>(<span class="number">512</span>), Req2_decode, Req3_decode]</span><br><span class="line">Step <span class="number">4</span>: [<span class="built_in">Req1_prefill_chunk4</span>(<span class="number">464</span>), Req2_decode, Req3_decode]</span><br><span class="line">Step <span class="number">5</span>: [Req1_decode, Req2_decode, Req3_decode, <span class="built_in">Req4_prefill</span>(NEW!)]</span><br><span class="line">       ↑ 任意时刻都可以加入新请求</span><br></pre></td></tr></table></figure>
<p><strong>效果</strong>：</p>
<ul>
<li>吞吐量提升 <strong>2-3 倍</strong></li>
<li>P99 延迟降低 <strong>50%</strong></li>
</ul>
<hr>
<h4 id="支持抢占的调度器"><a href="#支持抢占的调度器" class="headerlink" title="支持抢占的调度器"></a>支持抢占的调度器</h4><p>当显存不足时，低优先级请求会被抢占：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>策略</th>
<th>v0 (Swap)</th>
<th>v1 (Recompute)</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>抢占动作</strong></td>
<td>将 KV Cache swap 到 CPU</td>
<td>释放 KV Cache</td>
</tr>
<tr>
<td><strong>恢复动作</strong></td>
<td>swap 回 GPU</td>
<td>重新计算 prefill</td>
</tr>
<tr>
<td><strong>适用场景</strong></td>
<td>长文档、prefill 慢</td>
<td>短对话、prefill 快</td>
</tr>
<tr>
<td><strong>显存占用</strong></td>
<td>需要 CPU 内存</td>
<td>不占 CPU 内存</td>
</tr>
</tbody>
</table>
</div>
<hr>
<h3 id="1-2-本文聚焦"><a href="#1-2-本文聚焦" class="headerlink" title="1.2 本文聚焦"></a>1.2 本文聚焦</h3><p>从一个 API 请求出发，追踪到最终生成 token 的完整流程，深入源码理解 vLLM v1 的架构设计。</p>
<p><strong>阅读前提</strong>：</p>
<ul>
<li>了解 Transformer 基础（Attention、KV Cache）</li>
<li>了解 Python 异步编程（async/await）</li>
<li>有 PyTorch 使用经验</li>
</ul>
<hr>
<h2 id="2-核心架构：六层分工"><a href="#2-核心架构：六层分工" class="headerlink" title="2. 核心架构：六层分工"></a>2. 核心架构：六层分工</h2><h3 id="2-1-整体架构图"><a href="#2-1-整体架构图" class="headerlink" title="2.1 整体架构图"></a>2.1 整体架构图</h3><div class="svg-figure">
<svg viewBox="0 0 1180 980" width="100%" role="img" aria-label="vLLM API request to token generation architecture">
  <defs>
    <marker id="arrow-vllm-main" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#475569"/></marker>
    <style>
      .vbox{fill:#ffffff;stroke:#cbd5e1;stroke-width:2;rx:18;filter:drop-shadow(0 10px 14px rgba(15,23,42,.08));}
      .api{fill:#eff6ff;stroke:#93c5fd}.engine{fill:#f8fafc;stroke:#cbd5e1}.core{fill:#fffbeb;stroke:#fcd34d}.worker{fill:#ecfdf5;stroke:#86efac}.sample{fill:#f5f3ff;stroke:#c4b5fd}
      .vtitle{font:700 22px 'Times New Roman','SimSun',serif;fill:#0f172a}.vsub{font:16px 'Times New Roman','SimSun',serif;fill:#475569}.vsmall{font:14px 'Times New Roman','SimSun',serif;fill:#64748b}
      .varrow{stroke:#475569;stroke-width:2.6;fill:none;marker-end:url(#arrow-vllm-main)}.vline{stroke:#94a3b8;stroke-width:2;fill:none;stroke-dasharray:6 6}
    </style>
  </defs>

  <rect x="250" y="40" width="680" height="110" class="vbox api"/>
  <text x="590" y="78" text-anchor="middle" class="vtitle">API Layer</text>
  <text x="590" y="108" text-anchor="middle" class="vsub">FastAPI / gRPC / OpenAI-compatible endpoints</text>
  <text x="590" y="132" text-anchor="middle" class="vsmall">/v1/completions · /v1/chat/completions</text>
  <path d="M590 150 L590 205" class="varrow"/>
  <text x="620" y="183" class="vsmall">HTTP Request</text>

  <rect x="180" y="205" width="820" height="210" class="vbox engine"/>
  <text x="590" y="242" text-anchor="middle" class="vtitle">LLMEngine：前后处理层</text>
  <rect x="250" y="275" width="250" height="90" class="vbox api"/>
  <text x="375" y="312" text-anchor="middle" class="vsub">InputProcessor</text>
  <text x="375" y="340" text-anchor="middle" class="vsmall">tokenize / parse prompt</text>
  <rect x="680" y="275" width="250" height="90" class="vbox sample"/>
  <text x="805" y="312" text-anchor="middle" class="vsub">OutputProcessor</text>
  <text x="805" y="340" text-anchor="middle" class="vsmall">detokenize / stream output</text>
  <path d="M375 365 L375 470" class="varrow"/>
  <text x="402" y="438" class="vsmall">EngineCoreRequest</text>

  <rect x="180" y="470" width="820" height="420" class="vbox core"/>
  <text x="590" y="508" text-anchor="middle" class="vtitle">EngineCore：调度 + 执行核心</text>
  <rect x="260" y="545" width="250" height="80" class="vbox core"/>
  <text x="385" y="578" text-anchor="middle" class="vsub">Scheduler</text>
  <text x="385" y="604" text-anchor="middle" class="vsmall">决定谁算多少 token</text>
  <path d="M385 625 L385 675" class="varrow"/>
  <text x="415" y="657" class="vsmall">SchedulerOutput</text>
  <rect x="260" y="675" width="250" height="80" class="vbox api"/>
  <text x="385" y="708" text-anchor="middle" class="vsub">KVCacheManager</text>
  <text x="385" y="734" text-anchor="middle" class="vsmall">PagedAttention blocks</text>
  <path d="M385 755 L385 805" class="varrow"/>
  <text x="415" y="787" class="vsmall">allocated blocks</text>
  <rect x="260" y="805" width="250" height="80" class="vbox worker"/>
  <text x="385" y="838" text-anchor="middle" class="vsub">ModelExecutor</text>
  <text x="385" y="864" text-anchor="middle" class="vsmall">Worker / ModelRunner</text>
  <rect x="670" y="805" width="250" height="80" class="vbox sample"/>
  <text x="795" y="838" text-anchor="middle" class="vsub">Sampler</text>
  <text x="795" y="864" text-anchor="middle" class="vsmall">sample next token</text>
  <path d="M510 845 L670 845" class="varrow"/>
  <path d="M795 805 C795 675 805 510 805 365" class="varrow"/>
  <text x="825" y="625" class="vsmall">EngineCoreOutput</text>
  <path d="M680 320 L500 320" class="vline"/>
  <text x="590" y="306" text-anchor="middle" class="vsmall">文本响应回到用户</text>
</svg>
</div>

<h3 id="2-2-数据流转"><a href="#2-2-数据流转" class="headerlink" title="2.2 数据流转"></a>2.2 数据流转</h3><p>一个请求的数据变化：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line">用户输入 <span class="string">&quot;Hello, AI!&quot;</span></span><br><span class="line">  ↓ InputProcessor (tokenize)</span><br><span class="line">token_ids [<span class="number">15496</span>, <span class="number">11</span>, <span class="number">15000</span>, <span class="number">0</span>]</span><br><span class="line">  ↓ Scheduler (调度决策)</span><br><span class="line"><span class="class">SchedulerOutput </span>&#123;</span><br><span class="line"><span class="symbol">  scheduled_new_reqs:</span> [req_1],</span><br><span class="line"><span class="symbol">  num_scheduled_tokens:</span> &#123;req_1: <span class="number">4</span>&#125;  ← prefill <span class="number">4</span> tokens</span><br><span class="line">&#125;</span><br><span class="line">  ↓ ModelExecutor (forward)</span><br><span class="line">logits [<span class="number">1</span>, <span class="number">4</span>, <span class="number">32000</span>]  ← (batch, seq, vocab_size)</span><br><span class="line">  ↓ Sampler (采样)</span><br><span class="line"><span class="symbol">next_token_id:</span> <span class="number">9906</span>  ← <span class="string">&quot;Hi&quot;</span></span><br><span class="line">  ↓ OutputProcessor (detokenize)</span><br><span class="line">生成文本: <span class="string">&quot;Hi&quot;</span></span><br><span class="line">  ↓ 返回给用户</span><br></pre></td></tr></table></figure>
<!-- SECTION_2_3 -->
<h3 id="2-3-六大组件详解"><a href="#2-3-六大组件详解" class="headerlink" title="2.3 六大组件详解"></a>2.3 六大组件详解</h3><p>本节部分使用伪代码，在实际代码中会有很多校验和分支，就不赘述了。</p>
<h4 id="1-LLMEngine：前后处理层"><a href="#1-LLMEngine：前后处理层" class="headerlink" title="(1) LLMEngine：前后处理层"></a>(1) LLMEngine：前后处理层</h4><p><strong>作用</strong>：请求的预处理和响应的后处理</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LLMEngine</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="comment"># 输入处理：tokenizer + embedding</span></span><br><span class="line">        self.input_processor = InputProcessor(tokenizer, ...)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 核心引擎（调度+执行）</span></span><br><span class="line">        self.engine_core = EngineCoreClient.make_client(...)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 输出处理：detokenizer</span></span><br><span class="line">        self.output_processor = OutputProcessor(tokenizer, ...)</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">add_request</span>(<span class="params">self, request_id, prompt, sampling_params</span>):</span></span><br><span class="line">        <span class="comment"># 1. 预处理：prompt → token_ids</span></span><br><span class="line">        request = self.input_processor.process_inputs(</span><br><span class="line">            request_id, prompt, ...)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 注册到输出处理器</span></span><br><span class="line">        self.output_processor.add_request(request, ...)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 3. 提交给引擎核心</span></span><br><span class="line">        self.engine_core.add_request(request)</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">step</span>(<span class="params">self</span>) -&gt; <span class="built_in">list</span>[RequestOutput]:</span></span><br><span class="line">        <span class="comment"># 1. 从 EngineCore 拉取输出</span></span><br><span class="line">        outputs = self.engine_core.get_output()</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 后处理：token_ids → 文本</span></span><br><span class="line">        request_outputs = self.output_processor.process_outputs(outputs)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 3. Abort 已完成的请求</span></span><br><span class="line">        self._abort_finished_requests(request_outputs)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> request_outputs</span><br></pre></td></tr></table></figure>
<hr>
<h4 id="2-EngineCore：调度-执行核心"><a href="#2-EngineCore：调度-执行核心" class="headerlink" title="(2) EngineCore：调度+执行核心"></a>(2) EngineCore：调度+执行核心</h4><p><strong>作用</strong>：串联 Scheduler、KVCacheManager、ModelExecutor</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EngineCore</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.scheduler = Scheduler(...)</span><br><span class="line">		<span class="comment"># 这里初始化不同的executor：GPU/CPU/TPU/others</span></span><br><span class="line">        self.model_executor = executor_class(vllm_config)</span><br><span class="line">		<span class="comment"># kv cache配置</span></span><br><span class="line">		num_gpu_blocks, num_cpu_blocks, kv_cache_config = \</span><br><span class="line">            self._initialize_kv_caches(vllm_config)</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">step</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="comment"># 1. 调度：决定本步算哪些请求、各算多少 token</span></span><br><span class="line">        scheduler_output = self.scheduler.schedule()</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 执行：模型 forward + 采样</span></span><br><span class="line">        model_output = self.model_executor.execute_model(</span><br><span class="line">            scheduler_output)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 3. 更新：根据输出更新 scheduler 状态</span></span><br><span class="line">        engine_core_outputs = self.scheduler.update_from_output(</span><br><span class="line">            scheduler_output, model_output)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> engine_core_outputs</span><br></pre></td></tr></table></figure>
<hr>
<h4 id="3-Scheduler：请求调度器"><a href="#3-Scheduler：请求调度器" class="headerlink" title="(3) Scheduler：请求调度器"></a>(3) Scheduler：请求调度器</h4><p><strong>作用</strong>：决定”谁算多少 token”，管理三个队列</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Scheduler</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.waiting = RequestQueue()    <span class="comment"># 等待队列</span></span><br><span class="line">        self.running = RequestQueue()    <span class="comment"># 运行队列</span></span><br><span class="line">        self.kv_cache_manager = KVCacheManager()</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">schedule</span>(<span class="params">self</span>) -&gt; SchedulerOutput:</span></span><br><span class="line">        token_budget = self.max_num_scheduled_tokens  <span class="comment"># 本步预算</span></span><br><span class="line">        scheduled_new_reqs = []</span><br><span class="line">        scheduled_running_reqs = []</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 阶段1: 调度 RUNNING 队列（优先保证 decode）</span></span><br><span class="line">        <span class="keyword">while</span> req_index &lt; <span class="built_in">len</span>(self.running) <span class="keyword">and</span> token_budget &gt; <span class="number">0</span>:</span><br><span class="line">            <span class="comment"># 本步需要计算的token数量</span></span><br><span class="line">            num_new_tokens = <span class="built_in">min</span>(</span><br><span class="line">                request.num_tokens_with_spec - request.num_computed_tokens,</span><br><span class="line">                token_budget</span><br><span class="line">            )</span><br><span class="line">            </span><br><span class="line">            <span class="comment"># 尝试分配 KV blocks</span></span><br><span class="line">            new_blocks = self.kv_cache_manager.allocate_slots(</span><br><span class="line">                request, num_new_tokens)</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">if</span> new_blocks <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">                <span class="comment"># 显存不足，抢占低优先级请求</span></span><br><span class="line">                self._preempt_request(lowest_priority_req)</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            </span><br><span class="line">            scheduled_running_reqs.append(request)</span><br><span class="line">            token_budget -= num_new_tokens</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 阶段2: 调度 WAITING 队列（新请求入场）</span></span><br><span class="line">        <span class="keyword">if</span> token_budget &gt; <span class="number">0</span> <span class="keyword">and</span> <span class="keyword">not</span> preempted_reqs:</span><br><span class="line">            <span class="keyword">for</span> request <span class="keyword">in</span> self.waiting:</span><br><span class="line">                <span class="comment"># Prefix Caching 查询</span></span><br><span class="line">                num_computed_tokens = self.kv_cache_manager.get_computed_blocks(</span><br><span class="line">                    request)</span><br><span class="line">                </span><br><span class="line">                <span class="comment"># 计算本步需要算多少 token（Chunked Prefill）</span></span><br><span class="line">                num_new_tokens = <span class="built_in">min</span>(</span><br><span class="line">                    request.num_tokens - num_computed_tokens,</span><br><span class="line">                    token_budget,</span><br><span class="line">                    self.long_prefill_token_threshold  <span class="comment"># chunk 上限</span></span><br><span class="line">                )</span><br><span class="line">                </span><br><span class="line">                <span class="comment"># 分配 blocks</span></span><br><span class="line">                new_blocks = self.kv_cache_manager.allocate_slots(...)</span><br><span class="line">                <span class="keyword">if</span> new_blocks <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">                    self.waiting.remove(request)</span><br><span class="line">                    self.running.append(request)</span><br><span class="line">                    scheduled_new_reqs.append(request)</span><br><span class="line">                    token_budget -= num_new_tokens</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> SchedulerOutput(</span><br><span class="line">            scheduled_new_reqs=scheduled_new_reqs,</span><br><span class="line">            scheduled_running_reqs=scheduled_running_reqs,</span><br><span class="line">            num_scheduled_tokens=&#123;req.<span class="built_in">id</span>: num_tokens&#125;,</span><br><span class="line">        )</span><br></pre></td></tr></table></figure>
<p><strong>关键理解</strong>：</p>
<ul>
<li><strong>先 running 后 waiting</strong>：保证已有请求的 decode 流畅</li>
<li><strong>token_budget 全局共享</strong>：prefill 和 decode 混合在同一 batch</li>
<li><strong>Chunked Prefill</strong>：长 prompt 切片，避免饥饿</li>
</ul>
<hr>
<h4 id="4-KVCacheManager：显存管理"><a href="#4-KVCacheManager：显存管理" class="headerlink" title="(4) KVCacheManager：显存管理"></a>(4) KVCacheManager：显存管理</h4><p><strong>作用</strong>：管理 KV Cache 的分配和释放</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">KVCacheManager</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.coordinator = KVCacheCoordinator()  <span class="comment"># Block 管理器</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">allocate_slots</span>(<span class="params">self, request, num_new_tokens</span>):</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;分配物理 blocks&quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">return</span> self.coordinator.allocate_new_blocks(</span><br><span class="line">            request, num_new_tokens)</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_computed_blocks</span>(<span class="params">self, request</span>):</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;Prefix Caching：查询命中的 blocks&quot;&quot;&quot;</span></span><br><span class="line">        block_hashes = [<span class="built_in">hash</span>(tokens) <span class="keyword">for</span> tokens <span class="keyword">in</span> request.token_chunks]</span><br><span class="line">        <span class="keyword">return</span> self.coordinator.find_cached_blocks(block_hashes)</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">free</span>(<span class="params">self, request</span>):</span></span><br><span class="line">        <span class="string">&quot;&quot;&quot;释放 request 占用的 blocks&quot;&quot;&quot;</span></span><br><span class="line">        self.coordinator.free(request.request_id)</span><br></pre></td></tr></table></figure>
<p><strong>Block Pool 示意</strong>：<br><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">Physical</span> <span class="selector-tag">Blocks</span>: <span class="selector-attr">[0]</span><span class="selector-attr">[1]</span><span class="selector-attr">[2]</span><span class="selector-attr">[3]</span><span class="selector-attr">[4]</span><span class="selector-attr">[5]</span><span class="selector-attr">[6]</span><span class="selector-attr">[7]</span>...</span><br><span class="line">                  ↑  ↑          ↑  ↑</span><br><span class="line"><span class="selector-tag">Request</span> <span class="selector-tag">1</span> <span class="selector-tag">Table</span>: <span class="selector-attr">[0, 1, 4, 5]</span>  ← 逻辑连续，物理不连续</span><br><span class="line"><span class="selector-tag">Request</span> <span class="selector-tag">2</span> <span class="selector-tag">Table</span>: <span class="selector-attr">[0, 1, 2]</span>     ← 与 <span class="selector-tag">R1</span> 共享 <span class="selector-attr">[0, 1]</span></span><br></pre></td></tr></table></figure></p>
<hr>
<h4 id="5-ModelExecutor-Worker-ModelRunner"><a href="#5-ModelExecutor-Worker-ModelRunner" class="headerlink" title="(5) ModelExecutor / Worker / ModelRunner"></a>(5) ModelExecutor / Worker / ModelRunner</h4><p><strong>作用</strong>：执行模型推理</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GPUExecutor</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">execute_model</span>(<span class="params">self, scheduler_output</span>):</span></span><br><span class="line">        <span class="keyword">return</span> self.driver_worker.execute_model(scheduler_output)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Worker</span>(<span class="params">WorkerBase</span>):</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span>(<span class="params">self</span>):</span></span><br><span class="line">		self.device: <span class="type">Optional</span>[torch.device] </span><br><span class="line">        self.model_runner: <span class="type">Optional</span>[nn.Module]  <span class="comment"># 模型运行器</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">load_model</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.model_runner.load_model()</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">execute_model</span>(<span class="params">self, scheduler_output</span>):</span></span><br><span class="line">        <span class="keyword">return</span> self.model_runner.execute_model(scheduler_output)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GPUModelRunner</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">execute_model</span>(<span class="params">self, scheduler_output</span>):</span></span><br><span class="line">        <span class="comment"># 1. 更新 PersistentBatch 状态</span></span><br><span class="line">        self._update_states(scheduler_output)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 准备输入张量</span></span><br><span class="line">        model_inputs = self._prepare_inputs(scheduler_output)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 3. 模型 forward</span></span><br><span class="line">        hidden_states = self.model(**model_inputs)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 4. 采样生成 token</span></span><br><span class="line">        sampled_token_ids = self._sample(hidden_states)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> ModelExecutorOutput(sampled_token_ids=sampled_token_ids)</span><br></pre></td></tr></table></figure>
<hr>
<h4 id="6-Sampler-OutputProcessor"><a href="#6-Sampler-OutputProcessor" class="headerlink" title="(6) Sampler / OutputProcessor"></a>(6) Sampler / OutputProcessor</h4><p><strong>Sampler</strong>：从 logits 采样 token</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Sampler</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">sample</span>(<span class="params">self, logits, sampling_metadata</span>):</span></span><br><span class="line">        <span class="comment"># 应用 temperature </span></span><br><span class="line">        <span class="comment"># logits = logits / temperature</span></span><br><span class="line">	    logits = self.apply_temperature(logits, </span><br><span class="line">					    sampling_metadata.temperature,</span><br><span class="line">				        sampling_metadata.all_random)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 应用 top-k / top-p</span></span><br><span class="line">		 random_sampled, processed_logprobs = </span><br><span class="line">			 self.topk_topp_sampler(logits)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 采样</span></span><br><span class="line">        probs = torch.softmax(logits, dim=-<span class="number">1</span>)</span><br><span class="line">        token_ids = random_sample(probs, generators)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> token_ids</span><br></pre></td></tr></table></figure>
<p><strong>OutputProcessor</strong>：detokenize</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">OutputProcessor</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">process_outputs</span>(<span class="params">self, engine_core_outputs</span>):</span></span><br><span class="line">        outputs = []</span><br><span class="line">        <span class="keyword">for</span> output <span class="keyword">in</span> engine_core_outputs:</span><br><span class="line">            text = self.tokenizer.decode(</span><br><span class="line">                output.output_token_ids,</span><br><span class="line">                skip_special_tokens=<span class="literal">True</span></span><br><span class="line">            )</span><br><span class="line">            outputs.append(RequestOutput(text=text, ...))</span><br><span class="line">        <span class="keyword">return</span> outputs</span><br></pre></td></tr></table></figure>
<hr>
<h2 id="3-一个请求的完整生命周期"><a href="#3-一个请求的完整生命周期" class="headerlink" title="3. 一个请求的完整生命周期"></a>3. 一个请求的完整生命周期</h2><h3 id="3-1-接收阶段：LLM-generate-到-add-request"><a href="#3-1-接收阶段：LLM-generate-到-add-request" class="headerlink" title="3.1 接收阶段：LLM.generate 到 add_request"></a>3.1 接收阶段：LLM.generate 到 add_request</h3><p><strong>调用链</strong>：<br><figure class="highlight reasonml"><table><tr><td class="code"><pre><span class="line"><span class="module-access"><span class="module"><span class="identifier">LLM</span>.</span></span>generate<span class="literal">()</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">LLM</span>.</span><span class="module"><span class="identifier">_add_request</span>(</span></span>)</span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">LLMEngine</span>.</span></span>add<span class="constructor">_request()</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">InputProcessor</span>.</span></span>process<span class="constructor">_inputs()</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">EngineCore</span>.</span></span>add<span class="constructor">_request()</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">Scheduler</span>.</span></span>add<span class="constructor">_request()</span></span><br></pre></td></tr></table></figure></p>
<p><strong>关键源码</strong>：InputProcessor 处理不同类型的输入</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_prompt_to_llm_inputs</span>(<span class="params">self, prompt</span>):</span></span><br><span class="line">    parsed = parse_prompt(prompt)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> parsed[<span class="string">&quot;type&quot;</span>] == <span class="string">&quot;tokens&quot;</span>:</span><br><span class="line">        <span class="comment"># 已经是 token_ids</span></span><br><span class="line">        <span class="keyword">return</span> self._process_tokens(parsed[<span class="string">&quot;content&quot;</span>])</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> parsed[<span class="string">&quot;type&quot;</span>] == <span class="string">&quot;text&quot;</span>:</span><br><span class="line">        <span class="comment"># 文本 + 多模态数据</span></span><br><span class="line">        <span class="keyword">return</span> self._process_text(</span><br><span class="line">            parsed[<span class="string">&quot;content&quot;</span>],</span><br><span class="line">            tokenization_kwargs=...,</span><br><span class="line">            mm_uuids=...</span><br><span class="line">        )</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> parsed[<span class="string">&quot;type&quot;</span>] == <span class="string">&quot;embeds&quot;</span>:</span><br><span class="line">        <span class="comment"># 直接是 embeddings</span></span><br><span class="line">        <span class="keyword">return</span> self._process_embeds(parsed[<span class="string">&quot;content&quot;</span>])</span><br></pre></td></tr></table></figure>
<p><strong>支持的输入格式</strong>：</p>
<ul>
<li><code>str</code>：纯文本</li>
<li><code>List[int]</code>：token_ids</li>
<li><code>Dict</code>：多模态输入（文本 + 图片）</li>
<li><code>torch.Tensor</code>：embeddings</li>
</ul>
<hr>
<h3 id="3-2-调度阶段：Scheduler-schedule-的核心逻辑"><a href="#3-2-调度阶段：Scheduler-schedule-的核心逻辑" class="headerlink" title="3.2 调度阶段：Scheduler.schedule 的核心逻辑"></a>3.2 调度阶段：Scheduler.schedule 的核心逻辑</h3><p><strong>完整调用流程</strong>：<br><figure class="highlight reasonml"><table><tr><td class="code"><pre><span class="line"><span class="module-access"><span class="module"><span class="identifier">LLM</span>.</span><span class="module"><span class="identifier">_run_engine</span>(</span></span>)</span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">LLMEngine</span>.</span></span>step<span class="literal">()</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">EngineCore</span>.</span></span>step<span class="literal">()</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">Scheduler</span>.</span></span>schedule<span class="literal">()</span></span><br><span class="line">    → <span class="module-access"><span class="module"><span class="identifier">KVCacheManager</span>.</span></span>allocate<span class="constructor">_slots()</span></span><br><span class="line">    → <span class="module-access"><span class="module"><span class="identifier">KVCacheManager</span>.</span></span>get<span class="constructor">_computed_blocks()</span>  ← Prefix Caching</span><br></pre></td></tr></table></figure></p>
<h4 id="关键逻辑-1：计算本次需要处理的-token-数"><a href="#关键逻辑-1：计算本次需要处理的-token-数" class="headerlink" title="关键逻辑 1：计算本次需要处理的 token 数"></a>关键逻辑 1：计算本次需要处理的 token 数</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 还差多少 token 要算</span></span><br><span class="line">num_new_tokens = (</span><br><span class="line">    request.num_tokens_with_spec +</span><br><span class="line">    request.num_output_placeholders -</span><br><span class="line">    request.num_computed_tokens</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Chunked Prefill 的 chunk 上限</span></span><br><span class="line"><span class="keyword">if</span> <span class="number">0</span> &lt; self.long_prefill_token_threshold &lt; num_new_tokens:</span><br><span class="line">    num_new_tokens = self.long_prefill_token_threshold</span><br><span class="line"></span><br><span class="line"><span class="comment"># 不超过剩余预算</span></span><br><span class="line">num_new_tokens = <span class="built_in">min</span>(num_new_tokens, token_budget)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 不超过模型最大长度</span></span><br><span class="line">num_new_tokens = <span class="built_in">min</span>(</span><br><span class="line">    num_new_tokens,</span><br><span class="line">    self.max_model_len - request.num_computed_tokens</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<p><strong>判断 Prefill vs Decode</strong>：</p>
<ul>
<li><code>num_new_tokens == 1</code>：Decode（生成阶段）</li>
<li><code>num_new_tokens &gt; 1</code>：Prefill（处理 prompt 阶段）</li>
</ul>
<hr>
<h4 id="关键逻辑-2：抢占机制"><a href="#关键逻辑-2：抢占机制" class="headerlink" title="关键逻辑 2：抢占机制"></a>关键逻辑 2：抢占机制</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">new_blocks = self.kv_cache_manager.allocate_slots(</span><br><span class="line">    request, num_new_tokens, ...)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> new_blocks <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">    <span class="comment"># 显存不足，需要抢占</span></span><br><span class="line">    <span class="keyword">if</span> self.policy == SchedulingPolicy.PRIORITY:</span><br><span class="line">        <span class="comment"># 抢占最低优先级的请求</span></span><br><span class="line">        preempted_req = <span class="built_in">max</span>(</span><br><span class="line">            self.running,</span><br><span class="line">            key=<span class="keyword">lambda</span> r: (r.priority, r.arrival_time)</span><br><span class="line">        )</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="comment"># FCFS：抢占队尾（最新的）</span></span><br><span class="line">        preempted_req = self.running.pop()</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 释放 KV Cache（v1 recompute 策略）</span></span><br><span class="line">    self.kv_cache_manager.free(preempted_req)</span><br><span class="line">    preempted_req.status = RequestStatus.PREEMPTED</span><br><span class="line">    preempted_req.num_computed_tokens = <span class="number">0</span>  <span class="comment"># 下次重新计算</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 放回等待队列</span></span><br><span class="line">    self.waiting.prepend_request(preempted_req)</span><br></pre></td></tr></table></figure>
<p><strong>v1 的抢占策略</strong>：</p>
<ul>
<li>直接释放 KV Cache（不 swap 到 CPU）</li>
<li>下次调度时重新计算 prefill</li>
<li>优点：不占 CPU 内存，代码简单</li>
<li>缺点：重复计算（但 prefill 通常很快）</li>
</ul>
<hr>
<h4 id="关键逻辑-3：Prefix-Caching"><a href="#关键逻辑-3：Prefix-Caching" class="headerlink" title="关键逻辑 3：Prefix Caching"></a>关键逻辑 3：Prefix Caching</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> request.num_computed_tokens == <span class="number">0</span>:</span><br><span class="line">    <span class="comment"># 本地缓存查询</span></span><br><span class="line">    new_computed_blocks, num_local_cached_tokens = \</span><br><span class="line">        self.kv_cache_manager.get_computed_blocks(request)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 外部缓存查询（P/D disaggregation 场景）</span></span><br><span class="line">    <span class="keyword">if</span> self.connector <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">        num_external_cached_tokens, load_async = \</span><br><span class="line">            self.connector.get_num_new_matched_tokens(</span><br><span class="line">                request, num_local_cached_tokens)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 总缓存命中数</span></span><br><span class="line">    num_computed_tokens = (</span><br><span class="line">        num_local_cached_tokens + num_external_cached_tokens</span><br><span class="line">    )</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 更新统计</span></span><br><span class="line">    request.prefill_stats.num_cached_tokens = num_computed_tokens</span><br></pre></td></tr></table></figure>
<p><strong>工作原理</strong>：</p>
<ol>
<li>计算 request 的 block hash（基于 token_ids）</li>
<li>查询 hash 是否在缓存中</li>
<li>命中的 block 直接复用，跳过计算</li>
</ol>
<hr>
<h4 id="关键逻辑-4：Chunked-Prefill"><a href="#关键逻辑-4：Chunked-Prefill" class="headerlink" title="关键逻辑 4：Chunked Prefill"></a>关键逻辑 4：Chunked Prefill</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 剩余 token 数</span></span><br><span class="line">num_new_tokens = request.num_tokens - num_computed_tokens</span><br><span class="line"></span><br><span class="line"><span class="comment"># 如果没开 chunked prefill 且放不下，跳过</span></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">not</span> self.scheduler_config.enable_chunked_prefill \</span><br><span class="line">    <span class="keyword">and</span> num_new_tokens &gt; token_budget:</span><br><span class="line">    <span class="comment"># 本步不调度，等下一步</span></span><br><span class="line">    <span class="keyword">continue</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 开了 chunked prefill，切到 budget 允许的大小</span></span><br><span class="line">num_new_tokens = <span class="built_in">min</span>(num_new_tokens, token_budget)</span><br></pre></td></tr></table></figure>
<p><strong>效果</strong>：<br><figure class="highlight css"><table><tr><td class="code"><pre><span class="line">传统（无 chunked prefill）:</span><br><span class="line">Step <span class="number">1</span>: [<span class="built_in">Req1_prefill</span>(<span class="number">2000</span> tok)]  ← 独占</span><br><span class="line">Step <span class="number">2</span>: [Req1_decode, Req2_prefill, Req3_prefill]</span><br><span class="line"></span><br><span class="line">Chunked Prefill:</span><br><span class="line">Step <span class="number">1</span>: [<span class="built_in">Req1_chunk</span>(<span class="number">512</span>), <span class="built_in">Req2_prefill</span>(<span class="number">10</span>), <span class="built_in">Req3_prefill</span>(<span class="number">10</span>)]</span><br><span class="line">Step <span class="number">2</span>: [<span class="built_in">Req1_chunk</span>(<span class="number">512</span>), Req2_decode, Req3_decode]</span><br><span class="line">Step <span class="number">3</span>: [<span class="built_in">Req1_chunk</span>(<span class="number">512</span>), Req2_decode, Req3_decode]</span><br></pre></td></tr></table></figure></p>
<hr>
<h3 id="3-3-执行阶段：Worker-execute-model"><a href="#3-3-执行阶段：Worker-execute-model" class="headerlink" title="3.3 执行阶段：Worker.execute_model"></a>3.3 执行阶段：Worker.execute_model</h3><p><strong>调用链</strong>：<br><figure class="highlight reasonml"><table><tr><td class="code"><pre><span class="line"><span class="module-access"><span class="module"><span class="identifier">EngineCore</span>.</span></span>step<span class="literal">()</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">ModelExecutor</span>.</span></span>execute<span class="constructor">_model(<span class="params">scheduler_output</span>)</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">Worker</span>.</span></span>execute<span class="constructor">_model(<span class="params">scheduler_output</span>)</span></span><br><span class="line">  → <span class="module-access"><span class="module"><span class="identifier">GPUModelRunner</span>.</span></span>execute<span class="constructor">_model(<span class="params">scheduler_output</span>)</span></span><br></pre></td></tr></table></figure></p>
<h4 id="3-3-1-更新状态（-update-states）"><a href="#3-3-1-更新状态（-update-states）" class="headerlink" title="3.3.1 更新状态（_update_states）"></a>3.3.1 更新状态（_update_states）</h4><p><strong>作用</strong>：将 scheduler 的调度决策应用到 ModelRunner 的内部状态。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_update_states</span>(<span class="params">self, scheduler_output: SchedulerOutput</span>):</span></span><br><span class="line">    <span class="comment"># 1. 处理新请求（首次调度）</span></span><br><span class="line">    <span class="keyword">for</span> new_req_data <span class="keyword">in</span> scheduler_output.scheduled_new_reqs:</span><br><span class="line">        self.input_batch.add_request(</span><br><span class="line">            request_id=new_req_data.request_id,</span><br><span class="line">            prompt_token_ids=new_req_data.prompt_token_ids,</span><br><span class="line">            block_ids=new_req_data.block_ids,</span><br><span class="line">            ...</span><br><span class="line">        )</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 2. 更新 running 请求（增量）</span></span><br><span class="line">    <span class="keyword">for</span> cached_req_data <span class="keyword">in</span> scheduler_output.scheduled_cached_reqs:</span><br><span class="line">        self.input_batch.update_num_computed_tokens(</span><br><span class="line">            cached_req_data.request_id,</span><br><span class="line">            cached_req_data.num_computed_tokens,</span><br><span class="line">        )</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 3. 移除已完成的请求</span></span><br><span class="line">    <span class="keyword">for</span> req_id <span class="keyword">in</span> scheduler_output.finished_req_ids:</span><br><span class="line">        self.input_batch.remove_request(req_id)</span><br></pre></td></tr></table></figure>
<p><strong>关键理解</strong>：</p>
<ul>
<li><strong>PersistentBatch</strong>：v1 的 batch 是”持久化”的，不是每步重建</li>
<li><strong>增量更新</strong>：只传新增的 token 和 block，减少 IPC 开销</li>
<li><strong>相比 v0 的优势</strong>：省去每步重建 batch 的开销（约 5-10% 提速）</li>
</ul>
<hr>
<h4 id="3-3-2-准备输入（-prepare-inputs）"><a href="#3-3-2-准备输入（-prepare-inputs）" class="headerlink" title="3.3.2 准备输入（_prepare_inputs）"></a>3.3.2 准备输入（_prepare_inputs）</h4><p><strong>作用</strong>：构造模型 forward 需要的张量。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_prepare_inputs</span>(<span class="params">self, scheduler_output</span>):</span></span><br><span class="line">    input_ids = []</span><br><span class="line">    positions = []</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 1. 收集所有要计算的 token</span></span><br><span class="line">    <span class="keyword">for</span> req_id, num_tokens <span class="keyword">in</span> scheduler_output.num_scheduled_tokens.items():</span><br><span class="line">        req_state = self.input_batch.get_request(req_id)</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> req_state.is_prefill:</span><br><span class="line">            <span class="comment"># Prefill: 多个 token</span></span><br><span class="line">            start = req_state.num_computed_tokens</span><br><span class="line">            end = start + num_tokens</span><br><span class="line">            input_ids.extend(req_state.token_ids[start:end])</span><br><span class="line">            positions.extend(<span class="built_in">range</span>(start, end))</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># Decode: 只有 1 个 token</span></span><br><span class="line">            input_ids.append(req_state.last_token_id)</span><br><span class="line">            positions.append(req_state.num_computed_tokens)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 2. 构造 Attention Metadata（PagedAttention 需要）</span></span><br><span class="line">    attn_metadata = self.attn_backend.make_metadata(</span><br><span class="line">        <span class="comment"># 每个 request 的 block table</span></span><br><span class="line">        block_tables=self._get_block_tables(),</span><br><span class="line">        <span class="comment"># token → 物理 slot 的映射</span></span><br><span class="line">        slot_mapping=self._get_slot_mapping(),</span><br><span class="line">        <span class="comment"># 每个 request 在 batch 中的边界（前缀和）</span></span><br><span class="line">        query_start_loc=self._get_query_start_loc(),</span><br><span class="line">        <span class="comment"># 每个 request 的序列长度</span></span><br><span class="line">        seq_lens=self._get_seq_lens(),</span><br><span class="line">    )</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;input_ids&quot;</span>: torch.tensor(input_ids, device=<span class="string">&quot;cuda&quot;</span>),</span><br><span class="line">        <span class="string">&quot;positions&quot;</span>: torch.tensor(positions, device=<span class="string">&quot;cuda&quot;</span>),</span><br><span class="line">        <span class="string">&quot;attn_metadata&quot;</span>: attn_metadata,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p><strong>Prefill vs Decode 的输入差异</strong>：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>字段</th>
<th>Prefill</th>
<th>Decode</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>input_ids.shape</code></td>
<td><code>[sum(prompt_lens)]</code></td>
<td><code>[batch_size]</code></td>
</tr>
<tr>
<td><code>positions</code></td>
<td><code>[0,1,2,...,L-1]</code> per req</td>
<td><code>[L]</code> per req</td>
</tr>
<tr>
<td><code>query_start_loc</code></td>
<td><code>[0, L1, L1+L2, ...]</code></td>
<td><code>[0, 1, 2, ...]</code></td>
</tr>
<tr>
<td><code>block_tables</code></td>
<td>新分配的 blocks</td>
<td>包含历史所有 blocks</td>
</tr>
</tbody>
</table>
</div>
<hr>
<h4 id="3-3-3-模型推理（forward）"><a href="#3-3-3-模型推理（forward）" class="headerlink" title="3.3.3 模型推理（forward）"></a>3.3.3 模型推理（forward）</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">hidden_states = self.model(</span><br><span class="line">    input_ids=input_ids,           <span class="comment"># [num_tokens]</span></span><br><span class="line">    positions=positions,           <span class="comment"># [num_tokens]</span></span><br><span class="line">    kv_caches=self.kv_caches,      <span class="comment"># 显存中的 KV Cache</span></span><br><span class="line">    attn_metadata=attn_metadata,   <span class="comment"># PagedAttention metadata</span></span><br><span class="line">)</span><br><span class="line"><span class="comment"># 输出：[num_tokens, hidden_size]</span></span><br></pre></td></tr></table></figure>
<p><strong>Transformer 层内部</strong>（以 Llama 为例）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LlamaDecoderLayer</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, hidden_states, kv_cache, attn_metadata</span>):</span></span><br><span class="line">        <span class="comment"># 1. Attention（使用 PagedAttention）</span></span><br><span class="line">        residual = hidden_states</span><br><span class="line">        hidden_states = self.input_layernorm(hidden_states)</span><br><span class="line">        hidden_states = self.self_attn(</span><br><span class="line">            hidden_states,</span><br><span class="line">            kv_cache=kv_cache,</span><br><span class="line">            attn_metadata=attn_metadata,  <span class="comment"># ← 关键：block_tables 传入</span></span><br><span class="line">        )</span><br><span class="line">        hidden_states = residual + hidden_states</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. FFN</span></span><br><span class="line">        residual = hidden_states</span><br><span class="line">        hidden_states = self.post_attention_layernorm(hidden_states)</span><br><span class="line">        hidden_states = self.mlp(hidden_states)</span><br><span class="line">        hidden_states = residual + hidden_states</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> hidden_states</span><br></pre></td></tr></table></figure>
<p><strong>Self-Attention 调用 PagedAttention</strong>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LlamaAttention</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span>(<span class="params">self, hidden_states, kv_cache, attn_metadata</span>):</span></span><br><span class="line">        <span class="comment"># 1. QKV 投影</span></span><br><span class="line">        qkv = self.qkv_proj(hidden_states)</span><br><span class="line">        q, k, v = qkv.split(...)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 写入 KV Cache（PagedAttention）</span></span><br><span class="line">        <span class="comment"># 根据 attn_metadata.slot_mapping 写入指定 slot</span></span><br><span class="line">        cache_ops.reshape_and_cache(k, v, kv_cache, slot_mapping)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 3. Attention 计算（PagedAttention kernel）</span></span><br><span class="line">        <span class="comment"># 使用 block_tables 索引历史 KV</span></span><br><span class="line">        output = paged_attention(</span><br><span class="line">            q, kv_cache,</span><br><span class="line">            block_tables=attn_metadata.block_tables,</span><br><span class="line">            context_lens=attn_metadata.context_lens,</span><br><span class="line">        )</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 4. 输出投影</span></span><br><span class="line">        output = self.o_proj(output)</span><br><span class="line">        <span class="keyword">return</span> output</span><br></pre></td></tr></table></figure>
<hr>
<h4 id="3-3-4-采样（-sample）"><a href="#3-3-4-采样（-sample）" class="headerlink" title="3.3.4 采样（_sample）"></a>3.3.4 采样（_sample）</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_sample</span>(<span class="params">self, hidden_states, scheduler_output</span>):</span></span><br><span class="line">    <span class="comment"># 1. LM Head：hidden_states → logits</span></span><br><span class="line">    logits = self.model.lm_head(hidden_states)</span><br><span class="line">    <span class="comment"># logits.shape: [num_tokens, vocab_size]</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 2. 提取每个 request 的最后一个 token 的 logits</span></span><br><span class="line">    last_token_logits = []</span><br><span class="line">    <span class="keyword">for</span> req_id <span class="keyword">in</span> scheduler_output.num_scheduled_tokens:</span><br><span class="line">        last_idx = self._get_last_token_index(req_id)</span><br><span class="line">        last_token_logits.append(logits[last_idx])</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 3. 调用 Sampler</span></span><br><span class="line">    sampled_token_ids = self.sampler(</span><br><span class="line">        logits=torch.stack(last_token_logits),</span><br><span class="line">        sampling_metadata=self._get_sampling_metadata(),</span><br><span class="line">    )</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> sampled_token_ids  <span class="comment"># [batch_size]</span></span><br></pre></td></tr></table></figure>
<p><strong>为什么只对最后一个 token 采样</strong>？</p>
<ul>
<li><strong>Prefill</strong>：计算了所有 prompt token 的 hidden states，但只需最后一个的 logits 生成第一个输出 token</li>
<li><strong>Decode</strong>：每次只计算 1 个 token，就是那个需要采样的</li>
</ul>
<hr>
<h4 id="3-3-5-完整执行流程图"><a href="#3-3-5-完整执行流程图" class="headerlink" title="3.3.5 完整执行流程图"></a>3.3.5 完整执行流程图</h4><div class="svg-figure">
<svg viewBox="0 0 1180 860" width="100%" role="img" aria-label="vLLM ModelRunner execution flow">
  <defs>
    <marker id="arrow-exec" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#475569"/></marker>
    <style>
      .ebox{fill:#ffffff;stroke:#cbd5e1;stroke-width:2;rx:16;filter:drop-shadow(0 8px 12px rgba(15,23,42,.08));}
      .ein{fill:#eff6ff;stroke:#93c5fd}.estate{fill:#f8fafc;stroke:#cbd5e1}.etensor{fill:#ecfdf5;stroke:#86efac}.efwd{fill:#fffbeb;stroke:#fcd34d}.esample{fill:#f5f3ff;stroke:#c4b5fd}
      .etitle{font:700 20px 'Times New Roman','SimSun',serif;fill:#0f172a}.esub{font:15px 'Times New Roman','SimSun',serif;fill:#475569}.esm{font:14px 'Times New Roman','SimSun',serif;fill:#64748b}
      .earrow{stroke:#475569;stroke-width:2.4;fill:none;marker-end:url(#arrow-exec)}
    </style>
  </defs>

  <rect x="410" y="30" width="360" height="70" class="ebox ein"/>
  <text x="590" y="59" text-anchor="middle" class="etitle">scheduler_output</text>
  <text x="590" y="82" text-anchor="middle" class="esm">本步调度决策</text>
  <path d="M590 100 L590 145" class="earrow"/>

  <rect x="170" y="145" width="360" height="110" class="ebox estate"/>
  <text x="350" y="182" text-anchor="middle" class="etitle">_update_states</text>
  <text x="350" y="210" text-anchor="middle" class="esub">更新 PersistentBatch</text>
  <text x="350" y="234" text-anchor="middle" class="esm">新增 / 更新 / 移除请求</text>
  <rect x="650" y="145" width="360" height="110" class="ebox estate"/>
  <text x="830" y="184" text-anchor="middle" class="etitle">PersistentBatch</text>
  <text x="830" y="214" text-anchor="middle" class="esub">跨 step 保留 batch 状态</text>
  <path d="M530 200 L650 200" class="earrow"/>

  <path d="M350 255 L350 310" class="earrow"/>
  <rect x="170" y="310" width="360" height="120" class="ebox etensor"/>
  <text x="350" y="348" text-anchor="middle" class="etitle">_prepare_inputs</text>
  <text x="350" y="376" text-anchor="middle" class="esub">构造 forward 张量</text>
  <text x="350" y="402" text-anchor="middle" class="esm">input_ids · positions · attn_metadata</text>
  <rect x="650" y="310" width="360" height="120" class="ebox etensor"/>
  <text x="830" y="348" text-anchor="middle" class="etitle">Attention Metadata</text>
  <text x="830" y="376" text-anchor="middle" class="esub">block_tables / slot_mapping</text>
  <text x="830" y="402" text-anchor="middle" class="esm">PagedAttention 读取 KV Cache 的索引</text>
  <path d="M530 370 L650 370" class="earrow"/>

  <path d="M350 430 L350 485" class="earrow"/>
  <rect x="170" y="485" width="360" height="130" class="ebox efwd"/>
  <text x="350" y="524" text-anchor="middle" class="etitle">model.forward</text>
  <text x="350" y="552" text-anchor="middle" class="esub">Transformer 推理</text>
  <text x="350" y="578" text-anchor="middle" class="esm">Embedding → DecoderLayer × N → LM Head</text>
  <rect x="650" y="485" width="360" height="130" class="ebox efwd"/>
  <text x="830" y="524" text-anchor="middle" class="etitle">PagedAttention</text>
  <text x="830" y="552" text-anchor="middle" class="esub">读写 KV Cache</text>
  <text x="830" y="578" text-anchor="middle" class="esm">输出 logits [num_tokens, vocab]</text>
  <path d="M530 550 L650 550" class="earrow"/>

  <path d="M350 615 L350 670" class="earrow"/>
  <rect x="170" y="670" width="360" height="110" class="ebox esample"/>
  <text x="350" y="708" text-anchor="middle" class="etitle">_sample</text>
  <text x="350" y="736" text-anchor="middle" class="esub">只取每个请求最后一个 token 的 logits</text>
  <text x="350" y="760" text-anchor="middle" class="esm">Temperature / Top-k / Top-p</text>
  <rect x="650" y="670" width="360" height="110" class="ebox ein"/>
  <text x="830" y="708" text-anchor="middle" class="etitle">ModelExecutorOutput</text>
  <text x="830" y="738" text-anchor="middle" class="esub">sampled_token_ids: [...]</text>
  <path d="M530 725 L650 725" class="earrow"/>
</svg>
</div>

<hr>
<h3 id="3-4-输出阶段：从-logits-到用户文本"><a href="#3-4-输出阶段：从-logits-到用户文本" class="headerlink" title="3.4 输出阶段：从 logits 到用户文本"></a>3.4 输出阶段：从 logits 到用户文本</h3><h4 id="3-4-1-Scheduler-更新状态"><a href="#3-4-1-Scheduler-更新状态" class="headerlink" title="3.4.1 Scheduler 更新状态"></a>3.4.1 Scheduler 更新状态</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">update_from_output</span>(<span class="params">self, scheduler_output, model_output</span>):</span></span><br><span class="line">    <span class="keyword">for</span> req_id, sampled_token_id <span class="keyword">in</span> model_output.sampled_token_ids.items():</span><br><span class="line">        request = self.get_request(req_id)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 1. 更新已计算 token 数</span></span><br><span class="line">        num_new_tokens = scheduler_output.num_scheduled_tokens[req_id]</span><br><span class="line">        request.num_computed_tokens += num_new_tokens</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 添加生成的 token（如果是 decode 阶段完成）</span></span><br><span class="line">        <span class="keyword">if</span> request.num_computed_tokens &gt;= request.num_prompt_tokens:</span><br><span class="line">            request.output_token_ids.append(sampled_token_id)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 3. 检查是否完成</span></span><br><span class="line">        <span class="keyword">if</span> self._is_finished(request, sampled_token_id):</span><br><span class="line">            request.status = RequestStatus.FINISHED</span><br><span class="line">            self.running.remove(request)</span><br><span class="line">            self.finished_req_ids.append(req_id)</span><br><span class="line">            self.kv_cache_manager.free(request)  <span class="comment"># 释放 KV Cache</span></span><br></pre></td></tr></table></figure>
<p><strong>完成条件</strong>（满足任一）：</p>
<ul>
<li>生成了 <code>max_tokens</code> 个 token</li>
<li>遇到停止词（如 <code>&lt;|endoftext|&gt;</code>）</li>
<li>遇到 EOS token</li>
<li>用户主动 abort</li>
</ul>
<hr>
<h4 id="3-4-2-OutputProcessor-解码"><a href="#3-4-2-OutputProcessor-解码" class="headerlink" title="3.4.2 OutputProcessor 解码"></a>3.4.2 OutputProcessor 解码</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">process_outputs</span>(<span class="params">self, engine_core_outputs</span>):</span></span><br><span class="line">    request_outputs = []</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> output <span class="keyword">in</span> engine_core_outputs:</span><br><span class="line">        <span class="comment"># 1. Detokenize（token_ids → 文本）</span></span><br><span class="line">        text = self.tokenizer.decode(</span><br><span class="line">            output.output_token_ids,</span><br><span class="line">            skip_special_tokens=<span class="literal">True</span>,</span><br><span class="line">        )</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 构造 RequestOutput</span></span><br><span class="line">        request_output = RequestOutput(</span><br><span class="line">            request_id=output.request_id,</span><br><span class="line">            prompt=output.prompt,</span><br><span class="line">            outputs=[CompletionOutput(</span><br><span class="line">                text=text,</span><br><span class="line">                token_ids=output.output_token_ids,</span><br><span class="line">                finish_reason=output.finish_reason,</span><br><span class="line">            )],</span><br><span class="line">            finished=output.finished,</span><br><span class="line">        )</span><br><span class="line">        request_outputs.append(request_output)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> request_outputs</span><br></pre></td></tr></table></figure>
<hr>
<h4 id="3-4-3-流式输出（Streaming）"><a href="#3-4-3-流式输出（Streaming）" class="headerlink" title="3.4.3 流式输出（Streaming）"></a>3.4.3 流式输出（Streaming）</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">def</span> <span class="title">generate_stream</span>(<span class="params">prompt, sampling_params</span>):</span></span><br><span class="line">    request_id = <span class="keyword">await</span> engine.add_request(...)</span><br><span class="line">    </span><br><span class="line">    prev_text = <span class="string">&quot;&quot;</span></span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">for</span> output <span class="keyword">in</span> engine.step_async():</span><br><span class="line">        <span class="keyword">if</span> output.request_id == request_id:</span><br><span class="line">            current_text = output.outputs[<span class="number">0</span>].text</span><br><span class="line">            </span><br><span class="line">            <span class="comment"># 只返回增量部分</span></span><br><span class="line">            new_text = current_text[<span class="built_in">len</span>(prev_text):]</span><br><span class="line">            prev_text = current_text</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">yield</span> new_text</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">if</span> output.finished:</span><br><span class="line">                <span class="keyword">break</span></span><br></pre></td></tr></table></figure>
<p><strong>实现关键</strong>：</p>
<ul>
<li>OutputProcessor 维护每个 request 的 <code>prev_text</code></li>
<li>每次只返回 <code>new_text = current_text[len(prev_text):]</code></li>
<li>用户看到”打字机”效果</li>
</ul>
<hr>
<h2 id="4-PagedAttention：核心创新深入解析"><a href="#4-PagedAttention：核心创新深入解析" class="headerlink" title="4. PagedAttention：核心创新深入解析"></a>4. PagedAttention：核心创新深入解析</h2><h3 id="4-1-传统-KV-Cache-的问题"><a href="#4-1-传统-KV-Cache-的问题" class="headerlink" title="4.1 传统 KV Cache 的问题"></a>4.1 传统 KV Cache 的问题</h3><h4 id="内存布局对比"><a href="#内存布局对比" class="headerlink" title="内存布局对比"></a>内存布局对比</h4><p><strong>传统方案</strong>：预分配连续显存</p>
<figure class="highlight prolog"><table><tr><td class="code"><pre><span class="line"><span class="symbol">Request</span> <span class="number">1</span> (max_len=<span class="number">2048</span>):</span><br><span class="line">[<span class="symbol">Token_0</span>][<span class="symbol">Token_1</span>][<span class="symbol">Token_2</span>]...[<span class="symbol">Token_100</span>][████padding████] ← <span class="number">95</span><span class="comment">% 浪费</span></span><br><span class="line"></span><br><span class="line"><span class="symbol">Request</span> <span class="number">2</span> (max_len=<span class="number">2048</span>):</span><br><span class="line">[<span class="symbol">Token_0</span>][<span class="symbol">Token_1</span>]...[<span class="symbol">Token_50</span>][██████padding██████] ← <span class="number">97.5</span><span class="comment">% 浪费</span></span><br><span class="line"></span><br><span class="line">实际利用率：约 <span class="number">20</span><span class="comment">%</span></span><br></pre></td></tr></table></figure>
<p><strong>三大问题</strong>：</p>
<ol>
<li><strong>内存碎片</strong>：必须按 <code>max_tokens</code> 预留，但实际可能只用 5%</li>
<li><strong>无法共享</strong>：两个请求即使 prompt 相同，也要各自分配</li>
<li><strong>静态分配</strong>：一旦分配就无法改变大小</li>
</ol>
<p><strong>数据</strong>（vLLM 论文）：</p>
<ul>
<li>平均利用率：20.4%</li>
<li>最差情况：&lt; 10%（长 max_tokens 但实际生成很短）</li>
</ul>
<hr>
<h3 id="4-2-PagedAttention-的解决方案"><a href="#4-2-PagedAttention-的解决方案" class="headerlink" title="4.2 PagedAttention 的解决方案"></a>4.2 PagedAttention 的解决方案</h3><h4 id="核心思想：虚拟内存-Paging"><a href="#核心思想：虚拟内存-Paging" class="headerlink" title="核心思想：虚拟内存 Paging"></a>核心思想：虚拟内存 Paging</h4><p>像操作系统管理内存一样管理 KV Cache：</p>
<div class="svg-figure">
<svg viewBox="0 0 1180 620" width="100%" role="img" aria-label="PagedAttention physical block pool and request block tables">
  <defs>
    <marker id="arrow-block" markerWidth="10" markerHeight="10" refX="8" refY="5" orient="auto"><path d="M1,1 L9,5 L1,9 Z" fill="#475569"/></marker>
    <style>
      .bbox{fill:#ffffff;stroke:#cbd5e1;stroke-width:2;rx:14;filter:drop-shadow(0 8px 12px rgba(15,23,42,.08));}
      .phys{fill:#eff6ff;stroke:#93c5fd}.shared{fill:#dbeafe;stroke:#60a5fa}.private1{fill:#ecfdf5;stroke:#86efac}.private2{fill:#f5f3ff;stroke:#c4b5fd}
      .btitle{font:700 20px 'Times New Roman','SimSun',serif;fill:#0f172a}.bsub{font:15px 'Times New Roman','SimSun',serif;fill:#475569}.bsm{font:14px 'Times New Roman','SimSun',serif;fill:#64748b}.bid{font:700 18px 'Times New Roman','SimSun',serif;fill:#0f172a}
      .barrow{stroke:#475569;stroke-width:2.2;fill:none;marker-end:url(#arrow-block)}
    </style>
  </defs>

  <text x="590" y="48" text-anchor="middle" class="btitle">物理 Block Pool（GPU 显存）</text>
  <text x="590" y="76" text-anchor="middle" class="bsub">KV Cache 被切成固定大小的物理 block，请求只保存逻辑到物理的映射表</text>
  <g transform="translate(150 115)">
    <rect x="0" y="0" width="100" height="70" class="bbox shared"/><text x="50" y="43" text-anchor="middle" class="bid">0</text>
    <rect x="105" y="0" width="100" height="70" class="bbox shared"/><text x="155" y="43" text-anchor="middle" class="bid">1</text>
    <rect x="210" y="0" width="100" height="70" class="bbox phys"/><text x="260" y="43" text-anchor="middle" class="bid">2</text>
    <rect x="315" y="0" width="100" height="70" class="bbox phys"/><text x="365" y="43" text-anchor="middle" class="bid">3</text>
    <rect x="420" y="0" width="100" height="70" class="bbox private1"/><text x="470" y="43" text-anchor="middle" class="bid">4</text>
    <rect x="525" y="0" width="100" height="70" class="bbox private1"/><text x="575" y="43" text-anchor="middle" class="bid">5</text>
    <rect x="630" y="0" width="100" height="70" class="bbox private2"/><text x="680" y="43" text-anchor="middle" class="bid">6</text>
    <rect x="735" y="0" width="100" height="70" class="bbox phys"/><text x="785" y="43" text-anchor="middle" class="bid">7</text>
  </g>
  <text x="590" y="230" text-anchor="middle" class="bsm">蓝色 block 0/1 被多个请求共享；绿色和紫色 block 是各请求自己的后续 token</text>

  <rect x="110" y="285" width="420" height="210" class="bbox private1"/>
  <text x="320" y="322" text-anchor="middle" class="btitle">Request 1 Block Table</text>
  <text x="320" y="352" text-anchor="middle" class="bsub">逻辑连续：[0, 1, 2, 3]</text>
  <text x="320" y="382" text-anchor="middle" class="bsub">物理映射：[0, 1, 4, 5]</text>
  <g transform="translate(180 415)">
    <rect x="0" y="0" width="62" height="48" class="bbox shared"/><text x="31" y="31" text-anchor="middle" class="bid">0</text>
    <rect x="72" y="0" width="62" height="48" class="bbox shared"/><text x="103" y="31" text-anchor="middle" class="bid">1</text>
    <rect x="144" y="0" width="62" height="48" class="bbox private1"/><text x="175" y="31" text-anchor="middle" class="bid">4</text>
    <rect x="216" y="0" width="62" height="48" class="bbox private1"/><text x="247" y="31" text-anchor="middle" class="bid">5</text>
  </g>

  <rect x="650" y="285" width="420" height="210" class="bbox private2"/>
  <text x="860" y="322" text-anchor="middle" class="btitle">Request 2 Block Table</text>
  <text x="860" y="352" text-anchor="middle" class="bsub">逻辑连续：[0, 1, 2]</text>
  <text x="860" y="382" text-anchor="middle" class="bsub">物理映射：[0, 1, 6]</text>
  <g transform="translate(755 415)">
    <rect x="0" y="0" width="62" height="48" class="bbox shared"/><text x="31" y="31" text-anchor="middle" class="bid">0</text>
    <rect x="72" y="0" width="62" height="48" class="bbox shared"/><text x="103" y="31" text-anchor="middle" class="bid">1</text>
    <rect x="144" y="0" width="62" height="48" class="bbox private2"/><text x="175" y="31" text-anchor="middle" class="bid">6</text>
  </g>

  <path d="M250 415 C250 300 200 260 200 185" class="barrow"/>
  <path d="M322 415 C322 300 305 260 305 185" class="barrow"/>
  <path d="M394 415 C394 300 620 260 620 185" class="barrow"/>
  <path d="M466 415 C466 300 725 260 725 185" class="barrow"/>
  <path d="M786 415 C786 300 200 255 200 185" class="barrow"/>
  <path d="M858 415 C858 300 305 255 305 185" class="barrow"/>
  <path d="M930 415 C930 300 830 260 830 185" class="barrow"/>
  <text x="590" y="552" text-anchor="middle" class="bsub">逻辑地址连续，物理显存可以不连续；相同前缀通过引用同一批物理 block 实现共享。</text>
</svg>
</div>

<h4 id="Block-管理细节"><a href="#Block-管理细节" class="headerlink" title="Block 管理细节"></a>Block 管理细节</h4><p><strong>Block 结构</strong>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">block_size = <span class="number">16</span>  <span class="comment"># 每个 block 存 16 个 token</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Llama-7B 单个 block 占用</span></span><br><span class="line">per_block_size = (</span><br><span class="line">    block_size *       <span class="comment"># 16 tokens</span></span><br><span class="line">    <span class="number">32</span> *               <span class="comment"># 32 layers</span></span><br><span class="line">    <span class="number">2</span> *                <span class="comment"># K + V</span></span><br><span class="line">    <span class="number">32</span> *               <span class="comment"># 32 heads</span></span><br><span class="line">    <span class="number">128</span> *              <span class="comment"># head_dim</span></span><br><span class="line">    <span class="number">2</span>                  <span class="comment"># fp16 (2 bytes)</span></span><br><span class="line">) = <span class="number">8</span>,<span class="number">388</span>,<span class="number">608</span> <span class="built_in">bytes</span> ≈ <span class="number">8</span> MB</span><br><span class="line"></span><br><span class="line"><span class="comment"># A100-80GB 能存多少 block？</span></span><br><span class="line"><span class="comment"># 假设模型占 14GB，剩余 66GB</span></span><br><span class="line">num_blocks = <span class="number">66</span> * <span class="number">1024</span> / <span class="number">8</span> = <span class="number">8</span>,<span class="number">448</span> blocks</span><br><span class="line">max_tokens = <span class="number">8</span>,<span class="number">448</span> * <span class="number">16</span> = <span class="number">135</span>,<span class="number">168</span> tokens</span><br></pre></td></tr></table></figure>
<hr>
<h4 id="Block-Table-映射"><a href="#Block-Table-映射" class="headerlink" title="Block Table 映射"></a>Block Table 映射</h4><p><strong>CUDA Kernel 中的查找</strong>（简化伪代码）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">paged_attention</span>(<span class="params">Q, block_table, kv_cache</span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    Q: [num_queries, d_k]</span></span><br><span class="line"><span class="string">    block_table: [num_blocks]  逻辑 block ID → 物理 block ID</span></span><br><span class="line"><span class="string">    kv_cache: [num_physical_blocks, block_size, d_k]</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    output = []</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> query_idx <span class="keyword">in</span> <span class="built_in">range</span>(<span class="built_in">len</span>(Q)):</span><br><span class="line">        <span class="comment"># 1. 计算这个 query 对应的逻辑 block 和 offset</span></span><br><span class="line">        logical_block_idx = query_idx // BLOCK_SIZE</span><br><span class="line">        offset = query_idx % BLOCK_SIZE</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 查表：逻辑 block → 物理 block</span></span><br><span class="line">        physical_block_id = block_table[logical_block_idx]</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 3. 从物理 block 读取 K, V</span></span><br><span class="line">        K = kv_cache[physical_block_id][offset]</span><br><span class="line">        V = kv_cache[physical_block_id][offset]</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 4. 计算 Attention</span></span><br><span class="line">        attn_score = dot_product(Q[query_idx], K) / sqrt(d_k)</span><br><span class="line">        output.append(attn_score * V)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> output</span><br></pre></td></tr></table></figure>
<p><strong>实际 CUDA Kernel</strong>（位于 <code>csrc/attention/paged_attention_v2.cu</code>）：</p>
<ul>
<li>使用 shared memory 优化</li>
<li>Vectorized 访存（一次读 128 bits）</li>
<li>Warp-level reduction</li>
</ul>
<hr>
<h3 id="4-3-基于-Block-的高级特性"><a href="#4-3-基于-Block-的高级特性" class="headerlink" title="4.3 基于 Block 的高级特性"></a>4.3 基于 Block 的高级特性</h3><h4 id="4-3-1-Prefix-Caching（共享前缀）"><a href="#4-3-1-Prefix-Caching（共享前缀）" class="headerlink" title="4.3.1 Prefix Caching（共享前缀）"></a>4.3.1 Prefix Caching（共享前缀）</h4><p><strong>场景</strong>：多个请求共享相同的 system prompt</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># System Prompt (100 tokens) → Block [0, 1, 2, 3, 4, 5]</span></span><br><span class="line">system_prompt_blocks = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># Request 1</span></span><br><span class="line">block_table_1 = system_prompt_blocks + [<span class="number">10</span>, <span class="number">11</span>, <span class="number">12</span>]  <span class="comment"># 自己的 blocks</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Request 2</span></span><br><span class="line">block_table_2 = system_prompt_blocks + [<span class="number">20</span>, <span class="number">21</span>]      <span class="comment"># 共享前 6 个</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Request 3</span></span><br><span class="line">block_table_3 = system_prompt_blocks + [<span class="number">30</span>, <span class="number">31</span>, <span class="number">32</span>]  <span class="comment"># 共享前 6 个</span></span><br></pre></td></tr></table></figure>
<p><strong>引用计数管理</strong>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">block_ref_count = &#123;</span><br><span class="line">    <span class="number">0</span>: <span class="number">3</span>,  <span class="comment"># 被 3 个请求共享</span></span><br><span class="line">    <span class="number">1</span>: <span class="number">3</span>,</span><br><span class="line">    <span class="number">2</span>: <span class="number">3</span>,</span><br><span class="line">    <span class="number">3</span>: <span class="number">3</span>,</span><br><span class="line">    <span class="number">4</span>: <span class="number">3</span>,</span><br><span class="line">    <span class="number">5</span>: <span class="number">3</span>,</span><br><span class="line">    <span class="number">10</span>: <span class="number">1</span>,  <span class="comment"># 只属于 Request 1</span></span><br><span class="line">    <span class="number">11</span>: <span class="number">1</span>,</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 释放 Request 1 时</span></span><br><span class="line"><span class="keyword">for</span> block_id <span class="keyword">in</span> request1.block_table:</span><br><span class="line">    block_ref_count[block_id] -= <span class="number">1</span></span><br><span class="line">    <span class="keyword">if</span> block_ref_count[block_id] == <span class="number">0</span>:</span><br><span class="line">        free_block(block_id)  <span class="comment"># 引用计数归零才真正释放</span></span><br></pre></td></tr></table></figure>
<p><strong>Hash 算法</strong>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">compute_block_hash</span>(<span class="params">token_ids</span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    计算一个 block 的 hash，用于查找缓存</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">hash</span>(<span class="built_in">tuple</span>(token_ids))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 缓存查找</span></span><br><span class="line">cached_blocks = &#123;&#125;</span><br><span class="line"><span class="keyword">for</span> block_idx, token_chunk <span class="keyword">in</span> <span class="built_in">enumerate</span>(chunks(request.token_ids, <span class="number">16</span>)):</span><br><span class="line">    block_hash = compute_block_hash(token_chunk)</span><br><span class="line">    <span class="keyword">if</span> block_hash <span class="keyword">in</span> cached_blocks:</span><br><span class="line">        <span class="comment"># 命中缓存，复用已有 block</span></span><br><span class="line">        block_table[block_idx] = cached_blocks[block_hash]</span><br><span class="line">        block_ref_count[cached_blocks[block_hash]] += <span class="number">1</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="comment"># 未命中，分配新 block</span></span><br><span class="line">        new_block = allocate_block()</span><br><span class="line">        block_table[block_idx] = new_block</span><br><span class="line">        cached_blocks[block_hash] = new_block</span><br></pre></td></tr></table></figure>
<p><strong>效果数据</strong>：</p>
<ul>
<li>命中缓存时 TTFT 降低 <strong>5-10 倍</strong></li>
<li>多用户共享 system prompt 场景，显存占用降低 <strong>50-70%</strong></li>
</ul>
<hr>
<h4 id="4-3-2-Copy-on-Write（写时复制）"><a href="#4-3-2-Copy-on-Write（写时复制）" class="headerlink" title="4.3.2 Copy-on-Write（写时复制）"></a>4.3.2 Copy-on-Write（写时复制）</h4><p><strong>场景</strong>：Parallel Sampling（一个 prompt 生成多个答案）</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 初始状态：所有 sample 共享 prompt blocks</span></span><br><span class="line">prompt_blocks = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>]  <span class="comment"># &quot;写一首诗&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># n=3 个 sample</span></span><br><span class="line">sample_1_table = prompt_blocks  <span class="comment"># 共享</span></span><br><span class="line">sample_2_table = prompt_blocks  <span class="comment"># 共享</span></span><br><span class="line">sample_3_table = prompt_blocks  <span class="comment"># 共享</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 第一次生成（decode）</span></span><br><span class="line"><span class="comment"># Sample 1 需要写入新 token，触发 copy-on-write</span></span><br><span class="line">sample_1_table = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">10</span>]  <span class="comment"># 复制了一份</span></span><br><span class="line">sample_2_table = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>]      <span class="comment"># 仍然共享</span></span><br><span class="line">sample_3_table = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>]      <span class="comment"># 仍然共享</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 后续生成</span></span><br><span class="line">sample_1_table = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">10</span>, <span class="number">11</span>, <span class="number">12</span>]  <span class="comment"># 独立</span></span><br><span class="line">sample_2_table = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">20</span>, <span class="number">21</span>, <span class="number">22</span>]  <span class="comment"># 独立</span></span><br><span class="line">sample_3_table = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">30</span>, <span class="number">31</span>, <span class="number">32</span>]  <span class="comment"># 独立</span></span><br></pre></td></tr></table></figure>
<p><strong>效果</strong>：</p>
<ul>
<li><code>n=10</code> 时，显存占用约 <strong>2x</strong>（而不是 10x）</li>
<li>特别适合 Best-of-N 采样</li>
</ul>
<hr>
<h4 id="4-3-3-动态-Block-分配"><a href="#4-3-3-动态-Block-分配" class="headerlink" title="4.3.3 动态 Block 分配"></a>4.3.3 动态 Block 分配</h4><p><strong>传统方式</strong>（必须预分配）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 必须提前决定 max_tokens</span></span><br><span class="line">request.kv_cache = allocate_continuous(max_tokens=<span class="number">2048</span>)</span><br><span class="line"><span class="comment"># 即使只生成 10 个 token，也占 2048 个 token 的显存</span></span><br></pre></td></tr></table></figure>
<p><strong>PagedAttention</strong>（按需分配）：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 初始只分配 prompt 需要的 blocks</span></span><br><span class="line">request.block_table = allocate_blocks(num_prompt_tokens // <span class="number">16</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 每生成 1 个 token，检查是否需要新 block</span></span><br><span class="line"><span class="keyword">if</span> num_generated % <span class="number">16</span> == <span class="number">0</span>:</span><br><span class="line">    new_block = allocate_block()</span><br><span class="line">    request.block_table.append(new_block)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成结束，立即释放</span></span><br><span class="line">free_blocks(request.block_table)</span><br></pre></td></tr></table></figure>
<p><strong>效果</strong>：</p>
<ul>
<li>实际生成长度 &lt;&lt; max_tokens 时，节省大量显存</li>
<li>可以支持更大的 batch size</li>
</ul>
<hr>
<h2 id="5-性能数据对比"><a href="#5-性能数据对比" class="headerlink" title="5. 性能数据对比"></a>5. 性能数据对比</h2><h4 id="官方-Benchmark（A100-80GB，Llama-13B）"><a href="#官方-Benchmark（A100-80GB，Llama-13B）" class="headerlink" title="官方 Benchmark（A100-80GB，Llama-13B）"></a>官方 Benchmark（A100-80GB，Llama-13B）</h4><div class="table-container">
<table>
<thead>
<tr>
<th>指标</th>
<th>HuggingFace</th>
<th>vLLM</th>
<th>提升</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>显存利用率</strong></td>
<td>20.4%</td>
<td>85.3%</td>
<td><strong>4.2x</strong></td>
</tr>
<tr>
<td><strong>吞吐量 (req/s)</strong></td>
<td>5.2</td>
<td>53.7</td>
<td><strong>10.3x</strong></td>
</tr>
<tr>
<td><strong>TTFT (无缓存)</strong></td>
<td>2.1s</td>
<td>1.8s</td>
<td><strong>1.2x</strong></td>
</tr>
<tr>
<td><strong>TTFT (缓存命中)</strong></td>
<td>2.1s</td>
<td>0.3s</td>
<td><strong>7x</strong></td>
</tr>
<tr>
<td><strong>P99 延迟</strong></td>
<td>18.5s</td>
<td>3.2s</td>
<td><strong>5.8x</strong></td>
</tr>
</tbody>
</table>
</div>
<h4 id="不同-Batch-Size-下的吞吐量"><a href="#不同-Batch-Size-下的吞吐量" class="headerlink" title="不同 Batch Size 下的吞吐量"></a>不同 Batch Size 下的吞吐量</h4><div class="table-container">
<table>
<thead>
<tr>
<th>Batch Size</th>
<th>HF (tok/s)</th>
<th>vLLM (tok/s)</th>
<th>加速比</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>42</td>
<td>48</td>
<td>1.1x</td>
</tr>
<tr>
<td>8</td>
<td>280</td>
<td>1,120</td>
<td>4.0x</td>
</tr>
<tr>
<td>32</td>
<td>1,050</td>
<td>4,380</td>
<td>4.2x</td>
</tr>
<tr>
<td>128</td>
<td>2,100</td>
<td>16,800</td>
<td>8.0x</td>
</tr>
</tbody>
</table>
</div>
<p><strong>结论</strong>：Batch Size 越大，vLLM 优势越明显。</p>
<hr>
<h3 id="5-1-Block-Size-的权衡"><a href="#5-1-Block-Size-的权衡" class="headerlink" title="5.1 Block Size 的权衡"></a>5.1 Block Size 的权衡</h3><div class="table-container">
<table>
<thead>
<tr>
<th>Block Size</th>
<th>优点</th>
<th>缺点</th>
<th>适用场景</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>8 tokens</strong></td>
<td>碎片极少（&lt;5%）</td>
<td>Block table 开销大</td>
<td>短文本、精确控制</td>
</tr>
<tr>
<td><strong>16 tokens</strong></td>
<td>平衡点</td>
<td>-</td>
<td><strong>默认推荐</strong></td>
</tr>
<tr>
<td><strong>32 tokens</strong></td>
<td>Block table 小</td>
<td>碎片多（平均 16 tokens 浪费）</td>
<td>长文本</td>
</tr>
</tbody>
</table>
</div>
<p><strong>vLLM 默认</strong>：16 tokens（经验最优值）</p>
<p><strong>自定义</strong>：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">llm = LLM(</span><br><span class="line">    model=<span class="string">&quot;llama-7b&quot;</span>,</span><br><span class="line">    block_size=<span class="number">32</span>,  <span class="comment"># 可调整</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<h3 id="5-2-自研加速卡上的测试"><a href="#5-2-自研加速卡上的测试" class="headerlink" title="5.2 自研加速卡上的测试"></a>5.2 自研加速卡上的测试</h3><p>不同 <code>max_num_seqs</code> 的影响：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">configs = [<span class="number">1</span>, <span class="number">8</span>, <span class="number">32</span>, <span class="number">128</span>, <span class="number">256</span>]</span><br><span class="line"><span class="keyword">for</span> max_seqs <span class="keyword">in</span> configs:</span><br><span class="line">    llm = LLM(model=<span class="string">&quot;...&quot;</span>, max_num_seqs=max_seqs)</span><br></pre></td></tr></table></figure>
<p>Latency 结果：</p>
<p><img src="/images/vllm-local-latency.png" alt="不同 max_num_seqs 下的 Latency 结果"></p>
<p>Throughput 结果：</p>
<p><img src="/images/vllm-local-throughput.png" alt="不同 max_num_seqs 下的 Throughput 结果"></p>
<hr>
<h2 id="6-总结与下期预告"><a href="#6-总结与下期预告" class="headerlink" title="6. 总结与下期预告"></a>6. 总结与下期预告</h2><h3 id="6-1-核心要点回顾"><a href="#6-1-核心要点回顾" class="headerlink" title="6.1 核心要点回顾"></a>6.1 核心要点回顾</h3><p>本文从一个 API 请求出发，完整追踪了 vLLM v1 的执行流程：</p>
<h4 id="六层架构"><a href="#六层架构" class="headerlink" title="六层架构"></a>六层架构</h4><ol>
<li><strong>API Layer</strong>：HTTP 入口，兼容 OpenAI API</li>
<li><strong>LLMEngine</strong>：前后处理（tokenize/detokenize）</li>
<li><strong>EngineCore</strong>：调度+执行核心</li>
<li><strong>Scheduler</strong>：请求调度器（三队列管理）</li>
<li><strong>KVCacheManager</strong>：PagedAttention 显存管理</li>
<li><strong>ModelExecutor</strong>：模型推理（Worker/ModelRunner）</li>
</ol>
<h4 id="完整生命周期"><a href="#完整生命周期" class="headerlink" title="完整生命周期"></a>完整生命周期</h4><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">用户请求</span><br><span class="line">  ↓ <span class="number">3.1</span> 接收（tokenize）</span><br><span class="line">add_request → InputProcessor → EngineCore</span><br><span class="line">  ↓ <span class="number">3.2</span> 调度（Scheduler.schedule）</span><br><span class="line">选择 running / waiting → 分配 KV blocks → SchedulerOutput</span><br><span class="line">  ↓ <span class="number">3.3</span> 执行（ModelExecutor）</span><br><span class="line"><span class="variable">_update_states</span> → <span class="variable">_prepare_inputs</span> → forward → <span class="variable">_sample</span></span><br><span class="line">  ↓ <span class="number">3.4</span> 输出（OutputProcessor）</span><br><span class="line">update_from_output → detokenize → RequestOutput</span><br><span class="line">返回用户</span><br></pre></td></tr></table></figure>
<h4 id="PagedAttention-的四大特性"><a href="#PagedAttention-的四大特性" class="headerlink" title="PagedAttention 的四大特性"></a>PagedAttention 的四大特性</h4><ol>
<li><strong>Block 管理</strong>：像虚拟内存一样管理 KV Cache</li>
<li><strong>Prefix Caching</strong>：跨请求共享前缀</li>
<li><strong>Copy-on-Write</strong>：并行采样节省显存</li>
<li><strong>动态分配</strong>：按需分配，用完释放</li>
</ol>
<hr>
<h3 id="6-2-与传统方案对比"><a href="#6-2-与传统方案对比" class="headerlink" title="6.2 与传统方案对比"></a>6.2 与传统方案对比</h3><div class="table-container">
<table>
<thead>
<tr>
<th>维度</th>
<th>HuggingFace Transformers</th>
<th>vLLM</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>批处理</strong></td>
<td>Static Batching</td>
<td>Continuous Batching</td>
</tr>
<tr>
<td><strong>KV Cache</strong></td>
<td>连续分配，无法共享</td>
<td>PagedAttention，block 共享</td>
</tr>
<tr>
<td><strong>长 prompt</strong></td>
<td>阻塞整个 batch</td>
<td>Chunked Prefill，切片处理</td>
</tr>
<tr>
<td><strong>抢占</strong></td>
<td>不支持</td>
<td>支持 recompute/swap</td>
</tr>
<tr>
<td><strong>显存利用率</strong></td>
<td>20-30%</td>
<td>80-90%</td>
</tr>
<tr>
<td><strong>吞吐量</strong></td>
<td>基准</td>
<td><strong>10-24x</strong></td>
</tr>
<tr>
<td><strong>代码复杂度</strong></td>
<td>简单</td>
<td>复杂</td>
</tr>
</tbody>
</table>
</div>
<hr>
<h3 id="6-3-适用场景"><a href="#6-3-适用场景" class="headerlink" title="6.3 适用场景"></a>6.3 适用场景</h3><p><strong>vLLM 最适合</strong>：</p>
<ul>
<li>✅ 高并发服务（如 ChatBot API）</li>
<li>✅ 批量推理任务（如文档批量摘要）</li>
<li>✅ 多轮对话（Prefix Caching 收益大）</li>
<li>✅ 长上下文场景（Chunked Prefill 关键）</li>
<li>✅ Best-of-N 采样（Copy-on-Write 优化）</li>
</ul>
<p><strong>不适合</strong>：</p>
<ul>
<li>❌ 单个请求的绝对最低延迟（vLLM 有调度开销）</li>
<li>❌ 模型训练（vLLM 只做推理）</li>
<li>❌ 需要修改模型结构的场景（vLLM 对模型有约束）</li>
</ul>
<hr>
<h3 id="6-4-下一篇预告"><a href="#6-4-下一篇预告" class="headerlink" title="6.4 下一篇预告"></a>6.4 下一篇预告</h3><p><strong>《vLLM 架构源码解析（二）：PagedAttention CUDA Kernel 深度剖析》</strong></p>
<p>将深入以下内容：</p>
<ol>
<li><p><strong>FlashAttention 原理</strong></p>
<ul>
<li>Online Softmax 算法</li>
<li>Tile 机制与内存访问优化</li>
<li>HBM vs SRAM 的取舍</li>
</ul>
</li>
<li><p><strong>PagedAttention v2 Kernel 源码</strong></p>
<ul>
<li>逐行解读 <code>csrc/attention/paged_attention_v2.cu</code></li>
<li>Thread Block 的任务分配</li>
<li>Shared Memory 的使用技巧</li>
</ul>
</li>
<li><p><strong>Prefix Caching 实现细节</strong></p>
<ul>
<li>Hash 算法选择</li>
<li>引用计数的原子操作</li>
<li>LRU 淘汰策略</li>
</ul>
</li>
<li><p><strong>性能 Profiling</strong></p>
<ul>
<li>用 Nsight Compute 分析 kernel 性能</li>
<li>找出瓶颈并优化</li>
</ul>
</li>
<li><p><strong>实战案例</strong></p>
<ul>
<li>长上下文场景（32K tokens）优化</li>
<li>多用户场景（100+ 并发）调优</li>
</ul>
</li>
</ol>
<hr>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><h3 id="官方资源"><a href="#官方资源" class="headerlink" title="官方资源"></a>官方资源</h3><ol>
<li><strong>vLLM 论文</strong>：<a href="https://arxiv.org/abs/2309.06180">Efficient Memory Management for Large Language Model Serving with PagedAttention</a> (SOSP 2023)</li>
<li><strong>vLLM GitHub</strong>：<a href="https://github.com/vllm-project/vllm">https://github.com/vllm-project/vllm</a></li>
<li><strong>vLLM 官方文档</strong>：<a href="https://docs.vllm.ai/">https://docs.vllm.ai/</a></li>
<li><strong>FlashAttention 论文</strong>：<a href="https://arxiv.org/abs/2205.14135">https://arxiv.org/abs/2205.14135</a></li>
<li><strong>FlashAttention-2 论文</strong>：<a href="https://arxiv.org/abs/2307.08691">https://arxiv.org/abs/2307.08691</a></li>
</ol>
<h3 id="源码路径速查"><a href="#源码路径速查" class="headerlink" title="源码路径速查"></a>源码路径速查</h3><div class="table-container">
<table>
<thead>
<tr>
<th>组件</th>
<th>路径</th>
</tr>
</thead>
<tbody>
<tr>
<td>LLMEngine</td>
<td><code>vllm/v1/engine/llm_engine.py</code></td>
</tr>
<tr>
<td>EngineCore</td>
<td><code>vllm/v1/engine/core.py</code></td>
</tr>
<tr>
<td>InputProcessor</td>
<td><code>vllm/v1/engine/processor.py</code></td>
</tr>
<tr>
<td>OutputProcessor</td>
<td><code>vllm/v1/engine/output_processor.py</code></td>
</tr>
<tr>
<td>Scheduler</td>
<td><code>vllm/v1/core/sched/scheduler.py</code></td>
</tr>
<tr>
<td>KVCacheManager</td>
<td><code>vllm/v1/core/kv_cache_manager.py</code></td>
</tr>
<tr>
<td>Worker</td>
<td><code>vllm/v1/worker/gpu_worker.py</code></td>
</tr>
<tr>
<td>ModelRunner</td>
<td><code>vllm/v1/worker/gpu_model_runner.py</code></td>
</tr>
<tr>
<td>Sampler</td>
<td><code>vllm/model_executor/layers/sampler.py</code></td>
</tr>
<tr>
<td>PagedAttention Kernel</td>
<td><code>csrc/attention/paged_attention_v2.cu</code></td>
</tr>
</tbody>
</table>
</div>
<h2 id="附录：快速上手代码"><a href="#附录：快速上手代码" class="headerlink" title="附录：快速上手代码"></a>附录：快速上手代码</h2><h3 id="A-最简单的使用"><a href="#A-最简单的使用" class="headerlink" title="A. 最简单的使用"></a>A. 最简单的使用</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> vllm <span class="keyword">import</span> LLM, SamplingParams</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建 LLM 实例</span></span><br><span class="line">llm = LLM(model=<span class="string">&quot;meta-llama/Llama-2-7b-hf&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 采样参数</span></span><br><span class="line">sampling_params = SamplingParams(</span><br><span class="line">    temperature=<span class="number">0.8</span>,</span><br><span class="line">    top_p=<span class="number">0.95</span>,</span><br><span class="line">    max_tokens=<span class="number">128</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 批量推理</span></span><br><span class="line">prompts = [</span><br><span class="line">    <span class="string">&quot;Hello, my name is&quot;</span>,</span><br><span class="line">    <span class="string">&quot;The president of the US is&quot;</span>,</span><br><span class="line">    <span class="string">&quot;The capital of France is&quot;</span>,</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line">outputs = llm.generate(prompts, sampling_params)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> output <span class="keyword">in</span> outputs:</span><br><span class="line">    prompt = output.prompt</span><br><span class="line">    generated_text = output.outputs[<span class="number">0</span>].text</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;Prompt: <span class="subst">&#123;prompt!r&#125;</span>, Generated: <span class="subst">&#123;generated_text!r&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>
<h3 id="B-启用-Prefix-Caching"><a href="#B-启用-Prefix-Caching" class="headerlink" title="B. 启用 Prefix Caching"></a>B. 启用 Prefix Caching</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">llm = LLM(</span><br><span class="line">    model=<span class="string">&quot;meta-llama/Llama-2-7b-hf&quot;</span>,</span><br><span class="line">    enable_prefix_caching=<span class="literal">True</span>,  <span class="comment"># 开启前缀缓存</span></span><br><span class="line">    block_size=<span class="number">16</span>,               <span class="comment"># block 大小</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 相同 system prompt 的多个查询会自动共享</span></span><br><span class="line">system_prompt = <span class="string">&quot;You are a helpful assistant.&quot;</span></span><br><span class="line">queries = [</span><br><span class="line">    <span class="string">f&quot;<span class="subst">&#123;system_prompt&#125;</span>\nUser: What is Python?&quot;</span>,</span><br><span class="line">    <span class="string">f&quot;<span class="subst">&#123;system_prompt&#125;</span>\nUser: What is AI?&quot;</span>,</span><br><span class="line">    <span class="string">f&quot;<span class="subst">&#123;system_prompt&#125;</span>\nUser: What is ML?&quot;</span>,</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第一个请求会缓存 system prompt</span></span><br><span class="line"><span class="comment"># 后续请求命中缓存，TTFT 大幅降低</span></span><br><span class="line">outputs = llm.generate(queries, sampling_params)</span><br></pre></td></tr></table></figure>
<h3 id="C-OpenAI-兼容-API-Server"><a href="#C-OpenAI-兼容-API-Server" class="headerlink" title="C. OpenAI 兼容 API Server"></a>C. OpenAI 兼容 API Server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启动服务</span></span><br><span class="line">python -m vllm.entrypoints.openai.api_server \</span><br><span class="line">    --model meta-llama/Llama-2-7b-hf \</span><br><span class="line">    --host 0.0.0.0 \</span><br><span class="line">    --port 8000 \</span><br><span class="line">    --enable-prefix-caching \</span><br><span class="line">    --max-num-batched-tokens 8192</span><br><span class="line"></span><br><span class="line"><span class="comment"># 调用</span></span><br><span class="line">curl http://localhost:8000/v1/chat/completions \</span><br><span class="line">    -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">    -d <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">        &quot;model&quot;: &quot;llama-2-7b&quot;,</span></span><br><span class="line"><span class="string">        &quot;messages&quot;: [&#123;&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: &quot;Hello!&quot;&#125;],</span></span><br><span class="line"><span class="string">        &quot;max_tokens&quot;: 128</span></span><br><span class="line"><span class="string">    &#125;&#x27;</span></span><br></pre></td></tr></table></figure>
<h3 id="D-观察-Prefix-Cache-命中率"><a href="#D-观察-Prefix-Cache-命中率" class="headerlink" title="D. 观察 Prefix Cache 命中率"></a>D. 观察 Prefix Cache 命中率</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启用日志</span></span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line">os.environ[<span class="string">&quot;VLLM_LOGGING_LEVEL&quot;</span>] = <span class="string">&quot;INFO&quot;</span></span><br><span class="line"></span><br><span class="line">llm = LLM(</span><br><span class="line">    model=<span class="string">&quot;llama-7b&quot;</span>,</span><br><span class="line">    enable_prefix_caching=<span class="literal">True</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 运行推理</span></span><br><span class="line">outputs = llm.generate(prompts, sampling_params)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 从日志中查看：</span></span><br><span class="line"><span class="comment"># INFO 2026-07-06 [scheduler.py] Prefix cache hit rate: 78.5%</span></span><br></pre></td></tr></table></figure>
<hr>
<p><strong>下一篇预告</strong>：《vLLM 架构源码解析（二）：PagedAttention CUDA Kernel 深度剖析》</p>
<hr>
<p><em>文章字数：约 8000 字 | 预计阅读时间：25 分钟</em><br><em>源码版本：vLLM v0.6.x</em></p>
]]></content>
      <categories>
        <category>AI Infra</category>
        <category>vLLM</category>
      </categories>
      <tags>
        <tag>vLLM</tag>
        <tag>LLM Serving</tag>
        <tag>Inference</tag>
        <tag>Scheduler</tag>
        <tag>Engine</tag>
        <tag>Token Generation</tag>
      </tags>
  </entry>
  <entry>
    <title>How to think about psychology 11-12</title>
    <url>/2021/06/02/How-to-think-about-psychology-11-12/</url>
    <content><![CDATA[<h1 id="这才是心理学（11-12）How-to-think-about-psychology"><a href="#这才是心理学（11-12）How-to-think-about-psychology" class="headerlink" title="这才是心理学（11-12）How to think about psychology"></a>这才是心理学（11-12）How to think about psychology</h1><p>&emsp;接近一周的对于这本心理学入门的书籍进行的笔记也结束了，后续会针对整本书进行一个总结和回顾吧。</p>
<h2 id="第十一章-偶然性"><a href="#第十一章-偶然性" class="headerlink" title="第十一章 偶然性"></a>第十一章 偶然性</h2><p>&emsp;本章继续第10章介绍的概率，单独将概率中的偶然性拿出来进行讨论。</p>
<p>&emsp;首先，作者介绍了人们为什么对偶然性执迷不悟，同时指出偶然性的危害。</p>
<p>&emsp;人类大脑的进化方式，使得人类不懈地寻求世界中的各种模式，这也是人类智力的特征，促使了人类在信息处理和知识获取方面取得了辉煌的成就。但是，这种对于偶然性解释的进化，有时也会产生不好的效果。如一些金融分析师试图对每一次股市价格的微小波动做出解释，但是这种波动大部分情况下都是随机的，他们的执着于解释这些是为了让人们相信他们可以打败市场。同时即使有些人真的预测到一些变化，这种预测也是不可靠的。简单来说，假设将预测分为下跌和上涨两种情况，有1000人对第一次变化进行了预测，那么就会有500人可以对这次变化成功预测，这500人接着对第二次变化进行了预测，那么就会有250人对第二次变化成功预测，直到进行到第四次预测，那么仍然会有60人左右连着预测正确四次。因此，从概率上来说，总会有人在接下来的预测取得了成功，但是这些是不可靠的。</p>
<span id="more"></span>
<p>&emsp;同时，作者指出偶然性和随机性并不是在原则上具有不确定性，而是当下无法确定。例如，在生物学足够发达的情况下，我们或许能够解释为什么有些吸烟者不会患有肺癌，但是当下这种可变性必须归因于大量的偶然因素。</p>
<p>&emsp;人们有解释偶然事件的倾向，这一现象在心理学的研究中被称为相关错觉。许多控制研究都表明，当人么预设两个变量有关联时，即使在两个变量毫无关系的数据中，他们也会发现联系。当然，认为生活中每一件偶然的小事都需要精细的解释，这种想法是错误的。</p>
<p>&emsp;接着，作者解释了为什么人们误解了生活中的巧合。在事件中寻求模式和意义的倾向，加上巧合的“不可思议”特性，让许多人会忘记用偶然性来解释巧合，反而为理解这些事情寻求复杂的理论。其实，概率定律确保了随着事件发生的次数的增加，一些罕见匹配出现的概率会变得非常高，这一定律不仅允许罕见匹配的出现，而且从长远来看几乎保证了它的出现。例如，假设一个人每天都会参与100件不同的事（看电视、打电话等都可以拆分为几个单独的事件，因此可能不止100），那么所有事件的组合有4950种，一年365天，会得到18 067 500个匹配，所以在一年中一个罕见匹配发生的概率为0.00000033，因此一年出现多个罕见匹配并不奇怪。人们对于个人巧合不愿意去归因于偶然，可能具有一些动机性和情感性，但是实际上巧合发生的概率并不小。</p>
<p>&emsp;最后，作者指出要接受错误以减少错误。拒绝承认偶然因素的作用，会降低我们对现实世界的预测能力。承认预测会出现错误，有助于提高整体预测的准确性。如，假设两盏灯蓝和红分别以70%和30%的概率出现，那么在已知蓝灯亮的概率比红灯大时，随机预测准确的概率为58%（70 <em> 0.7 + 30 </em> 0.3），小于每次都预测蓝灯亮的概率70%。同时，有些人也会提出群体统计不适用于单个个体或单个事件，但是很多研究已经证明，即使临床医生可以获得比统计方法更多的信息，但是统计预测仍然比临床医生的预测更加准确！因此，在试图为每一个不同寻常的案例寻求独特的解释时，往往会使我们失去了对更常见的个案的预测。对于心理学来说，预测应该是概率性的，是对总体趋势的预测。</p>
<h2 id="第十二章-不招人待见的心理学"><a href="#第十二章-不招人待见的心理学" class="headerlink" title="第十二章 不招人待见的心理学"></a>第十二章 不招人待见的心理学</h2><p>&emsp;本章作者主要说明了心理学为大众所怀疑的种种原因。</p>
<p>&emsp;心理学的形象问题。在书店中的心理学读物可以分为三种：1. 少数几本心理学早期经典著作，但是这些多半侧重老式的精神分析观点，完全不能代表当代心理学。2. 伪装成心理学的伪科学书籍，如心灵感应、意念移物等，但是在心理学领域，超感官感知的研究常常是不可靠也不能重复的。3. 一些自助类读物如励志类、人类行为的老生常谈以及没有科学依据的“新疗法”。然而许多经过严格的心理学检验被证明有效的认知和行为疗法，很少出现在书架上。互联网上的情况也是类似。</p>
<p>&emsp;作者认为许多跨学科的研究，涉及到心理学家的贡献时，往往会被忽略。</p>
<p>&emsp;最后作者指出自己是最坏的敌人。如罗宾 道斯和斯科特 利连菲尔德所反对的，心理学领域基于心理学的科学性来颁发资格认证，然后又用资格认证来保护心理学从业者的不科学行为。同时作者也指出，心理学正在逐渐成为的单一的意识形态文化。每个人都有一套关于人类行为的理论，凡是千万不要把个人的心理学理论与科学心理学混为一谈。</p>
<p>&emsp;作者在本章花了很多篇幅用于作为心理学家的自我反省，或者说对整个心理学界进行反省，这也告诫我们，不要认为科学家的行为也是科学的，这些往往也是个人偏见所作出的一些行为。同时告诫读者，要警惕伪科学和伪心理学。</p>
]]></content>
      <categories>
        <category>Psychology</category>
        <category>How to think about psychology</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>心理学</tag>
        <tag>这才是心理学</tag>
      </tags>
  </entry>
  <entry>
    <title>How to think about psychology 1-2</title>
    <url>/2021/05/27/How-to-think-about-psychology-1-2/</url>
    <content><![CDATA[<h1 id="这才是心理学（1-2）How-to-think-about-psychology"><a href="#这才是心理学（1-2）How-to-think-about-psychology" class="headerlink" title="这才是心理学（1-2）How to think about psychology"></a>这才是心理学（1-2）How to think about psychology</h1><h2 id="第一章-心理学介绍"><a href="#第一章-心理学介绍" class="headerlink" title="第一章 心理学介绍"></a>第一章 心理学介绍</h2><p>&emsp;心理学作为一门科学，研究的是人类以及动物的行为，并且依据研究对行为做出预测。作者认为心理学和其他科学一样，是基于数据的科学的研究，但是心理学相对于其他科学如物理学、化学等，是一门较为“年轻”的学科。</p>
<p>&emsp;人们对于西格蒙德·佛洛伊德精神分析的“心理学理论”印象深刻，但是正如发展心理学家艾莉森·戈伯尼克所说佛罗伊徳理论是一种僵尸理论，它彻底误导了大众对于心理学徳印象。因为，其理论并不是建立在科学的研究之上得出的结论，而是依附于佛洛依德的个案研究以及自省。</p>
<span id="more"></span>
<p>&emsp;作者在第一章为心理学辟谣，并且简要介绍了心理学的多样性以及统一性。同时指出，定义科学的三个相互关联的特征：</p>
<p>​    &emsp;&emsp;（1）系统实证主义的运用。即科学家通过检验来认识世界。</p>
<p>​    &emsp;&emsp;（2）公共知识的产生。即可重复性和同行评审（peer review）。</p>
<p>​    &emsp;&emsp;（3）对可解决问题的细查。即处理的是可解决的、可具体指明的问题。</p>
<p>&emsp;心理学作为一门科学，也必须满足上述三个特征。</p>
<p>&emsp;世俗智慧（常识）作为对行为的内隐“解释”是难以驳倒的，因为不管发生什么事都可以有一些包含世俗智慧的谚语来解释。同时无法反驳也造成了这些理论的无用性。即使有一些世俗信念可以进行实证检验，但是通过心理学研究可以发现许多常见的关于行为的文化信念都是错误的。虽然有一些世俗信念被证明是错误的，但是新的传言层出不穷。如果人们意识到这种世俗信念的易错性，那么民众心理学的问题就不会那么具有危害性。但是，超过80%的公众认为日常生活提供了足够的心理学训练，但是这恰恰相反。心理学作为世俗智慧的验证者，往往会与根深蒂固的文化信念相冲突，从而不被接受。</p>
<h2 id="第二章-可证伪性"><a href="#第二章-可证伪性" class="headerlink" title="第二章 可证伪性"></a>第二章 可证伪性</h2><p>&emsp;作者在第一章强调了心理学的科学性同时提醒读者要警惕一些世俗智慧的危害，保持批判的思维。</p>
<p>&emsp;作者在第二章强调了心理学的可证伪性。作者认为理论不是用来解释结果的，而是用来预测结果。结果是对理论的证明。这也是科学中证伪性的重要性所在，证伪性是知预测的结果可以用来证明理论的错误，越清晰的预测结果越能证明理论的正确性，预测的数量多并不能表示理论有多正确。</p>
<p>&emsp;作者同时指出，科学就是在不断证伪的过程中向前发展的。如果理论在实验中被证实，那么理论就得到了某种程度上的确认，如果被证伪，就必须对理论做出改变或者提出一个新的更加逼近真理的理论来取代。</p>
<p>&emsp;如果一个理论不可证伪，那么它对自然界中的实际时间就没有任何意义即使无用的。心理学一直被不可证伪的理论所困扰，也是其发展缓慢的原因之一。</p>
<h2 id="较有感触的句子"><a href="#较有感触的句子" class="headerlink" title="较有感触的句子"></a>较有感触的句子</h2><p>“现代思潮中一个危险的趋势是，应避免让普罗大众知道世界的本质：一种无知的面纱是必要的，以保护没有能力应对那些真相的公众。”</p>
<p>&emsp;同作者一样，反对这一观点。人们有权利了解世界的本质，相信与否取决于个人。人们是否需要规避让自己感到不适的发现应由个人所决定，而不是其他组织。</p>
]]></content>
      <categories>
        <category>Psychology</category>
        <category>How to think about psychology</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>心理学</tag>
        <tag>这才是心理学</tag>
      </tags>
  </entry>
  <entry>
    <title>How to think about psychology-3-4</title>
    <url>/2021/05/28/How-to-think-about-psychology-3-4/</url>
    <content><![CDATA[<h1 id="这才是心理学（2-3）How-to-think-about-psychology"><a href="#这才是心理学（2-3）How-to-think-about-psychology" class="headerlink" title="这才是心理学（2-3）How to think about psychology"></a>这才是心理学（2-3）How to think about psychology</h1><h2 id="第三章-操作主义与本质主义"><a href="#第三章-操作主义与本质主义" class="headerlink" title="第三章 操作主义与本质主义"></a>第三章 操作主义与本质主义</h2><p>&emsp;操作主义是指科学理论的概念必须以某种方式建立在可观察事件的基础之上，或与之相关联，而这种可观察时间是可以被测量的。</p>
<p>&emsp;本质主义认为只有从内在本质或者本质的属性对现象做出终极的解释的理论，才是好的科学理论。</p>
<p>&emsp;科学家为操作主义者而不是本质主义者，因为他们并不尝试去回答任何关于“本质”的问题，同时这也不是科学本来面目，科学的独特优势提供一种消除错误的方法，这些错误是我们知识库中的一部分。如果将饥饿定义为“胃里的啃咬感”，这不是操作性的定义，因为其不可测量，而将其定义为可测量的食物消化时间或者一些血糖等生理指标，就是一种操作性的定义。科学中的概念是由一组操作定义的，这也促使我们更加全面地思考问题。我们需要培养对操作性定义的细节进行深究的习惯。</p>
<span id="more"></span>
<p>&emsp;一个概念的操作性必须满足信度以及效度才会有用。信度指的是测量工具的一致性，即多次测量结果相差较小。效度指的是测量工具是否测量了它本应测量的内容，例如用鞋码测试智力，虽然鞋码的测量具有信度，但是这明显是一个荒谬的无效的测试。</p>
<p>&emsp;作者指出尽管大部分概念是间接的操作性定义，如A行为模式（与冠心病的发病率有关）通过一系列二级概念来定义，包括强烈的竞争欲望、潜在的敌意、时间紧迫感等。但是所有概念都在一定程度上通过与操作性相关联的。同时概念的定义并不是固定的，而是不断演变的。</p>
<p>&emsp;心理学术语必须有直接或间接的操作性定义。但是由于人们的“预设偏见问题”，往往忽视了操作主义的必要性。比如，在物理学中讨论磁场时，就必须有相应的方法来测量，但是在心理学中，人们提到“智力”时，往往都有自己的一份评判标准，但是对于心理学家来说，“智力”的定义往往与大众内心的评判标准不一样。由于心理学中存在许多与日常用于相同的词汇，当大众提及这些词汇时会由于预设偏见对心理学上的这种词会产生误会。</p>
<p>&emsp;同时由于人们常常要求心理学回答本质问题，如人们并不会要求科学家解释“地心引力”的本质，然而人们常常将地心引力替换为“智力”来询问心理学家，由于心理学也是一门科学，这些本质问题往往是无法回答的。</p>
<h2 id="第四章-见证与个案研究"><a href="#第四章-见证与个案研究" class="headerlink" title="第四章 见证与个案研究"></a>第四章 见证与个案研究</h2><p>&emsp;个案和见证在科学研究的最初阶段是有效的，通过其寻找一些有趣现象和关键变量是进一步研究的基础。但是，其在科学的研究后期是毫无用处的。佛洛依德工作的局限性在于其仅仅通过个案和见证提出了一些假设，但是在后期并没有摆脱个案和见证的影响，利用科学的方式对假设进行检验。</p>
<p>&emsp;安慰剂效应是见证叙述对研究毫无价值的证明之一。安慰剂效应指的是无论一种治疗方法是否有真正的治疗成分，人们都倾向于报告治疗对他们有帮助。所有类型的心理治疗都涉及安慰剂效应。人们很容易相信见证叙述，但是这对于科学的研究来说毫无用处。“鲜活性”问题也是见证叙述无法作为研究的证据。“鲜活性”问题指出没有什么比真诚的个人见证更加打动人，如报纸包导飞机坠毁事故，人们对于飞机坠毁感到可怕，但是每年死于车祸的人数比死于飞机事故的人数多得多。人们并没有感受到车祸的可怕实由于车祸分布在世界各地，媒体并不能形象地为我们展现这些车祸死者，因为他们分布在全国各地。事件的鲜活性也会对科学证据本身产生影响，如人们倾向于认为科学实验的结果包括对结果进行总结的图像往往比描述相同结果的表更加可信。</p>
<p>​    鲜活的见证如此有说服力的一个重要原因在于，人类是所谓的“认知吝啬者”，即人类在处理一个问题时倾向于选择最不费力的心理加工过程。好消息是，使用科学和统计思维来看待问题可以通过训练来达到自动化程度。</p>
<p>&emsp;“巴纳姆效应是”是使用鲜活性来反驳鲜活性的有效例子之一。例如很多人对于占卜、算命等，往往会给出一种泛化的个性化总结，而人们往往认为这种总结是针对自己的准确而独特的描述。但是，对于每个人解读都一样的“个性化解读”，很好地证明了人们的见证证据是没有价值的。</p>
<p>&emsp;见证为伪科学打开了便利之门，但是理论主张是否合理，是不能用见证叙述和个案研究的证据来判定，人们对于伪科学需要提防其危害。</p>
]]></content>
      <categories>
        <category>Psychology</category>
        <category>How to think about psychology</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>心理学</tag>
        <tag>这才是心理学</tag>
      </tags>
  </entry>
  <entry>
    <title>How to think about psychology 5-7</title>
    <url>/2021/05/31/How-to-think-about-psychology-5-7/</url>
    <content><![CDATA[<h1 id="这才是心理学（5-7）How-to-think-about-psychology"><a href="#这才是心理学（5-7）How-to-think-about-psychology" class="headerlink" title="这才是心理学（5-7）How to think about psychology"></a>这才是心理学（5-7）How to think about psychology</h1><h2 id="第五章-相关性与因果关系问题"><a href="#第五章-相关性与因果关系问题" class="headerlink" title="第五章 相关性与因果关系问题"></a>第五章 相关性与因果关系问题</h2><p>&emsp;本章主要向读者解释两个变量具有相关性并不表示这两个变量存在因果关系。</p>
<p>&emsp;第三变量的存在会使两个变量之间产生误导性关联，这种第三变量有时候并不是很容易分辨。在“使用“烤箱法”避孕”这个案列中，可以很清楚的意识到由于经济水平的不同，导致了使用的避孕方法/工具的不同，从而影响到避孕效果，然而经济水平较高的家庭中往往会有一些高级家具如烤箱等，因此烤箱和避孕在调查中虽然显示了一定的联系，但是第三变量的存在（经济水平）才是正确的因果关系。当一些因果关系在常识看来是显而易见时，或者当我们带着强烈的预设偏见或者我们的理论取向支配了我们对现象的解释时，相关性很容易被视为因果关系的证据。</p>
<span id="more"></span>
<p>&emsp;当第三变量被排除之后，还有必要确定因果关系的方向，即方向性问题。如在新赫布里底群岛上生活的岛民认为，虱子使人健康，因为健康的人相较于非健康的人身上有更多的虱子。但是实际原因是，不健康的人会更快地发烧，从而杀死虱子。因此是不健康导致了虱子较少，而不是虱子少导致了不健康。</p>
<p>&emsp;选择偏差同样是导致许多虚假相关产生的原因。“自我选择偏差”指的是人们自己选择进入一个特定地群体。自我选择会导致个人变量和环境特征之间的虚假相关——这种相关并不表示因果关系。如在美国空气质量较好的亚利桑那州，由于呼吸系统疾病导致的死亡率高于全国的平均水平，这并不能说明亚利桑那的空气质量导致了高死亡率，恰恰相反，由于自我选择偏差，得了呼吸系统疾病的人往往会选择搬家至亚利桑那州，从而导致了死亡率的升高。当选择效应出现时，匆忙下结论会导致我们在现实世界中做出错误的选择。</p>
<p>&emsp;本章从第三变量、方向性问题以及选择偏差解释了相关性并不代表因果性。</p>
<h2 id="第六章-变量控制"><a href="#第六章-变量控制" class="headerlink" title="第六章 变量控制"></a>第六章 变量控制</h2><p>&emsp;本章主要介绍控制变量的重要性。</p>
<p>&emsp;科学家通过比较在不同条件下得到的结果，可以排除一些解释，并证实另一些解释。实验设计的基本目标是分离变量。科学家通过两种方法尽可能地排除不正确的理论：1.直接对实验情景进行控制。2.在可以比较各种可能解释地自然情景下进行观察。</p>
<p>&emsp;在19世纪50年代伦敦反复爆发的霍乱中，很多医生认为霍乱通过气体传播，称为“秽气理论”，但是约翰 斯诺认为霍乱通过病人排泄物污染地供水系统传播。为了证明这一点，斯诺并没有直接选择不同供水系统的居民区来进行实验，因为居民的经济水平会影响到选择的供水系统，而经济水平也会通过饮食、居住来影响到健康。斯诺选择的是在存在两种供水系统的同一片居民区中进行调查，统一居民区中人们的经济水平往往是相近的，因此这控制了经济水平这个变量。</p>
<p>&emsp;在斯诺的例子中，其使用第二种方式来排除错误的结论。在最佳的情况下，是科学家能够操纵感兴趣的变量，并且可以对其他可能影响实验的无关变量进行控制。但是斯诺并不能去改变排水系统，幸运的是他找到了一种可以控制经济变量等其它因素的情况，不过这种情况很少见。</p>
<p>&emsp;虽然斯诺的实验进行了有效的控制，但是在实验中直接操纵变量能产生更有力的推论。在斯诺的实验中，是由被试者决定自己属于哪一组实验的，即自己选择与哪一家供水公司签约，这也涉及到许多其他因素。</p>
<p>&emsp;当操纵变量与随机分配相结合是，科学家就可以排除那些归因于被试本身特征的解释了。随机分配与随机取样是不一样的。使用随机分配可以避免安慰剂效应以及选择效应等。随机实验可以通过重复验证来避免随机所产生的系统误差，因此可以得出令人信服的结论。操纵变量可以通过创设特殊条件来实现。</p>
<p>&emsp;本章主要介绍了控制变量的重要性，同时实验中通过创设特殊条件来排除其他因素的干扰并且通过随机重复实验可以得到令人信服的结论。</p>
<h2 id="第七章-“这不是真正的生活！”"><a href="#第七章-“这不是真正的生活！”" class="headerlink" title="第七章 “这不是真正的生活！”"></a>第七章 “这不是真正的生活！”</h2><p>&emsp;本章主要针对心理学的几点质疑做了科学的解释。</p>
<p>&emsp;首先，针对心理学实验中的非自然性的批评，作者指出科学实验的人为性并不是一种缺点，非自然的实验使得心理学家可以控制变量，从而得到可信的数据，虽然在非自然情况下取得的理论研究暂时不能直接应用到实际生活中，但是，理论到实际的应用是需要很多时间的，同时许多实际应用也间接的使用到这些理论。</p>
<p>&emsp;其次，针对心理学实验中并没有全部使用到随机取样得到问题，作者指出在应用研究中，其目的是预测一个非常具体的情景中的一个特定行为，如民意选举等，由于应用性质是直接的，样本的随机性和实验条件的代表行问题就很重要。同时，随机取样和随机分配是不同的。如果研究中使用了随机分配，那么它就是一项真实验，如果没有使用，则是相关研究。</p>
<p>&emsp;最后，针对“大二学生”问题，即在一些研究中，参与研究的被试为美国大学中的一些大二学生，质疑这些研究的结果是否具有推广性。作者承认这是一个研究中的问题，但是也指出1. 更加广范围的研究会完善这些理论研究，而不会完全去否定原有的研究结果；2. 许多心理学研究的是一些基本的如视觉系统，所有人的这些特征是相似的，因此对于这些研究其完全不是问题；3.研究结果的可重复性保证了其研究可以推广到不同地理区域中、社会经济地位等人群中。互联网的发展也为大二学生问题提供了一个有效的解决途径，可以通过互联网来对不同区域、不同背景的人体进行实验，同时，也有大量的实验证明心理学许多研究领域中的研究成果和之前的实验结果是相似的。但是，肯定会存在不同的实验结果，这也是推动所有科学发展的必经过程。</p>
<p>&emsp;作者在本章中也指出虽然许多实验在动物中实现，但是人类的行为与动物行为得出的规律非常地相似！ </p>
]]></content>
      <categories>
        <category>Psychology</category>
        <category>How to think about psychology</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>心理学</tag>
        <tag>这才是心理学</tag>
      </tags>
  </entry>
  <entry>
    <title>How to think about psychology 8-10</title>
    <url>/2021/06/01/How-to-think-about-psychology-8-10/</url>
    <content><![CDATA[<h1 id="这才是心理学（8-10）How-to-think-about-psychology"><a href="#这才是心理学（8-10）How-to-think-about-psychology" class="headerlink" title="这才是心理学（8-10）How to think about psychology"></a>这才是心理学（8-10）How to think about psychology</h1><h2 id="第八章-关联性和聚合性"><a href="#第八章-关联性和聚合性" class="headerlink" title="第八章 关联性和聚合性"></a>第八章 关联性和聚合性</h2><p>&emsp;爱因斯坦综合征：认为所有科学的进步都符合“飞跃”模式，就像爱因斯坦对物理学做出的贡献一样。</p>
<p>&emsp;关联性原则：一个新的科学理论，必须与先前已经确定的实证事实所关联，即兼容旧的事实。即使是爱因斯坦理论中存在许多概念重构，其在低俗运动情况下的解释也和牛顿理论所做的预测基本相同。我们需要警惕一些伪科学中违反了关联性原则，即声称先前的数据结论不相关。很多科学领域的进步靠的不是单一的突破，而是一系列难以描述的时断时续的过程。</p>
<p>&emsp;聚合性原则：在没有一个单独的实验可以帮助一锤定音，而是每个实验都至少帮助排除一些替代解释。可以从实验局限性和理论检验的角度分别理解聚合性原则。</p>
<span id="more"></span>
<p>&emsp;实验的局限性指出，实验总是可能以各种方式出错的，如果所有的实验都具有相似的局限性，那么实验结果就值得去怀疑，然而如果所有实验的局限性各不相同，那么所有实验得出的结果就会更加可信。聚合性原则要求我们将结论建立在大量略有差异的实验所得数据之上。</p>
<p>&emsp;理论检验指出，当一系列实验支持某个理论，同时又共同排除那些最重要的竞争理论时，研究则具有高度聚合性。虽然单一的实验并不能排除所有的替代解释，但是当所有能排除一些替代解释的实验组合起来，如果数据都呈现出某种特定的趋势，那么就能产生一个强有力的结论。即得出结论需要多个实验综合，并且评判是否具有聚合性。</p>
<p>&emsp;心理学实验的诊断性往往较低，即经常只能排除一小部分替代解释，所以得出结论需要建立在聚合性证据原则之上。需要警惕心理学中假设的证据仍然不明确时，不应当将其视为“已证实的理论”。同时针对某个实验或者部分实验的局限性，而忽略整体实验所得出的符合聚合性原则的结果的反驳，也是不可取的。在评估心理学证据时，心中要想的是科学共识，而不是重大突破，是渐进整合而不是大飞跃。</p>
<p>&emsp;聚合性原则也提醒我们，当对某个问题的初步研究结果似乎相互矛盾时，不应该绝望，这是得出真正结论、聚合所有实验的的必经之路。同时，元分析的统计技术也可以帮助将不同研究的证据组合起来形成一个结论，其使用一个通用指标来表示两个实现比较时得到的效应，从而对不同的研究效应进行比较，最后用标准的方法对结果进行统计学上的合并。心理学同医学等其他科学一样依赖于元分析得出结论。</p>
<h2 id="第九章-多重原因问题"><a href="#第九章-多重原因问题" class="headerlink" title="第九章 多重原因问题"></a>第九章 多重原因问题</h2><p>&emsp;本章主要强调人的行为是由多重原因决定的同时指出了客观承认多重原因的困难之处。</p>
<p>&emsp;作者指出任何一个特定行为都不是由一个而是由许多个不同的变量引起的。同时，多个变量造成的特定结果并不会降低变量的重要性，即使单个变量只能解释结果的一小部分。</p>
<p>&emsp;交互作用：一个自变量的效应大小可能依赖于另一个自变量水平。例如生物因素与环境因素的交互作用，只有在特定的环境因素下，给特定的人群以特定的生物因素，才可能造成某种后果。交互作用展示了造成结果的多个变量之间可能不是简单的相加。</p>
<p>&emsp;单一原因解释即人们拒绝承认多重原因，因为人们往往带有预设偏见（情感）去看待问题，认为所有原因是相互竞争的，认为强调一个原因必然会降低另一个原因的重要性。同时当不同的潜在原因依附于不同的意识形态立场时，承认多重原因的存在就会变得尤为困难。最后，由于多重原因的组合造成了某个特定的问题，因此也需要多种方法的组合才可以更加有效地解决这个问题。</p>
<h2 id="第十章-概率推理问题——人类认知的阿克琉斯之槌（弱点）"><a href="#第十章-概率推理问题——人类认知的阿克琉斯之槌（弱点）" class="headerlink" title="第十章 概率推理问题——人类认知的阿克琉斯之槌（弱点）"></a>第十章 概率推理问题——人类认知的阿克琉斯之槌（弱点）</h2><p>&emsp;作者指出我们所说的概率取视，是指有较大的可能性，但是并非所有的情况下都是如此。因此每个失败的预测并不是错误的。同时作者指出心理学揭示的几乎所有事实和关系都是用概率来表述的。</p>
<p>&emsp;“某某人”统计学：由于某些人知道有一个“某某”人与某个成熟的统计趋势相左，这个取视就会受到质疑。如吸烟问题中，吸烟者总会举出一些反例来反驳研究对吸烟有害健康的结论。人们通常使用“某某人”的论据来反驳心理学上的研究成果，但是心理学也是概率性的，其是对群体的总体趋势进行的预测，即总体预测或者统计预测。</p>
<p>&emsp;社会越复杂，人们就越需要概率思维。作者指出了概率推理中的一些常见误区：</p>
<p>&emsp;&emsp;（1）未充分使用概率信息</p>
<p>&emsp;&emsp;人们在做出决策时，具体的单个信息往往会压倒较为抽象的概率信息（也是鲜活性问题）。 人们也往往会忽略基础比率的存在。如假设1000个人中就有1个人携带HIV病毒，有一种检查可以检查出是否携带HIV但是存在5%的假阳性，即错误检查出病毒，那么随机检测1个人，其真的携带HIV的概率为多少？正确答案为约2%。具体解答为，1000人检测中有50会被错误地检测出病毒，因此最终结果为1/51.</p>
<p>&emsp;&emsp;（2）未能使用样本信息</p>
<p>&emsp;&emsp;较小的样本总是会产生更多的极端值。更多的样本往往会更加接近真实概率。</p>
<p>&emsp;&emsp;（3）赌徒谬误</p>
<p>&emsp;&emsp;人们倾向于将过去的事件和未来的事件联系起来，即原本无关的事件看成是有关的。如，若抛一枚硬币连续5次为正面朝上，第6次人们往往预测为反面朝上。但是实际是，两个事件的结果是相互独立的，一个事件的出现不会影响另一个事件出现的概率。但是人们总是错误地认为，在一个随机序列中不应出现重复或某种模式。这种错误的想法往往导致人们轻而易举的相信那些声称自己有通灵能力的人，他们就是利用人们这种错觉。但是真正的随机序列，往往在人们看来不像是随机的。</p>
]]></content>
      <categories>
        <category>Psychology</category>
        <category>How to think about psychology</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>心理学</tag>
        <tag>这才是心理学</tag>
      </tags>
  </entry>
  <entry>
    <title>中断设置和键鼠处理</title>
    <url>/2024/06/23/IDT&amp;GDT/</url>
    <content><![CDATA[<h2 id="VGA-设定"><a href="#VGA-设定" class="headerlink" title="VGA 设定"></a>VGA 设定</h2><p>首先简单看待调色板设定的相关程序，具体可以参考下面的链接：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">set_palette</span><span class="params">(<span class="keyword">int</span> start, <span class="keyword">int</span> end, <span class="keyword">unsigned</span> <span class="keyword">char</span> *rgb)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> eflags = io_load_eflags(); <span class="comment">// 记录标志</span></span><br><span class="line"></span><br><span class="line">  io_cli(); <span class="comment">// 禁止中断</span></span><br><span class="line"></span><br><span class="line">  io_out8(<span class="number">0x03c8</span>, start); <span class="comment">// vga 设定，参考：https://wiki.osdev.org/VGA_Hardware</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = start; i &lt;= end; i++) &#123;</span><br><span class="line">    io_out8(<span class="number">0x03c9</span>, rgb[<span class="number">0</span>] / <span class="number">4</span>);</span><br><span class="line">    io_out8(<span class="number">0x03c9</span>, rgb[<span class="number">1</span>] / <span class="number">4</span>);</span><br><span class="line">    io_out8(<span class="number">0x03c9</span>, rgb[<span class="number">2</span>] / <span class="number">4</span>);</span><br><span class="line">    rgb += <span class="number">3</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  io_store_eflags(eflags);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="GDT-amp-IDT"><a href="#GDT-amp-IDT" class="headerlink" title="GDT&amp;IDT"></a>GDT&amp;IDT</h2><h3 id="GDT"><a href="#GDT" class="headerlink" title="GDT"></a>GDT</h3><p>“global segment descriptor table”，全局段号记录表。将段号记录在内存的某个地方，然后将内存的起始地址和有效设定个数放在CPU的GDTR的（global segment descriptor table register）特殊寄存器中。段寄存器是16位，但是由于cpu设定原因，低3位不可用，所以真正可以使用的位数为13位，即可以分为8192个段。</p>
<span id="more"></span>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"> <span class="meta">#<span class="meta-keyword">define</span> ADR_GDT 0x00270000</span></span><br><span class="line"> <span class="meta">#<span class="meta-keyword">define</span> LIMIT_GDT 0x0000ffff</span></span><br><span class="line"></span><br><span class="line"> <span class="meta">#<span class="meta-keyword">define</span> ADR_BOOTPACK 0x00280000</span></span><br><span class="line"> <span class="meta">#<span class="meta-keyword">define</span> LIMIT_BOOTPACK 0x0007ffff</span></span><br><span class="line"></span><br><span class="line"> <span class="meta">#<span class="meta-keyword">define</span> AR_DATA32_RW 0x4092</span></span><br><span class="line"> <span class="meta">#<span class="meta-keyword">define</span> AR_CODE32_ER 0x409a</span></span><br><span class="line"> </span><br><span class="line"> <span class="class"><span class="keyword">struct</span> <span class="title">SegmentDescriptor</span> *<span class="title">gdt</span> =</span> (struct SegmentDescriptor *)ADR_GDT;</span><br><span class="line"> </span><br><span class="line"> <span class="class"><span class="keyword">struct</span> <span class="title">SegmentDescriptor</span> &#123;</span></span><br><span class="line">   <span class="keyword">short</span> limit_low, base_low;</span><br><span class="line">   <span class="keyword">char</span> base_mid, access_right;</span><br><span class="line">   <span class="keyword">char</span> limit_high, base_high;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"></span><br><span class="line"> <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt;= LIMIT_GDT / <span class="number">8</span>; i++) &#123;</span><br><span class="line">   set_segmdesc(gdt + i, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> set_segmdesc(gdt + <span class="number">1</span>, <span class="number">0xffffffff</span>, <span class="number">0x00000000</span>, AR_DATA32_RW);</span><br><span class="line"> set_segmdesc(gdt + <span class="number">2</span>, LIMIT_BOOTPACK, ADR_BOOTPACK, AR_CODE32_ER);</span><br><span class="line"> load_gdtr(LIMIT_GDT, ADR_GDT);</span><br></pre></td></tr></table></figure>
<p>这里设定了两个段，第一个段从0x00000000到0xffffffff，设定了cpu能管理内存的大小。第二个段从0x00270000到0x0007ffff共512KB，</p>
<p>函数的最后一个变量代表段属性（access right），因为12位段属性的高4放在limit high中，所以程序里有意把ar当作如下16位处理：</p>
<p>“xxxx0000xxxxxxx”。</p>
<p>ar的高4位称为扩展访问权，由“GD00”构成，G值得是Gbit，D指段模式，为1表示32位模式，为0表示16位模式。</p>
<p>ar低8位：</p>
<p>00000000（0x00）：未使用的记录表</p>
<p>10010010（0x92）：系统专用，可读写，不可执行</p>
<p>10011010（0x9a）：系统专用，可执行，可读不可写</p>
<p>11110010（0xf2）：应用程序用，可读写，不可执行</p>
<p>11111010（0xfa）：应用程序用，可执行，可读不可写</p>
<p>CPU处于系统模式还是应用模式，取决于执行中的应用程序是位于访问权为0x9a的段，还是位于访问权为0xfa的段。</p>
<h3 id="IDT"><a href="#IDT" class="headerlink" title="IDT"></a>IDT</h3><p>“interrput descriptor table “，中断记录表。IDT记录了0-255的中断号码与调用函数的关系。</p>
<figure class="highlight reasonml"><table><tr><td class="code"><pre><span class="line"> #define ADR_IDT <span class="number">0x0026f800</span></span><br><span class="line"> #define LIMIT_IDT <span class="number">0x000007ff</span></span><br><span class="line"> </span><br><span class="line">#define AR_INTGATE32 <span class="number">0x008e</span></span><br><span class="line"> </span><br><span class="line"> <span class="keyword">struct</span> GateDescriptor *idt = (<span class="keyword">struct</span> GateDescriptor *)ADR_IDT;</span><br><span class="line"> </span><br><span class="line"> <span class="keyword">struct</span> GateDescriptor &#123;</span><br><span class="line">   short offset_low, selector;</span><br><span class="line">   <span class="built_in">char</span> dw_count, access_right;</span><br><span class="line">   short offset_high;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"> <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt;= LIMIT_IDT<span class="operator"> / </span><span class="number">8</span>; i++) &#123;</span><br><span class="line">   set<span class="constructor">_gatedesc(<span class="params">idt</span> + <span class="params">i</span>, 0, 0, 0)</span>;</span><br><span class="line"> &#125;</span><br><span class="line"> load<span class="constructor">_idtr(LIMIT_IDT, ADR_IDT)</span>;</span><br><span class="line"></span><br><span class="line"> set<span class="constructor">_gatedesc(<span class="params">idt</span> + 0x21, (<span class="params">int</span>)</span>asm_int_handler21, <span class="number">2</span><span class="operator"> * </span><span class="number">8</span>, AR_INTGATE32);</span><br><span class="line"> set<span class="constructor">_gatedesc(<span class="params">idt</span> + 0x27, (<span class="params">int</span>)</span>asm_int_handler27, <span class="number">2</span><span class="operator"> * </span><span class="number">8</span>, AR_INTGATE32);</span><br><span class="line"> set<span class="constructor">_gatedesc(<span class="params">idt</span> + 0x2c, (<span class="params">int</span>)</span>asm_int_handler2c, <span class="number">2</span><span class="operator"> * </span><span class="number">8</span>, AR_INTGATE32);</span><br></pre></td></tr></table></figure>
<h3 id="PIC"><a href="#PIC" class="headerlink" title="PIC"></a>PIC</h3><p> “programmable interrupt controller”，可编程中断控制器。在设计上CPU只能处理一个中断，PIC是连接着CPU的芯片，它将8个中断信号（interrupt request）集合成一个中断信号。当有一个中断信号进来，就将唯一的输出管脚设置为ON，并且通知CPU。一般有两个PIC，从PIC通过2号IRQ与主PIC相连（硬件设计）。</p>
<h4 id="PIC寄存器"><a href="#PIC寄存器" class="headerlink" title="PIC寄存器"></a>PIC寄存器</h4><p>PIC的寄存器都是8位的。</p>
<p>IMR：”interrupt mask register”，中断屏蔽寄存器，如果某一位的值是1，则该位对应的IRQ信号被屏蔽。</p>
<p>ICW：”initial control word”，初始化控制数据。ICW共有4个，即存储了4个字节的数据。ICW1和ICW4 和 PIC与主板的配线方式、中断信号的电气信号有关，因此这里的电脑设定程序是一个固定值。ICW3是有关主-从连接设定，因为硬件已经不可更改，所以软件设定也是一致的（00000100）。</p>
<ul>
<li>我们可以编程的中断信号只有ICW2，ICW2决定了IRQ以哪一号中断通知CPU。</li>
</ul>
<p><em>注：中断发生之后，CPU命令PIC发送两个字节的数据，PIC利用信号线发送 “0xcd 0x？”的两个数据，从CPU看来，就是执行“INT 0x？”的指令。</em></p>
<p>我们可以设定的中断为“0x20 - 0x2f”，16个中断信号：IRQ0-16，由上述PIC发送。其中“0x00 - 0x1f”是CPU内部中断，不可以和外部中断冲突。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">init_pic</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 禁止所有中断</span></span><br><span class="line">  io_out8(PIC0_IMR, <span class="number">0xff</span>);</span><br><span class="line">  io_out8(PIC1_IMR, <span class="number">0xff</span>);</span><br><span class="line"></span><br><span class="line">  io_out8(PIC0_ICW1, <span class="number">0x11</span>); <span class="comment">// 边缘触发模式</span></span><br><span class="line">  io_out8(PIC0_ICW2, <span class="number">0x20</span>); <span class="comment">// IRQ0-7由INT20-27接收</span></span><br><span class="line">  io_out8(PIC0_ICW3, <span class="number">1</span> &lt;&lt; <span class="number">2</span>); <span class="comment">// PIC1由IRQ2连接</span></span><br><span class="line">  io_out8(PIC0_ICW4, <span class="number">0x01</span>); <span class="comment">// 无缓冲区模式</span></span><br><span class="line"></span><br><span class="line">  io_out8(PIC1_ICW1, <span class="number">0x11</span>); <span class="comment">// 边缘触发模式</span></span><br><span class="line">  io_out8(PIC1_ICW2, <span class="number">0x28</span>); <span class="comment">// IRQ8-15由INT28-2f接收</span></span><br><span class="line">  io_out8(PIC1_ICW3, <span class="number">2</span>); <span class="comment">// PIC1由IRQ2连接</span></span><br><span class="line">  io_out8(PIC1_ICW4, <span class="number">0x01</span>); <span class="comment">// 无缓冲区模式</span></span><br><span class="line"></span><br><span class="line">  io_out8(PIC0_IMR, <span class="number">0xfb</span>); <span class="comment">// PIC1以外中断全部禁止</span></span><br><span class="line">  io_out8(PIC1_IMR, <span class="number">0xff</span>); <span class="comment">// 禁止全部中断</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="中断处理程序"><a href="#中断处理程序" class="headerlink" title="中断处理程序"></a>中断处理程序</h2><p>鼠标处理为例，鼠标中断处理程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">int_handler2c</span><span class="params">(<span class="keyword">int</span> *esp)</span> </span>&#123;</span><br><span class="line">  io_out8(PIC1_OCW2, <span class="number">0x64</span>); <span class="comment">// 通知PIC1 IRQ-12的受理已经完成</span></span><br><span class="line">  io_out8(PIC0_OCW2, <span class="number">0x62</span>); <span class="comment">// 通知PIC0 IRQ-02的受理已经完成</span></span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">char</span> data = io_in8(PORT_KEYDAT);</span><br><span class="line">  </span><br><span class="line">  fifo8_put(&amp;mousefifo, data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>IRQ-12是从PIC的4号，因为主/从PIC的协调不能自动完成，因此需要首先通知从PIC，再通知主PIC完成鼠标的中断处理。</p>
<p>这里使用一个队列来处理不同的中断，因为中断通知完成之后，会处理屏幕信息比较耗时，如果一直等待处理完成再打开中断的话会导致系统不连贯，以及不能从网上接受数据的情况。</p>
<p>中断处理完成之后必须使用IRETD指令，所以需要使用汇编：</p>
<figure class="highlight x86asm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">asm_int_handler2c:</span></span><br><span class="line">  <span class="keyword">PUSH</span>    <span class="built_in">ES</span></span><br><span class="line">  <span class="keyword">PUSH</span>    <span class="built_in">DS</span></span><br><span class="line">  <span class="keyword">PUSHAD</span></span><br><span class="line">  <span class="keyword">MOV</span>     <span class="built_in">EAX</span>, <span class="built_in">ESP</span></span><br><span class="line">  <span class="keyword">PUSH</span>    <span class="built_in">EAX</span></span><br><span class="line">  <span class="keyword">MOV</span>     <span class="built_in">AX</span>, <span class="built_in">SS</span></span><br><span class="line">  <span class="keyword">MOV</span>     <span class="built_in">DS</span>, <span class="built_in">AX</span></span><br><span class="line">  <span class="keyword">MOV</span>     <span class="built_in">ES</span>, <span class="built_in">AX</span></span><br><span class="line">  <span class="keyword">CALL</span>    int_handler2c</span><br><span class="line">  <span class="keyword">POP</span>     <span class="built_in">EAX</span></span><br><span class="line">  <span class="keyword">POPAD</span></span><br><span class="line">  <span class="keyword">POP</span>     <span class="built_in">DS</span></span><br><span class="line">  <span class="keyword">POP</span>     <span class="built_in">ES</span></span><br><span class="line">  <span class="keyword">IRETD</span></span><br></pre></td></tr></table></figure>
<h2 id="键-鼠初始化"><a href="#键-鼠初始化" class="headerlink" title="键/鼠初始化"></a>键/鼠初始化</h2><h3 id="键盘初始化："><a href="#键盘初始化：" class="headerlink" title="键盘初始化："></a>键盘初始化：</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PORT_KEYDAT 0x0060</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PORT_KEYSTA 0x0064</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PORT_KEYCMD 0x0064</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> KEYSTA_SEND_NOTREADY 0x02</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> KEYCMD_WRITE_MODE 0x60</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> KBC_MODE 0x47</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">wait_KBC_sendready</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="keyword">if</span> ((io_in8(PORT_KEYSTA) &amp; KEYSTA_SEND_NOTREADY) == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">init_keyboard</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">  wait_KBC_sendready();</span><br><span class="line">  io_out8(PORT_KEYCMD, KEYCMD_WRITE_MODE);</span><br><span class="line">  wait_KBC_sendready();</span><br><span class="line">  io_out8(PORT_KEYDAT, KBC_MODE);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在初始化程序中，一边确认是否可以往键盘控制电路发送信息，一边发送模式设定指令。模式设定的指令是0x60，利用鼠标模式的模式号码是0x47.</p>
<h3 id="键盘初始化：-1"><a href="#键盘初始化：-1" class="headerlink" title="键盘初始化："></a>键盘初始化：</h3><figure class="highlight reasonml"><table><tr><td class="code"><pre><span class="line">#define KEYCMD_SENDTO_MOUSE <span class="number">0xd4</span></span><br><span class="line">#define MOUSECMD_ENABLE <span class="number">0xf4</span></span><br><span class="line"></span><br><span class="line">void enable<span class="constructor">_mouse(<span class="params">struct</span> FIFO32 <span class="operator">*</span><span class="params">fifo</span>, <span class="params">int</span> <span class="params">data0</span>, <span class="params">struct</span> MouseDec <span class="operator">*</span><span class="params">mdec</span>)</span> &#123;</span><br><span class="line">  mousefifo = fifo;</span><br><span class="line">  mousedata0 = data0;</span><br><span class="line"></span><br><span class="line">  wait<span class="constructor">_KBC_sendready()</span>;</span><br><span class="line">  io<span class="constructor">_out8(PORT_KEYCMD, KEYCMD_SENDTO_MOUSE)</span>;</span><br><span class="line">  wait<span class="constructor">_KBC_sendready()</span>;</span><br><span class="line">  io<span class="constructor">_out8(PORT_KEYDAT, MOUSECMD_ENABLE)</span>;</span><br><span class="line"></span><br><span class="line">  mdec-&gt;phase = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>发送鼠标激活指令，归根结底还是要向键盘控制器发送指令。</p>
<p>如果往键盘控制电路发送指令0xd4，下一个数据会自动发送给鼠标。</p>
<p>收到鼠标激活指令后，会给CPU发送0xfa中断，因此最后一行为了区分第一个鼠标中断信号和后续的鼠标中断，在接收到0xfa后，鼠标的phase就不会再是0。</p>
]]></content>
      <categories>
        <category>30daysOS</category>
        <category>CS</category>
      </categories>
      <tags>
        <tag>CS</tag>
        <tag>30daysOS</tag>
      </tags>
  </entry>
  <entry>
    <title>初识操作系统（启动区和初始化）</title>
    <url>/2024/06/02/%E5%88%9D%E8%AF%86%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h2 id="工具介绍"><a href="#工具介绍" class="headerlink" title="工具介绍"></a>工具介绍</h2><ul>
<li><p>GCC: GNU Compiler Collection, 可以编译多语言的编译器。编译流程：预处理-&gt;编译-&gt;汇编-&gt;链接。</p>
<ul>
<li><p>参考：</p>
<p>介绍：<a href="https://www.cnblogs.com/QG-whz/p/5456720.html">https://www.cnblogs.com/QG-whz/p/5456720.html</a></p>
<p>GCC官网：<a href="https://gcc.gnu.org/onlinedocs/gcc/index.html">https://gcc.gnu.org/onlinedocs/gcc/index.html</a></p>
</li>
</ul>
</li>
</ul>
<ul>
<li><p>NASM：NASM是一个为可移植性与模块化而设计的一个80x86的汇编器。文件格式为 xx.asm</p>
<ul>
<li><p>参考：</p>
<p>官方手册：<a href="https://www.nasm.us/xdoc/2.16.03/html/nasmdoc0.html">https://www.nasm.us/xdoc/2.16.03/html/nasmdoc0.html</a></p>
</li>
</ul>
</li>
<li><p>QEMU: 一个开源的计算机仿真器和虚拟器。可以模拟不同架构的处理器（arm、x86、risc-v），也可以作为虚拟机使用</p>
<ul>
<li><p>参考：</p>
<p>官方文档：<a href="https://www.qemu.org/docs/master/about/index.html">https://www.qemu.org/docs/master/about/index.html</a></p>
</li>
</ul>
</li>
<li><p>MAKEFILE: 批处理工具，可以通过其指定如何编译链接文件</p>
<ul>
<li>参考：<a href="http://makefiletutorial.foofun.cn/#%E6%9C%89%E4%BB%80%E4%B9%88%E6%9B%BF%E4%BB%A3%E6%96%B9%E6%A1%88%EF%BC%9F">http://makefiletutorial.foofun.cn/#%E6%9C%89%E4%BB%80%E4%B9%88%E6%9B%BF%E4%BB%A3%E6%96%B9%E6%A1%88%EF%BC%9F</a> </li>
</ul>
</li>
</ul>
<span id="more"></span>
<h2 id="寄存器介绍"><a href="#寄存器介绍" class="headerlink" title="寄存器介绍"></a>寄存器介绍</h2><p>介绍在8086中存在的寄存器。</p>
<p>通用寄存器：</p>
<ul>
<li>AX，accumulator，累加寄存器</li>
<li>CX，counter，计数寄存器</li>
<li>DX，data，数据寄存器</li>
<li>BX，base，基址寄存器</li>
</ul>
<p>X表示extend，因为之前的寄存器都是8位的。因此8位寄存器有：AL，AH，CL，CH，DL，DH，BL，BH。</p>
<p>另外32位通用寄存器为：EAX，ECX，EBX，EDX。</p>
<hr>
<p>索引寄存器，包含段地址的偏移量：</p>
<ul>
<li>SP，stack pointer，栈指针寄存器</li>
<li>BP，base pointer，基址指针寄存器</li>
<li>SI，source index，源变址寄存器</li>
<li>DI，destination index，目的变址寄存器</li>
</ul>
<p>32位索引寄存器为：ESP，EBP，ESI，EDI。</p>
<hr>
<p>段寄存器，作程序指令，数据或栈的基础位置：</p>
<ul>
<li>CS，code segment，代码段寄存器</li>
<li>SS，stack segment，栈段寄存器</li>
<li>DS，data segment，数据段寄存器</li>
<li>ES，extra segment，附加段寄存器</li>
<li>FS，segment part2，没用名称</li>
<li>GS，segment part3， 没有名称</li>
</ul>
<hr>
<p>状态和控制寄存器：</p>
<ul>
<li>IP，instruction pointer，指令指针寄存器，它是从 Code Segment 代码寄存器处的偏移来存储执行的下一条指令</li>
<li>FLAG : Flag 寄存器用于存储当前进程的状态</li>
</ul>
<h2 id="启动区介绍-bootloader"><a href="#启动区介绍-bootloader" class="headerlink" title="启动区介绍(bootloader)"></a>启动区介绍(bootloader)</h2><figure class="highlight x86asm"><table><tr><td class="code"><pre><span class="line">CYLS   <span class="built_in">EQU</span>   <span class="number">10</span></span><br><span class="line"><span class="symbol">entry:</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">AX</span>, <span class="number">0</span>             <span class="comment">; 初始化寄存器</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">SS</span>, <span class="built_in">AX</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">SP</span>, <span class="number">0x7c00</span>				<span class="comment">;https://wiki.osdev.org/Memory_Map_(x86)</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">DS</span>, <span class="built_in">AX</span></span><br><span class="line"></span><br><span class="line"><span class="comment">; 读取硬盘</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">AX</span>, <span class="number">0x0820</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">ES</span>, <span class="built_in">AX</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="number">CH</span>, <span class="number">0</span>             <span class="comment">; 柱面0</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="number">DH</span>, <span class="number">0</span>             <span class="comment">; 磁头0</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">CL</span>, <span class="number">2</span>             <span class="comment">; 扇区2，从第二个开始读，第一个默认读取</span></span><br><span class="line"></span><br><span class="line"><span class="symbol">readloop:</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">SI</span>, <span class="number">0</span>             <span class="comment">; 记录失败次数的寄存器</span></span><br><span class="line"><span class="symbol">retry:</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="number">AH</span>, <span class="number">0x02</span>          <span class="comment">; AH=0x02：读盘</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">AL</span>, <span class="number">1</span>             <span class="comment">; 1个扇区</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">BX</span>, <span class="number">0</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">DL</span>, <span class="number">0x00</span>          <span class="comment">; A驱动器</span></span><br><span class="line">  <span class="keyword">INT</span>   <span class="number">0x13</span>              <span class="comment">; 调用磁盘BIOS</span></span><br><span class="line">  <span class="keyword">JNC</span>   next              <span class="comment">; 没出错跳转到next</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">ADD</span>   <span class="built_in">SI</span>, <span class="number">1</span>             <span class="comment">; 失败次数+1</span></span><br><span class="line">  <span class="keyword">CMP</span>   <span class="built_in">SI</span>, <span class="number">5</span>             <span class="comment">; 失败次数是否达到5次</span></span><br><span class="line">  <span class="keyword">JAE</span>   error             <span class="comment">; 失败次数达到5次跳转到error</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="number">AH</span>, <span class="number">0x00</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">DL</span>, <span class="number">0x00</span>          <span class="comment">; A驱动器</span></span><br><span class="line">  <span class="keyword">INT</span>   <span class="number">0x13</span>              <span class="comment">; 重置驱动器</span></span><br><span class="line">  <span class="keyword">JMP</span>   retry</span><br><span class="line"></span><br><span class="line"><span class="symbol">next:</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">AX</span>, <span class="built_in">ES</span>            <span class="comment">; 把内存地址后移0x200，移动一个软盘的位置</span></span><br><span class="line">  <span class="keyword">ADD</span>   <span class="built_in">AX</span>, <span class="number">0x0020</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">ES</span>, <span class="built_in">AX</span>            <span class="comment">; 实现ES += 0x0020的目的</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">; 扇区范围 1～18</span></span><br><span class="line">  <span class="keyword">ADD</span>   <span class="built_in">CL</span>, <span class="number">1</span>             <span class="comment">; 扇区加1</span></span><br><span class="line">  <span class="keyword">CMP</span>   <span class="built_in">CL</span>, <span class="number">18</span>            <span class="comment">; 扇区是否达到18</span></span><br><span class="line">  <span class="keyword">JBE</span>   readloop          <span class="comment">; 小于等于18扇区则跳转到readloop</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">CL</span>, <span class="number">1</span>             <span class="comment">; 恢复到扇区1</span></span><br><span class="line">  <span class="comment">; 磁头范围 0～1（正面0，反面1）</span></span><br><span class="line">  <span class="keyword">ADD</span>   <span class="number">DH</span>, <span class="number">1</span></span><br><span class="line">  <span class="keyword">CMP</span>   <span class="number">DH</span>, <span class="number">2</span></span><br><span class="line">  <span class="keyword">JB</span>    readloop          <span class="comment">; 磁头未达到2则跳转到readloop</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="number">DH</span>, <span class="number">0</span></span><br><span class="line">  <span class="comment">; 柱面范围 0 ～ 79</span></span><br><span class="line">  <span class="keyword">ADD</span>   <span class="number">CH</span>, <span class="number">1</span></span><br><span class="line">  <span class="keyword">CMP</span>   <span class="number">CH</span>, CYLS</span><br><span class="line">  <span class="keyword">JB</span>    readloop          <span class="comment">; 读取指定数量的柱面，未达到CYLS则跳转readloop</span></span><br><span class="line"></span><br><span class="line"><span class="comment">; 读取完毕，跳转到haribote.sys执行</span></span><br><span class="line">  <span class="keyword">MOV</span>   [<span class="number">0x0ff0</span>], <span class="number">CH</span>      <span class="comment">; 记下IPL读了多远</span></span><br><span class="line">  <span class="keyword">JMP</span>   <span class="number">0xc200</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>初始化寄存器，其中0x7c00表示程序装载的位置，可以参考链接中的内存分布图，0x00007C00 — 0x00007DFF为启动区装载地址（IBM规定）。参考：<a href="https://www.glamenv-septzen.net/en/view/6">https://www.glamenv-septzen.net/en/view/6</a></p>
<p>初始化之后为读盘操作，这里读了10个柱面，每个柱面18个扇区，2个磁头（1张软盘有80个柱面，2个磁头，18个扇区，一个扇区512字节），即C0-H0-S1 -&gt; C9-H1-S18，共180KB内容。INT指令为调用BIOS函数。</p>
<p>整体地址：</p>
<pre><code>+ 0x00007C00 -- 0x00007DFF：启动区状态地址
+ 0x000008000 -- 0x000008200：启动区内容，默认读取
+ 0x000008200 -- 0x00000E600：软盘剩下内容
</code></pre><p>一般向一个空软盘保存文件时：</p>
<ol>
<li>文件名会写在0X002600以后的地方</li>
<li>文件的内容会写在0X004200以后的地方</li>
</ol>
<p>因此磁盘的内容位于内存0X8000+0X4200=0XC200的地方，即最后的jump语句执行的地方。</p>
<h2 id="初始化介绍"><a href="#初始化介绍" class="headerlink" title="初始化介绍"></a>初始化介绍</h2><figure class="highlight x86asm"><table><tr><td class="code"><pre><span class="line"><span class="comment">; 有关BOOT_INFO</span></span><br><span class="line">CYLS    <span class="built_in">EQU</span>   <span class="number">0x0ff0</span>      <span class="comment">; 设置启动区</span></span><br><span class="line">LEDS    <span class="built_in">EQU</span>   <span class="number">0x0ff1</span></span><br><span class="line">VMODE   <span class="built_in">EQU</span>   <span class="number">0x0ff2</span>      <span class="comment">; 关于颜色数目的信息，颜色的位数</span></span><br><span class="line">SCRNX   <span class="built_in">EQU</span>   <span class="number">0x0ff4</span>      <span class="comment">; 分辨率X</span></span><br><span class="line">SCRNY   <span class="built_in">EQU</span>   <span class="number">0x0ff6</span>      <span class="comment">; 分辨率Y</span></span><br><span class="line">VRAM    <span class="built_in">EQU</span>   <span class="number">0x0ff8</span>      <span class="comment">; 图像缓冲区的起始位置</span></span><br><span class="line"></span><br><span class="line"><span class="symbol">entry:</span></span><br><span class="line"><span class="comment">; 设置屏幕模式</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">AL</span>, <span class="number">0x13</span>          <span class="comment">; VGA显卡，320x200x8 bit</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="number">AH</span>, <span class="number">0x00</span></span><br><span class="line">  <span class="keyword">INT</span>   <span class="number">0x10</span> 							<span class="comment">; 调用bios</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">BYTE</span> [VMODE], <span class="number">8</span>   <span class="comment">; 屏幕的模式</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">WORD</span> [SCRNX], <span class="number">320</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">WORD</span> [SCRNY], <span class="number">200</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="built_in">DWORD</span> [VRAM], <span class="number">0x000a0000</span></span><br><span class="line"></span><br><span class="line"><span class="comment">; 用BIOS取得键盘上各种LED指示灯的状态</span></span><br><span class="line">  <span class="keyword">MOV</span>   <span class="number">AH</span>, <span class="number">0x02</span></span><br><span class="line">  <span class="keyword">INT</span>   <span class="number">0x16</span>              <span class="comment">; 键盘BIOS</span></span><br><span class="line">  <span class="keyword">MOV</span>   [LEDS], <span class="built_in">AL</span></span><br></pre></td></tr></table></figure>
<p>将画面模式信息保存在内存里面，同时把像素数、颜色数、键盘信息保存在0x0ff0附近，从内存分布图上来看，这块没有被使用。</p>
<p>参考BIOS设定：<a href="https://wiki.osdev.org/BIOS">https://wiki.osdev.org/BIOS</a></p>
<h3 id="MAKEFILE详解："><a href="#MAKEFILE详解：" class="headerlink" title="MAKEFILE详解："></a>MAKEFILE详解：</h3><p>来源：<a href="https://github.com/ghosind/HariboteOS">https://github.com/ghosind/HariboteOS</a></p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">AS		:= nasm</span><br><span class="line">CC		:= $(GCCPREFIX)gcc</span><br><span class="line">LD		:= $(GCCPREFIX)ld</span><br><span class="line"></span><br><span class="line">ipl<span class="selector-class">.bin</span>:</span><br><span class="line">	$(AS) -f bin ipl<span class="selector-class">.asm</span> -o ipl<span class="selector-class">.bin</span> -l ipl.lst</span><br><span class="line"></span><br><span class="line">asmhead<span class="selector-class">.bin</span>:</span><br><span class="line">	$(AS) -f elf asmhead<span class="selector-class">.asm</span> -o asmhead<span class="selector-class">.bin</span> -l asmhead.lst</span><br><span class="line"></span><br><span class="line">bootpack<span class="selector-class">.bin</span>:</span><br><span class="line">	$(CC) $(CFLAGS) -c bootpack<span class="selector-class">.c</span> -o bootpack.bin</span><br><span class="line"></span><br><span class="line">func<span class="selector-class">.bin</span>:</span><br><span class="line">	$(AS) -f elf func<span class="selector-class">.asm</span> -o func<span class="selector-class">.bin</span> -l func.lst</span><br><span class="line"></span><br><span class="line">haribote<span class="selector-class">.sys</span>: asmhead<span class="selector-class">.bin</span> bootpack<span class="selector-class">.bin</span> func.bin</span><br><span class="line">	$(LD) -m elf_i386 --oformat binary asmhead<span class="selector-class">.bin</span> bootpack<span class="selector-class">.bin</span> func<span class="selector-class">.bin</span> -o haribote<span class="selector-class">.sys</span> -T haribote.ld</span><br><span class="line"></span><br><span class="line">image: ipl<span class="selector-class">.bin</span> haribote.sys</span><br><span class="line">	<span class="selector-tag">dd</span> <span class="keyword">if</span>=/dev/zero of=$(IMG) bs=<span class="number">512</span> count=<span class="number">2880</span></span><br><span class="line">	<span class="selector-tag">dd</span> <span class="keyword">if</span>=ipl<span class="selector-class">.bin</span> of=$(IMG) bs=<span class="number">512</span> count=<span class="number">1</span> conv=notrunc</span><br><span class="line">	<span class="selector-tag">dd</span> <span class="keyword">if</span>=haribote<span class="selector-class">.sys</span> of=$(IMG) seek=<span class="number">33</span> bs=<span class="number">512</span> conv=notrunc （<span class="number">512</span>*<span class="number">33</span>=<span class="number">0</span>x4200）</span><br></pre></td></tr></table></figure>
<p>gcc已经做好编译汇编的操作，在haribote.sys进行链接，最后用dd命令生成img文件。</p>
<h2 id="Linux启动介绍"><a href="#Linux启动介绍" class="headerlink" title="Linux启动介绍"></a>Linux启动介绍</h2><p>内核启动过程：<a href="https://www.cnblogs.com/anywherego/p/18217546">https://www.cnblogs.com/anywherego/p/18217546</a></p>
<p>启动区需要看grub源码～</p>
]]></content>
      <categories>
        <category>30daysOS</category>
        <category>CS</category>
      </categories>
      <tags>
        <tag>CS</tag>
        <tag>30daysOS</tag>
      </tags>
  </entry>
</search>
